# 网易云接入排期 · 第一期：接口调试

> 文档版本：v0.1
> 范围：**只做协议探针与接口调试**，不做产品 UI 接入、不做登录、不做网易特色功能
> 依据：[网易云接入对比酷狗.md](网易云接入对比酷狗.md) P0 · [多音源接入方案.md](多音源接入方案.md) §7
> 参考：`D:\work\NeriPlayer` `core/api/netease/` + `NeteaseApiProbeViewModel`
> 口径：1 人全栈 · 5 人日/周 · 有效编码约 6h/日
> 前置：**多音源阶段 A（M5）已完成**（`MusicSource` / `KugouSource` / `platform:id`），可平行开探针
>
> **本文是网易云排期主文档**；后续第二期（可听闭环）、三期（账号）另追加章节，不塞进 `工期规划.md`。

---

## 一、一期目标（DoD）

**一句话**：在 debug 探针里，用网易 weapi/eapi 真接口跑通「搜索 → 播放 URL → 歌词」，并留下可复现的 Dart 客户端。

| # | 验收项 | 通过标准 |
| --- | --- | --- |
| D1 | weapi 加密 | 任一 weapi 接口返回 `code=200` 业务 JSON（非 -460 风控误报格式） |
| D2 | eapi 加密 | `song/enhance/player/url/v1` 或 `song/lyric/v1` 返回 200 |
| D3 | 搜索 | 关键词出歌单（id / 歌名 / 歌手 / 时长 / 封面） |
| D4 | 播放 URL | 返回可播 `url`；记下 `level` / `type` / 是否试听 |
| D5 | 歌词 | 拿到 LRC 或 YRC 原文；能标出是否含译文/音译 |
| D6 | 会话 | 游客态可重复调用；记录 Cookie 字段清单 |
| D7 | 失败映射 | 301 / 无 url / fee 等能区分并打印 |
| D8 | 工程 | `flutter analyze` 零问题；探针可从 debug 页或 CLI 重复跑 |

**明确不做（一期）**

- 接入 `MusicSource` / 播放器 / 搜索页 UI
- 扫码 / 短信 / 密码登录、`yd_token`
- 歌单/专辑/歌手详情、每日推荐、FM、雷达、我喜欢
- Windows 专项
- 网易评论、热搜

---

## 二、需要哪些接口（一期必调清单）

> 端点以 NeriPlayer `NeteaseClient` 为准。加密与会话是**基础设施**，不算业务接口，但必须先通。

### 2.1 基础设施（必做，无 UI）

| ID | 模块 | 对应 NeriPlayer | 说明 | 优先级 |
| --- | --- | --- | --- | --- |
| I0 | `NeteaseCrypto.weApiEncrypt` | `NeteaseCrypto.weApiEncrypt` | AES-CBC×2 + RSA(modPow 无填充) → `params`/`encSecKey` | P0 |
| I0b | `NeteaseCrypto.eApiEncrypt` | `eApiEncrypt` | AES-ECB + MD5 盐拼接 → `params` | P0 |
| I0c | （可选）`linuxApiEncrypt` / `anonymous` | 同名 | 一期可只留桩，业务不依赖 | P2 |
| S1 | Cookie 会话 | `NeteaseRequestSession` | 内存 + 持久化 CookieJar；合并 `os=pc`/`appver` | P0 |
| S2 | WEAPI 预热 | `ensureWeapiSession` | GET `https://music.163.com/` 取 `__csrf` | P0 |
| S3 | HTTP | OkHttp→Dio | Form POST；解 Brotli/Gzip；UA/Referer 头 | P0 |
| S4 | 通用 call | `callWeApi` / `callEApi` | host：`music.163.com/weapi` · `interface.music.163.com/eapi` | P0 |

### 2.2 业务接口（一期只调这 5 个 + 1 校验）

| ID | 用途 | 方法（Neri 命名） | 端点 | 加密 | 关键参数 | 成功判据 |
| --- | --- | --- | --- | --- | --- | --- |
| **A1** | 搜索单曲 | Neri：`cloudsearch/get/web`；**本仓默认** `/weapi/search/get` | `POST /weapi/search/get`（cloudsearch 实测 50000005） | WEAPI | `s, type=1, limit, offset` | `result.songs[]` |
| **A2** | 播放 URL（主） | `getSongDownloadUrl` | `POST /eapi/song/enhance/player/url/v1` | EAPI | `ids=[id], level, encodeType=flac` | `data[0].url` 非空；记 `level/type/size/freeTrialInfo` |
| **A2b** | 播放 URL（备） | `getSongUrl` | `POST /weapi/song/enhance/player/url` | WEAPI | `ids=[id], br=320000` | 同上（A2 失败时对照） |
| **A3** | 歌词（主） | `getLyricNew` | `POST /eapi/song/lyric/v1` | EAPI | `id, lv=0, tv=1, rv=0, yv=1, ytv=1, yrv=0` | `lrc` 或 `yrc` 非空 |
| **A3b** | 歌词（备） | （CloudMusicSearchApi 路径） | `GET /api/song/lyric?id=&lv=-1&tv=-1&…` | 明文 GET | query | 对照 A3 |
| **A4** | 曲目详情（校验用） | `getSongDetail` | `POST /weapi/v3/song/detail` | WEAPI | `c=[{"id":…}], ids=[…]` | 歌名/歌手/专辑与搜索一致 |

**一期刻意不调**（二期+）：歌单/专辑/歌手、每日推荐、FM、用户歌单、likeSong、扫码、账号、雷达、精品歌单、评论、热搜。

### 2.3 错误码观测表（调试时必记）

| 场景 | 现象 | 记入 |
| --- | --- | --- |
| 需登录 | body `code == 301` | `LoginRequired` |
| 无版权 / fee | `data[0].code != 200` 或无 `url`，`fee>0` | `NoPermission` |
| 试听 | `freeTrialInfo` 非空且时长 << 原曲 | `isPreviewClip` |
| 风控 | `-460` / 验证码提示 | `RateLimited` + 请求头/Cookie 快照 |
| 解析失败 | 非 JSON / 字段缺失 | `UpstreamChanged` + 原文片段 |

---

## 三、调试载体（要做什么工具）

| 载体 | 形态 | 用途 |
| --- | --- | --- |
| **T1 CLI 探针** | `app/tool/probe_netease_api.dart` | 无 UI、可脚本化；对齐现有 `tool/probe_*.dart` |
| **T2 Debug 页** | `features/debug` 下 `NeteaseApiProbe` | 真机/桌面点按钮跑 A1–A4，看 JSON/日志（对齐 Neri `NeteaseApiProbeViewModel`） |
| **T3 日志** | `NetworkLogHub` | 打印 URL / 是否 csrf / 响应 code / 片段 |

CLI 与 Debug 页**共用**同一套 `core/api/netease/`，禁止两套加密实现。

一期产物代码布局：

```text
app/lib/core/api/netease/
  netease_crypto.dart      # I0
  netease_endpoints.dart   # 常量
  netease_client.dart      # S1–S4（Cookie/预热并入此处，无独立 session 文件）+ A1–A4
  netease_failures.dart    # code → SourceFailure
app/tool/probe_netease_api.dart
app/lib/features/debug/netease_api_probe_*.dart   # T2（未做，二期补）
```

---

## 四、任务拆解与排期（细）

> 依赖链：A 加密 → B 会话/HTTP → C 业务四接口 → D 探针载体 → E 验收记录。  
> 总计 **8.5 人日 ≈ 2 周内 1.7 周**（含缓冲可到 9.5）。

| # | 任务 | 产出 | 人日 | 依赖 | 验收 |
| --- | --- | --- | --- | --- | --- |
| **W1 · 协议底座** | | | **4** | | |
| 1.1 | `NeteaseCrypto.weApiEncrypt` + 单测 | 固定 payload 的 `params/encSecKey` 形状正确 | 1 | — | 单测：JSON→双 AES→RSA hex |
| 1.2 | `NeteaseCrypto.eApiEncrypt` + 单测 | `params` 大写 hex | 0.5 | — | 与 Neri 样例对照 |
| 1.3 | `NeteaseEndpoints` + Dio 客户端（UA/Referer/Brotli/Gzip） | 可 POST Form | 0.5 | — | 对 `music.163.com/` GET 200 |
| 1.4 | Cookie 会话 + 持久化 + 合并规则 | `NeteaseSession` | 1 | 1.3 | 响应 Set-Cookie 可回传 |
| 1.5 | `ensureWeapiSession`（`__csrf` 预热） | WEAPI 请求前自动预热 | 0.5 | 1.4 | 请求 query 含 `csrf_token` |
| 1.6 | `callWeApi` / `callEApi` | 统一入口 | 0.5 | 1.1–1.5 | 手工调通 1 个 weapi |
| **W1 出口** | | | | | **加密 + 会话可发真实请求** |
| **W2 · 业务接口 + 探针** | | | **4.5** | | |
| 2.1 | **A1 搜索** `cloudsearch` + mapper | `List<ProbeSong>` | 0.5 | 1.6 | 关键词出列表 |
| 2.2 | **A2 播放 URL** EAPI v1 + 解析 `url/level/type/freeTrialInfo` | `ProbePlayUrl` | 1 | 1.6 | 任一 songId 有 url |
| 2.3 | **A2b** WEAPI 播放 URL 对照 | 同上 | 0.25 | 2.2 | A2 失败时可切换 |
| 2.4 | **A3 歌词** EAPI `lyric/v1` + YRC/LRC/译文字段拆解 | `ProbeLyric` | 0.75 | 1.6 | 打印 yrc/lrc 有无 |
| 2.5 | **A3b** GET `/api/song/lyric` 对照 | 同上 | 0.25 | 2.4 | |
| 2.6 | **A4 曲目详情**（校验歌名/歌手） | 简单 DTO | 0.25 | 1.6 | 与 A1 一致 |
| 2.7 | 失败映射 `code/fee/url` → 统一文案 | `mapNeteaseFailure` | 0.5 | 2.2 | 301/无 url 可区分 |
| 2.8 | **T1 CLI** `probe_netease_api.dart` | 一键：搜索→详情→URL→歌词 | 0.5 | 2.1–2.6 | `dart run tool/…` 可重复 |
| 2.9 | **T2 Debug 页**（4 按钮 + JSON 区 + 日志） | 设置/调试入口 | 0.5 | 2.8 | 真机/桌面可点 |
| **W2 出口** | | | | | **D1–D7 全过** |
| **缓冲** | | | **0.5–1** | | |
| 3.1 | 踩坑熄火（Brotli、csrf、风控 -460） | 记录进本文附录 | 0.5–1 | — | |
| | **合计** | | **9–9.5** | | **约 2 周** |

### 4.1 依赖图

```text
1.1 weapi ──┐
1.2 eapi  ──┼─ 1.3 Dio ─ 1.4 Session ─ 1.5 csrf ─ 1.6 call* ─┬─ 2.1 搜索 ─┐
            │                                                ├─ 2.2 URL  ─┼─ 2.7 失败映射 ─ 2.8 CLI ─ 2.9 Debug页
            │                                                ├─ 2.4 歌词 ─┤
            │                                                └─ 2.6 详情 ─┘
            │                         2.3/2.5 对照口（并行，不挡主链）
```

### 4.2 裁剪与并行

| 策略 | 内容 |
| --- | --- |
| 可砍 | 2.3 / 2.5 对照口（主口通即可）→ 省 **0.5** |
| 可并 | 2.1 与 2.2 两人并行（若双人） |
| 不可砍 | 1.1 / 1.2 / 1.4 / 1.5 / 2.2 / 2.4 / 2.8 |
| 范围锁 | 任何「顺便接 UI / 接登录」进 backlog，不进一期 |

---

## 五、一期验收脚本（人工 15 分钟）

1. `dart run tool/probe_netease_api.dart --keyword 周杰伦`（或 Debug 页点「搜索」）
2. 看到 ≥5 首：id / 歌名 / 歌手
3. 选第一首 → 「播放 URL」→ 打印 `url` 非空 + `level`
4. 同一 id → 「歌词」→ 打印 `yrc` 或 `lrc` 长度 > 0
5. 「详情」→ 歌名与搜索一致
6. 故意用 `id=0` 或错误 id → 打印明确失败类型（非裸 stack）
7. 断网再跑 → `NetworkFailure` 文案，进程不崩
8. `flutter analyze` 零问题

---

## 六、二期预告（只列标题，本期不排）

| 期 | 主题 | 大致内容 |
| --- | --- | --- |
| 二 | 可听闭环 | `NeteaseSource`、搜索/播放/歌词进产品、音源切换、角标 |
| 三 | 内容页 | 歌单/专辑/歌手/榜单/每日推荐 |
| 四 | 账号 | 扫码/短信/密码、我喜欢、回写同步 |
| 五 | 特色与策略 | 私人 FM、雷达歌单、试听完善、降档策略 |

---

## 七、一期实测记录（2026-09-24）

| ID | 结果 | 备注 |
| --- | --- | --- |
| I0 weapi | ✅ | A4 detail 成功证明加密正确 |
| I0b eapi | ✅ | A3 lyric / A2 play url 成功 |
| S2 csrf | ⚠️ | 首页 GET **无 Set-Cookie**；weapi 在无 `__csrf` 时 detail/search 仍可 |
| A1 搜索 | 🟡 | **`cloudsearch/get/web` → 50000005**；**`/weapi/search/get` → 200**（已作默认） |
| A2 播放 URL | ✅ | eapi v1 返回真 mp3 url；`level=exhigh`。无版权曲 `code=404` + `cannotListenReason=1` |
| A2b WEAPI URL | ✅ | 协议通；同曲目无版权时同样 404 |
| A3 歌词 | ✅ | lrc / tlyric 有内容；YRC 视曲目 |
| A4 详情 | ✅ | `weapi/v3/song/detail` |

**结论**：D1–D8 达成（搜索改用 `search/get`）。`cloudsearch` 的 50000005 记为上游/客户端形态问题，备选口保留。

### 7.1 与 NeriPlayer 对齐核对

| 项 | NeriPlayer | 本仓 | 结论 |
| --- | --- | --- | --- |
| 加密常量 PRESET_KEY/IV/EAPI_KEY/盐/格式/RSA PEM | ✅ | ✅ 逐字一致 | **对齐** |
| weapi 双重 AES-CBC + RSA 无填充 | ✅ | ✅ | **对齐**（A4 详情成功背书） |
| eapi MD5 盐 + AES-ECB 大写 hex | ✅ | ✅ | **对齐**（A2/A3 成功背书） |
| `POST /weapi/cloudsearch/get/web` + `total=true` | ✅ | ❌ 默认不用 | **有意偏离**：实测 50000005；改 `/weapi/search/get` |
| `POST /eapi/song/enhance/player/url/v1` `ids/level/encodeType=flac` | ✅ | ✅ | **对齐** |
| `POST /weapi/song/enhance/player/url` `ids/br` | ✅ | ✅ | **对齐** |
| `POST /eapi/song/lyric/v1` 八参数（id/cp/lv/tv/rv/yv/ytv/yrv） | ✅ | ✅ | **对齐** |
| `GET /api/song/lyric` | ✅（SearchApi） | ✅ A3b | **对齐** |
| `POST /weapi/v3/song/detail` `c/ids` | ✅ | ✅ | **对齐** |
| WEAPI query `csrf_token` | ✅ | ✅ | 对齐（csrf 常空，不影响一期口） |
| Cookie `os/appver` + 会话合并 | ✅ | 🟡 简化内存 Cookie | 一期够用 |
| 播放/歌词 301 → 预热重试 | ✅ | ✅ `_retryOnceOnLoginRequired`（force 预热 + 重试一次） | **已补齐** |
| HTTP/2 stream reset → HTTP/1.1 | ✅ | ❌ | 二期可选 |
| Debug 探针页 T2 | ✅ | ❌ 只有 CLI T1 | 二期补 UI |

**一期「要用的」业务口已全部落地**；与 Neri 唯一实质偏离是 **A1 搜索端点**（为可用性改为 `search/get`，cloudsearch 保留对照）。

### 7.2 登录 / 我喜欢 / 详情探针（2026-09-24 追加）

| ID | 端点 | Neri 对齐 | 实测 |
| --- | --- | --- | --- |
| L1 | `weapi/w/nuser/account/get` | ✅ | ✅ `code=200`，游客无 profile |
| L2 | `weapi/login/qrcode/unikey` | ✅ | ✅ `code=200` 拿到 unikey（**yd_token 实测可省**，见 §7.6） |
| L2b | `weapi/login/qrcode/client/login` | ✅ | ✅ **实机扫到 803**（2026-09-24，见 §7.6） |
| L3 | `eapi/w/login/cellphone`（密码 MD5 / 短信） | ✅ | 方法就绪，**未用真实凭据触发** |
| L3b | `weapi/sms/captcha/sent` + `verify`（interface host） | ✅ | 方法就绪 |
| K1 | `weapi/user/playlist` | ✅ | 代码就绪，**游客无 uid 跳过** |
| K2 | `weapi/song/like/get` | ✅ | 同上 |
| K3 | `weapi/song/like` | ✅ | 写接口未触发 |
| K4 | `weapi/playlist/manipulate/tracks` | ✅ 参数同 Neri | 写接口未触发 |
| K5 | `eapi/mine/rn/resource/list`（interface3） | ✅ | 方法就绪 |
| D1 | `api/v6/playlist/detail`（plain） | ✅ | ✅ 歌单名/trackCount/tracks |
| D2 | `weapi/v1/album/{id}`（interface） | ✅ | ✅ 张悬《神的游戏》9 首 |
| D3 | `api/artist/head/info/get`（plain） | ✅ | ✅ 周杰伦 568 曲 / 44 专 |
| D3b | `api/artist/detail/dynamic` | ✅ | ✅ `code=200`（followed/concert/videoNum） |
| D4 | `api/v1/artist/songs` | ✅ | ✅ 分页出曲 |
| D5 | `api/artist/albums/{id}` | ✅ | ✅ 出专辑列表 |

#### 7.2.1 G 组：推荐 / 发现（2026-09-24 追加）

| ID | 端点 | Neri 对齐 | 实测 |
| --- | --- | --- | --- |
| G1 | `weapi/personalized/playlist` | ✅ | ✅ 游客 5 单 |
| G2 | `weapi/v1/discovery/recommend/resource` | ✅ | ⚠️ 301 需登录（预期） |
| G3 | `weapi/v3/discovery/recommend/songs` | ✅ | ✅ 游客 24 曲 |
| G4 | `weapi/v1/radio/get` | ✅ | ✅ 出 1 首 |
| G5 | `weapi/personalized/newsong` | ✅ | ✅ |
| G6 | `weapi/playlist/list` | ✅ | ✅ |
| G7a | `weapi/playlist/highquality/list` | ✅ | ✅ |
| G7b | `api/playlist/highquality/tags`（weapi 加密 + 非 weapi 路径） | ✅ | ✅ code=200 |
| G9 | `api/playlist/detail`（MGC 雷达元数据） | ✅ | ✅ 时光雷达 35 曲 |
| G8 | 评论 / 热搜 | ❌ Neri 未覆盖 | ❌ 待自研 |
| G10 | 官方榜单（复用歌单详情） | ✅ | ✅ 飙升 100 / 新歌 100 / 热歌 200 |
| G11 | 榜单列表 `api/toplist/detail` | ✅ | ✅ 免登录 `code=200`，**63 张**榜（2026-09-26） |
| G12 | 新碟上架 `api/album/new` | ✅ | ✅ 5 区 `code=200`、`count=10`、`total=500`，**`area` 生效**（2026-09-26，探针见 §7.22，**已接「新碟上架」Tab** 见 §7.23） |
| G13 | 歌手列表 `api/v1/artist/list` | ✅ | ✅ 修正为带 `v1` 的路径 + `initial` 转 ASCII 码后，`type`/`area`/`initial` **全部生效**（2026-09-26，探针见 §7.22，**已接「歌手」Tab** 见 §7.23） |

### 7.2.2 第二轮实测（2026-09-24，游客态打满）

| 项 | 结果 |
| --- | --- |
| A1c 分类搜索 | ✅ `type=1/10/100/1000` 四类各 10 条 |
| B4 音质档位 | ✅ 跑通；**游客态三档全降到 `standard`**，高音质需 VIP |
| B5 试听识别 | ✅ 付费曲 `fee=1` + `preview=true`（720KB≈45s） |
| C2 明文歌词 | ✅ lrc=704（C1 为 1014，`lv` 档差异） |
| D5 / D7 / G10 | ✅ 全部通过 |
| E3 扫码轮询 | ✅ 未扫码回 `801 等待扫码` |
| 歌单上限 | ⚠️ `trackCount=1268` 但 `tracks` 只回 **1000**，>1000 需分页/换口 |
| 预热重复请求 | 🔧 首页 GET 无 Set-Cookie → `__csrf` 恒空，原判断 `_preheated && csrf.isNotEmpty` 导致**每次 weapi 白跑一次首页**（一轮多 14 次）。已改为只判 `_preheated` |

**结论：游客态接口全部实测通过**；一期「协议探针」可以正式收口，余下只有登录态（需真实账号）与未实现项。

**登录态写操作 / 我喜欢列表**：协议形态已对齐，**需扫码或账密登录后再跑一轮**（探针 `--suite like --uid <id>`；写操作要额外加 `--write`）。

### 7.3 探针覆盖面（2026-09-24 收口）

CLI 探针已覆盖**全部已实现方法**；`--suite` 语义改为「跑什么写什么」（不再隐含 search，减少请求数）：

| suite | 覆盖 |
| --- | --- |
| `search`（默认） | A1 搜索、A1c 分类（`--types`）、A4 详情、A2/A2b 播放 URL（`--level`）、B4 音质扫描（`--levels`）、A3 + C2 歌词 |
| `cloudsearch` | A1b 对照口（实测 50000005） |
| `login` | E1/E2/E3（E2/E3 已实测扫到 803）；E4–E7 需 `--phone`/`--password`/`--captcha`/`--send-sms`（默认不发） |
| `like` | F0–F3、F6（需登录或 `--uid`）；F4/F5/F5b 需 `--write`（F5b 另加 `--del`）。**登录态推荐 `--suite login,like --qr`**：同进程共用 client，扫码后 uid 自动取；F3 附 `songDetailRaw` 前 5 首验证「id→详情」，F6 打印 `count` + 首条记录键名两行 |
| `detail` | D1–D7 |
| `discover` | G1–G7b、G9、G10 |

**B3 已补齐**：播放 URL / 歌词遇 body `code==301` 自动 `ensureWeapiSession(force: true)` 后重试一次。

**已跑通（2026-09-24 第二轮）**：游客态全部 ✅，见 §7.2.2；剩余 🟡 只有「需真实凭据」的 E4–E7 与 F 组。状态口径见 [网易云接口文档.md](网易云接口文档.md) §五。

### 7.4 阶段 B 适配层（2026-09-24 落地）

协议收口后网易云已接进「多音源」抽象：

| 文件 | 内容 |
| --- | --- |
| `core/api/netease/netease_mappers.dart` | 响应 → 统一模型（`Track` / `PlayUrlResult` / `LyricPayload`），含 YRC 逐字解析 |
| `data/sources/netease/netease_source.dart` | `NeteaseSource`：`MusicSource` + `DailyRecommendSource`(G3) / `RankSource`(G10) / `PersonalFmSource`(G4) / `UserPlaylistWriteSource`(F5) |
| `data/sources/sources.dart` | `registerDefaultMusicSources()` 装配酷狗 + 网易云（原先挂在 `kugou_source.dart` 尾部） |

新增客户端方法 `removeSongsFromPlaylistRaw`（F5b，`op=del`，**探针已覆盖但未真机实测**，见下 §7.18）。
`dart analyze lib` 0 issue；口径与未实测项见 [网易云接口文档.md](网易云接口文档.md) §九。

### 7.5 阶段 C 搜索多源混排（2026-09-24 落地）

| 文件 | 内容 |
| --- | --- |
| `core/source/music_source.dart` | 基类 4 个搜索改返回 `SearchPageResult<T>`（items + total 一次解码） |
| `core/models/search_result.dart` | `AlbumBrief` / `ArtistBrief` 补 `platform`（与 `PlaylistBrief` / `Track` 对齐） |
| `core/source/music_platform.dart` | 加 `label` 展示名，UI 不再自己 `switch (platform)` |
| `core/api/netease/netease_client.dart` | `searchSongsRaw` → `searchRaw`（`type` 参数已存在，四类复用） |
| `core/api/netease/netease_mappers.dart` | 新增 `mapNeteaseSearchPlaylists/Albums/Artists` + 3 个 Brief mapper（多键回退） |
| `data/sources/netease/netease_source.dart` | 实现 4 个搜索，共用私有 `_search(..., kind)`；`_NeteaseSearchKind` 枚举 wire 值 |
| `data/sources/kugou/kugou_source.dart` | 4 个搜索委托 `SearchRepository` 的分页方法 |
| `features/search/search_controller.dart` | 依赖 `MusicSourceRegistry`；多源并行取页 + 轮询交错 + `sourceFilter` + `hasMoreFlag` |
| `features/search/search_page.dart` | 顶部 `_SourceFilterBar`（全部/各平台 chips）+ 结果行来源角标 |
| `shared/widgets/common.dart` | `SourceBadge`；`TrackTile.showSource`、`SearchResultRow.platform` |

语义：**不做**跨源同曲去重；单源失败静默忽略、全失败才抛；混排无单一 `total` 时由控制器算 `hasMoreFlag`。
`flutter analyze lib test tool` 0 issue；`flutter test` 282 项全绿（含新增 9 项多源混排/筛选用例）。
口径详见 [多音源接入方案.md](多音源接入方案.md) §11.1。

**待用户手动核对**：`dart run tool/probe_netease_api.dart --types 1,10,100,1000` 打印歌单/专辑/歌手
首条记录字段名（这三类 mapper 目前走防御式多键回退，未实测）。

**下一步（UI 层）**：我的/我喜欢按源分 Tab、设置页默认源与分源账号。进度见 [多音源接入方案.md](多音源接入方案.md) §11.1。
（**2026-09-24 已完成**：分源账号见 §7.6，按源分 Tab + 默认源见 §7.8。）

### 7.6 阶段 D 网易扫码登录（2026-09-24 落地）

对齐 `D:\work\NeriPlayer` `core/api/netease/NeteaseQrLoginClient.kt`，把扫码登录从「协议探针」提升到产品可用：

| 层 | 文件 | 内容 |
| --- | --- | --- |
| 契约 | `core/source/capabilities.dart` | 新增 `DeviceLoginSource`（`createLoginQr` / `pollLoginQr` / `currentAccount` / `logout`）+ `LoginQrSession` / `LoginQrStatus` / `LoginQrPoll` / `LoginAccount` |
| 协议 | `core/api/netease/netease_client.dart` | `_request` 拆出 `_requestDetailed`（回传 `x-refresh-token`）；`callWeApi` 支持 `extraHeaders`；新增 `createQrSession` / `pollQrLogin` / `confirmQrLogin` / `seedCookies` / `clearLogin`；`buildLoginChainId` / `buildScanLoginUrl` / `mergeQrCredentialCookies` |
| 映射 | `core/api/netease/netease_mappers.dart` | `mapNeteaseAccount`（游客态 / 坏 JSON → null） |
| 适配 | `data/sources/netease/netease_source.dart` | `implements DeviceLoginSource`；`mapNeteaseQrStatus`（800/801/802/803） |
| 存储 | `data/storage/netease_auth_store.dart` | key `netease.auth.v1`；`encode` / `decode` 纯函数 + `restoreInto` / `save` / `clear` |
| 状态机 | `features/auth/netease_login_controller.dart` | `NeteaseLoginPhase` + 2s 轮询 + 世代号防串号；`bootstrap` 已登录则不重复扫码 |
| 页面 | `features/auth/netease_login_page.dart` | 二维码（`QrImageView`）+ 四态（loading/expired/error/正常）+ 已登录账号卡 + 退出 |
| 装配 | `main.dart` · `app.dart` · `features/settings/settings_page.dart` | 启动 `restoreInto`；路由 `/netease-login`；设置页「音源账号」入口 |

**扫码链路三个关键点**（对齐 Neri）：① 二维码内容是 `scanlogin` URL，不是裸 unikey；② chainId 格式
`v1_{sDeviceId}_web_login_{ts}`，轮询须带 `x-loginmethod: QrCode` + `x-login-chain-id`；③ 扫码链路用桌面 UA +
`x-os: web` / `x-channelsource: undefined` / `nm-gcore-status: 1`，**不做 weapi 预热、不带 csrf**。
803 后 `confirmQrLogin` 先 `account/get` 验账号，失败则用 `x-refresh-token` 当 `MUSIC_U` 合并再验。

探针新增 `--qr`（终端渲染二维码 + 每 2s 轮询）/ `--qr-timeout` / `--yd-token`，用法见
[网易云接口文档.md](网易云接口文档.md) 探针章节。

`flutter analyze lib test tool` 0 issue；`flutter test` **300 项全绿**（含新增 `netease_login_test.dart`
18 项：状态码映射 / chainId 与 scanlogin URL 构造 / cookie 合并 / 登录态落盘编解码 / 控制器状态机）。

**实机扫码实测（2026-09-24，通过）**：`dart run tool/probe_netease_api.dart --suite login --qr` 终端渲染二维码后，
801 `等待扫码` → 802 `授权中` → 803 `授权登陆成功`；`x-refresh-token` 长度 162 → 合并出 `MUSIC_U`；
`confirmQrLogin` 返回 `hasLogin=true`，`account/get` 出 `userId=1593114455 / nickname=MZHENGHF`。

**关键结论：`yd_token` 可省略** —— 全程 `ydDeviceToken` 发空串即走到 803，故 **E9（WebView 设备指纹）不实现**，
原「E9 结论待定」风险项关闭。扫码登录从「协议探针」升级为**产品可用**（UI 已接，见上表）。

**下一步可跑**：已有 `MUSIC_U`，可开跑登录态接口（见 §7.7）。

### 7.7 阶段 E 登录态 F 组实测（2026-09-24）

命令：`dart run tool/probe_netease_api.dart --suite login,like --qr`（**同进程**：扫码登录后 uid 自动取，免传 cookie）。

| 项 | 结果 |
| --- | --- |
| F1 用户歌单 | ✅ `code=200, count=21`，首单 `MZHENGHF喜欢的音乐` |
| F2 我喜欢 id | ✅ **`likedPlaylistId=2412580948`**（`specialType==5` 命中） |
| F3 喜欢歌曲 id | ✅ `count=1009`，顶层字段名 `ids`（纯数字数组） |
| F3b id→详情链路 | ✅ 前 5 首 `songDetailRaw` 取到 `id=65536 / 爱情转移 / 陈奕迅` |
| F6 收藏专辑 | ⚠️ `count=0`（该账号无收藏专辑，**非空形态未验证**，待有收藏的账号复核） |
| F4/F5 写操作 | 🔒 未触发（探针默认不发） |

**`song/detail` 字段名已实测确认**（此前靠多键回退猜）：详情走**新字段结构**——
歌手 **`ar`**（数组，含 `id`/`name`/`tns`/`alias`）、专辑 **`al`**（封面在 **`al.picUrl`**，不是顶层 `picUrl`）、
时长 **`dt`**（毫秒）。写「我喜欢」映射时**必须读 `ar`/`al`**。

探针侧同步增强：F3 增加 `[F3b]` 详情验证；F6 改用 `_firstItemKeys` 打印首条记录键名。

### 7.8 阶段 F UI 层：我喜欢分源 + 默认源（2026-09-24 落地）

| 项 | 落地 |
| --- | --- |
| 能力契约 | `core/source/capabilities.dart` 新增 `UserLibrarySource.likedTracks()`（网易「我喜欢」入口，UI 经 registry 取） |
| 映射 | `mapNeteaseLikedSongIds`（F3 顶层 `ids` → `List<int>`）/ `mapNeteaseSongDetails`（A4 `songs[]` → `[Track]`，读 `ar`/`al.picUrl`/`dt`） |
| 适配器 | `NeteaseSource.likedTracks()` = F3 → A4 分批详情，**100 id/批**；未登录抛 `LoginRequired` |
| 页面态 | `features/likes/netease_likes_controller.dart`（`neteaseLikesProvider`）：登录态异步回填后自动拉取、登出自动清空 |
| 我喜欢页 | 歌曲 Tab 内加 `SourceFilterBar`（全部/酷狗/网易云）；`全部` 时酷狗在前、网易在后顺序拼接，**不做**跨源同曲去重；歌手/专辑 Tab 仍只走酷狗；**红心按钮仅酷狗曲目显示**（网易写口未实测） |
| 默认源 | 设置项 `settings.defaultSource`，只作搜索页 +「我喜欢」页的**初始筛选值**（非全局切换）；`SearchController.applyDefaultSourceFilter` + `_sourceFilterTouched` 保证不覆盖用户手动选择 |
| 公共组件 | `_SourceFilterBar`/`_SourceChip` 从 `search_page.dart` 提取为公共 `SourceFilterBar`（`shared/widgets/common.dart`），搜索页与我喜欢页共用 |
| 测试 | 新增 `test/netease_likes_test.dart`（F 组映射 + Notifier 5 用例）、`test/settings_default_source_test.dart`（4 用例）、`search_controller_test.dart` 补 2 用例；全量 **318 项通过**，`flutter analyze lib test tool` 无问题 |

**口径**：本条清掉了 [多音源接入方案.md](多音源接入方案.md) §11.1 的最后两条 ⏳，一期 UI 层改造收口。
**遗留**：「取消喜欢」写口（F4）与歌单删曲（F5b）仍未实测；F6 收藏专辑非空形态待复核。
→ 2026-09-26 已把这三项的探针跑法补齐，见下 §7.18。

### 7.9 搜索单曲封面修复（2026-09-25）

**现象**：搜索页网易云来源的歌曲列表封面全是灰块（酷狗来源正常）。

**根因**：产品搜索走旧口 `/weapi/search/get`，其曲目是**旧结构**——`album` 键为
`publishTime,size,artist,copyrightId,name,id,picId,mark,status`，**只有数字 `picId`，没有 `picUrl`**；
而 mapper 只读 `album.picUrl` → `coverUrl` 为空串 → `CoverBox` 退化成灰色渐变块。
（专辑/歌单/歌手搜索有 `picUrl`/`coverImgUrl`，不受影响；这也解释了为何只有歌曲 Tab 出问题。）

**修复**：

| 项 | 内容 |
| --- | --- |
| 算法 | `NeteaseCrypto.picUrl(picId)`：`picId` 与盐 `3go8&$8*3*3h0k(2)2` 逐字节异或 → MD5 → base64Url，拼 `https://p3.music.126.net/{hash}/{picId}.jpg?param=300y300`（与 `anonymous` 同族算法） |
| 接线 | `mapNeteaseSong` 封面改为 `album.picUrl` → 空则走 `album.picId` 回退（`_albumCover`） |
| 探针 | `_fieldSample` 对单曲额外打印 `albumKeys` / `albumCover`，便于核对 `album` 内层字段 |
| 验证 | **实测 `picId=109951163038292176` → `200 image/jpg`，内容为对应专辑封面**；单测锁已知向量（`netease_crypto_test.dart`）+ 旧/新两种形态的 mapper 用例（`netease_client_parse_test.dart`） |
| 全量 | `flutter analyze` 无问题；`flutter test` **322 项通过** |

**口径**：这是**协议形态差异**而非接口不可用——旧口能搜到歌、拿到时长/歌手，只是封面要自己拼 URL。
换新口 `cloudsearch` 可省掉这段（但本环境返回 `50000005`，不可用）。细节见
[网易云接口文档.md](网易云接口文档.md) §三 A1c 与 §七 已知坑。

### 7.10 阶段 H 详情页深链分发（2026-09-25 落地）

**现象**：搜索页切网易云后点歌单/专辑/歌手，进的是酷狗详情页——网易 id 被打到酷狗接口，必出
「加载失败」级 bug（歌单页可能因用户歌单分支误命中而表现诡异）。

**修复**：

| 项 | 内容 |
| --- | --- |
| 契约 | `capabilities.dart` 新增 `PlaylistDetailSource`(D2，歌单/榜单共用) / `AlbumDetailSource`(D3) / `ArtistDetailSource`(D4+D6)；统一模型 `AlbumDetail`/`ArtistDetail`/`ArtistSongsPage` 上移 `core/models/catalog_models.dart`（catalog_repository `export` 兼容旧引用） |
| 映射 | `mapNeteasePlaylistDetail` / `mapNeteaseAlbumDetail` / `mapNeteaseArtistDetail` / `mapNeteaseArtistSongs`（字段依据 NeriPlayer 解析实现：D3 的 `songs[]` 在顶层、D4 是 `data.artist` 嵌套、D6 分页标志是 `more`） |
| 双源实现 | `NeteaseSource` implements 三契约（复用已实测协议层 `playlistDetailRaw`/`albumDetailRaw`/`artistDetailRaw`/`artistSongsRaw`）；`KugouSource` 同样 implements（包装既有 repository，构造注入） |
| 路由 | `/rank/:id`、`/playlist/:id`、`/album/:id`、`/artist/:id` 读 `?src=<wireName>` 按源分发；**不带 src 默认酷狗**（深链兼容）。搜索页三类结果点击 + `artistTapFor` 曲目行点歌手带 src |
| 页面 | 三个详情页只认契约取数；歌单页网易走 D2 单次加载 + 换榜 picker 走 `rankBoards()`；歌手页 `_fetchSongsPage` 收口网易 offset 分页 / 酷狗 page 分页差异 |
| 验证 | `netease_client_parse_test.dart` 新增 D 组映射 6 用例；`flutter analyze` 无问题；`flutter test` **338 项全过**（基线 332） |

**待实测**：真实网易歌单/专辑/歌手 id 的端到端 UI 验收（App 手动跑）；日推/FM 的分发属后续阶段
（已分别见 §7.12 / §7.13，同见 [多音源接入方案.md](多音源接入方案.md) §11.1 边界）。

### 7.11 阶段 I 榜单列表页分发（2026-09-25 落地）

**现象**：`/ranks`（发现页入口）画面的榜单列表页直连 `playlistRepository.fetchRankList()`（酷狗
`rank/list`），停用酷狗后整页废——网易有 `RankSource` 却顶不上。

**修复**：

| 项 | 内容 |
| --- | --- |
| 契约收敛 | `RankSource.rankBoards()` 返回类型由 `List<({id,name,coverUrl})>` 改为 `Future<List<PlaylistBrief>>`；酷狗直接透传 `fetchRankList()`，网易由固定三榜 + `_boardMeta` 补名/封面构造（`isRank`/`platform: netease`/`rankTypeName: '网易官方榜'`） |
| 去重复 | `playlist_detail_page._openRankPicker` 删除「网易侧手动构造 PlaylistBrief」转换块，两源共用 `rankBoards()` 一份实现 |
| 分发 | 可用源 = `settings.enabledSources` ∩ registry 中具备 `RankSource` 的平台；首选 `effectiveDefaultSource`，不可用退首个可用源；**榜单两源同名榜不可混排**，故单源取数 |
| 切源栏 | `SourceFilterBar` 新增 `showAll`（默认 true，榜单传 false 不出「全部」chip）；可用源 >1 时显示，=1 隐藏，=0 出 `SourceDisabledView` 空态 |
| 深链 | 卡片点击 `/rank/${id}`，非酷狗源带 `?src=<wireName>`（酷狗不带，与搜索页/`artistTapFor` 同口径） |
| 开关联动 | `ref.listen(settingsControllerProvider)` 监听 `enabledSources`：当前源被停用时自动切剩余可用源并重取；切源响应带「源已变则丢弃」防串台 |
| 验证 | 新增 `test/rank_list_dispatch_test.dart` 6 用例（两源切源栏/切源重取/单源隐藏/点击带 src/酷狗不带 src/无可用源空态）；`flutter analyze` 无问题；`flutter test` **352 项全过**（基线 346） |

**待实测**：关酷狗后榜单列表应显示网易三榜（飙升/新歌/热歌）且可点进详情、曲目与换榜正常；
两源开启时应出现切源栏。**FM 的分发见 §7.13**（同见 [多音源接入方案.md](多音源接入方案.md) §11.1 边界）。

### 7.12 阶段 J 日推页分发（2026-09-25 落地）

**现象**：`/daily-recommend` 直连 `recommendRepository.fetchDaily()`（酷狗 `/top/ip` + 个性化接口），
停用酷狗后整页废——网易 G3（游客 24 曲）已实测可用却顶不上；且网易侧契约只回 `List<Track>`，
**丢了 `personalized`/`needLogin` 语义**。

**修复**：

| 项 | 内容 |
| --- | --- |
| 契约升级 | `DailyRecommendSource` 的 `dailyRecommendedSongs(): List<Track>` → `dailyRecommend(): Future<DailyRecommendResult>`；模型 `DailyRecommendResult`（tracks/personalized/needLogin/error）上移 `core/models/daily_recommend.dart`（`recommend_repository` 用 `export` 兼容旧引用，同 D 组先例）；`dateLabel` 一并上移为 `dailyRecommendDateLabel()`，页面不再直连 repository |
| 两源语义 | 酷狗透传 `fetchDaily()`（保留公开兜底 / 个性化 / `needLogin` / 网关拦截文案）；网易 G3 映射 `personalized: true`、`needLogin`/`error` 恒空。UI 不按源分支，副标题与「N 首 · 个性化推荐」两源共用 |
| 分发 | 可用源 = `settings.enabledSources` ∩ registry 中具备 `DailyRecommendSource` 的平台；首选 `effectiveDefaultSource`，不可用退首个；**日推是一整个今日歌单，两源混排无意义**，故单源取数 |
| 切源栏/空态 | 与 §7.11 同一套（`SourceFilterBar(showAll: false)` / `SourceDisabledView`），切源重取 + 旧响应丢弃 + 整源开关联动 |
| 不在本期 | 「为你推荐」推荐中心（`/recommend`，风格/歌单/编辑精选）网易无对应接口，维持「停用酷狗 → 空态」 |
| 验证 | 新增 `test/daily_recommend_dispatch_test.dart` 4 用例（默认源取数+个性化文案/切源重取/单源隐藏/无可用源空态）；`hero_transitions_test.dart` 补装离线源；`flutter analyze` 无问题；`flutter test` **356 项全过** |

**待实测**：关酷狗后日推页应显示网易日推 24 曲（副标题「为你量身定制的每日歌单」）且可播放；
两源开启时应出现切源栏。**FM 分发见 §7.13**（同见 [多音源接入方案.md](多音源接入方案.md) §11.1 边界）。

### 7.13 阶段 K 私人 FM 分发（2026-09-25 落地）

**现象**：`FmController`（`features/fm/fm_controller.dart`）原先直连 `FmRepository`（酷狗
`/v2/personal_recommend`）+ `SearchRepository`（关键词兜底），停用酷狗后整页废——网易 G4
（`radio/get`，实测出 1 首）已可用却顶不上；且酷狗独有「档位 / 曲库」轴对网易无意义。

**修复**：

| 项 | 内容 |
| --- | --- |
| 取数走契约 | `registry.capability<PersonalFmSource>(会话源).nextFmTracks(remain:)`；关键词兜底也走**同一源**的 `searchSongs`（`registry.of(platform)`），不跨源。`FmController` 改为无参构造（不再注入 `search`/`fmRepo`） |
| 会话源落盘 | `FmSession` 新增 `source`（`fm.session.source` 存 wire 值）；旧数据缺该字段 → `null`，`restore()` 按设置默认源解析（`FmSessionStore._readSource`），不写死酷狗 |
| 一次会话不混源 | 切源 = `switchSource()` **结束当前会话并以新源重开**；未起播时只改「下次开台」的源（不白跑一次取数）。`ref.listen(settingsControllerProvider)` 监听 `enabledSources`：当前源被停用 → 用剩余源重开，全部停用 → 收掉会话（已入队曲目仍可播完） |
| 批量差异收口 | 网易**一次只回 1 首**：`_collect` 循环拼装到 `kFmSeedBatch = 10`（`kFmSeedBatchMaxCalls = 10` 限流，`tracks.isNotEmpty` 保证首次无歌不空转）；酷狗一次一整批不进循环。首轮仍必须 `remain=0`（`clampRemainSongcnt`） |
| 兜底按能力判定 | `HeartRadioSource`（酷狗独有）：带此能力的源未登录时**直接走关键词池不请求 FM 接口**（保住既有行为）；网易无此能力 → **必须请求**才知道「需要登录」，失败**不回落关键词**，`SourceFailure.message` 直接进 `gatewayError`（不用公开搜索冒充个性化流） |
| 档位轴显隐 | `FmController.hasModeAxis` = 会话源是否 `implements HeartRadioSource`。为 false 时 FM 页 / 播放页 FM 面板 / 发现页快捷入口：模式胶囊与歌池轴整块不挂，卡面改挂电台名（`FmRadioCard.showModeAxis` / `_FmStationTag`），角标不再写「· Alpha」（`FmSourceBadge.showPool`） |
| 切源栏/空态 | 可用源 = `enabledSources` ∩ 具备 `PersonalFmSource` 的平台；首选 `effectiveDefaultSource`。>1 出 `SourceFilterBar(showAll: false)`，=1 隐藏，=0 出 `SourceDisabledView` |
| 喜欢写口分级 | 酷狗走 `likesProvider.like()`（本地 + 云端曲库）；**非酷狗源只写本地**（`likesProvider.likeLocal()`）——F4/F5 写口未实测，不把网易曲目写进酷狗歌单污染曲库。不喜欢上报走 `reportFmFeedback(trash)`（网易空实现） |
| 验证 | 新增 `test/fm_source_dispatch_test.dart` 7 用例（两源切源栏/点 chip 切源重开/单源隐藏/无可用源空态/网易隐藏档位轴+角标/网易一次 1 首拼装到批量/会话源落盘与旧数据兼容）；`test/fakes/fake_music_source.dart` 新增 `ScriptedFmSource`（implements `HeartRadioSource` 模拟酷狗语义）；`fm_controller_test` / `fm_controls_test` / `fm_page_test` / `quick_entries_test` 的取数改为经全局 registry；`flutter analyze` 无问题；`flutter test` **363 项全过**（基线 356） |

**待实测**（App 手动跑）：① 关掉酷狗进 `/fm` 走网易 G4（一次 1 首会被拼到 ~10 首，档位/曲库轴应消失、
角标写「来源：网易云私人 FM」）；② 两源开启时 FM 页出切源栏，点另一个源应结束当前会话并以新源重开
（不混源）；③ 网易源下点「喜欢」只进本地「我喜欢」，酷狗曲库无新增；④ 未登录网易时 FM 应报
「需要登录」而不是回落成关键词检索。

---

### 7.14 阶段 L 两级功能开关（2026-09-25 落地）

**诉求**：在整源开关之下再挂一层**按功能**的开关，例如「酷狗只留搜索、关掉私人 FM」，
不必为了关一个功能而把整个源停用。

**做法**：

| 项 | 内容 |
| --- | --- |
| 功能枚举 | 新增 `core/source/features.dart`：`SourceFeature{search, personalFm, dailyRecommend, rank}`（label 搜索/私人 FM/每日推荐/排行榜，`id` 为 `search`/`fm`/`daily`/`rank`）。**只覆盖入口类功能**；详情页 / 播放 / 歌词 /「我喜欢」这些被动能力不做开关（关掉只会把到手的内容变残）。探索发现 / 推荐中心不做（网易无分发，做了只能切出空态） |
| 能力过滤 | `SourceFeature.supportedBy(registry, platform)`：`search` 恒真（基类能力），其余探 `PersonalFmSource` / `DailyRecommendSource` / `RankSource`。**不具备能力的功能不列**（否则是只能点出空态的假入口） |
| 落盘 | `settings.disabledFeatures`（`Set<String>`，token `<wireName>:<id>`，如 `kugou:fm`）。**存「关闭项」而非「启用项」**：缺省 = 全开，老数据无需迁移，以后新增功能也不会一上线就是关的。允许全关（与 `enabledSources` 拒绝空集不同）；读取时丢弃已不存在的 token |
| 判定 | `AppSettings.isFeatureOn(p,f)`（子项自身取值，供设置页显示）/ `isFeatureEnabled(p,f)`（父 AND 子，消费点用）/ `featureSwitchCause(f)`（空态归因）。父开关关闭**不重置**子项取值 |
| 设置页 | `settings_page.dart` 的 `_sourceSwitchTiles`：父 `SwitchListTile`（源开关，沿用「最后一个源不可关」）下挂 `for f in SourceFeature.values if supportedBy(...)` 的 dense 子项，key = `feature_switch_<wireName>_<id>`；父关闭时 `onChanged: null` 置灰但 `value` 保留 |
| 消费点 | FM `availableSources()` / 搜索 `availablePlatforms` 与混排 `_sources()` / 榜单 `_availableSources()` / 日推 `_availableSources()` 各乘 `isFeatureEnabled`；三处 `ref.listen(settingsControllerProvider)` 早退条件补 `disabledFeatures` 比较，使改子开关能现场触发切源 / 收会话 |
| 空态文案 | `SourceDisabledView` 增可选 `feature`：非空 → 「X的「Y」已关闭」+「X 音源仍启用，只是这一项功能被单独关掉了」；空 → 原「X音源已停用」。归因判据用 `featureSwitchCause`（**不能用 `effectiveDefaultSource`**，它按定义必然在启用集里、永远为真） |
| 验证 | 新增 `test/feature_switches_test.dart` 12 用例：默认值与落盘 5（全开 / 单个 token 还原 / 空列表合法 / 脏 token 丢弃 / 关→开往返）、两级 AND 语义 3（父关子取值保留 / 父关时子开也不生效 / 空态归因）、消费点过滤与设置页 4（FM 可用源 / 搜索筛选条 / 榜单空态文案归因到功能 / 设置页 8 个子开关存在且父关后置灰取值保留）。`flutter analyze` 无问题；`flutter test` **375 项全过**（基线 363） |

**待实测**（App 手动跑）：① 设置页每个源下应看到 4 个子开关，关掉父开关后子项变灰但开关位置不变，
重新打开父开关应恢复原配置；② 关掉「酷狗 × 私人 FM」后进 `/fm` 应直接用网易；再把「网易云 × 私人 FM」
也关掉 → 整页提示「这四项都关了 / 音源已停用」；③ 关掉「酷狗 × 排行榜」且只剩酷狗时，`/ranks`
应提示「酷狗的「排行榜」已关闭」而不是「音源已停用」；④ 关掉「网易云 × 搜索」后搜索页筛选条只剩酷狗。

---

### 7.15 阶段 M 账号体系按源（2026-09-25 落地）

**诉求**：网易云登录成功后，「我的」页与「个人中心」页仍显示酷狗未登录（游客）——UI 层没有
「按源看账号」的概念。

**根因**：登录态是两套旁路（酷狗 `AuthController` key `auth.user.v1`、网易 `NeteaseLoginController`
key `netease.auth.v1`），两页只 `watch(authControllerProvider)`，故永远只反映酷狗。

**做法**：

| 项 | 内容 |
| --- | --- |
| 账号中枢 | 新增 `features/profile/source_account.dart`：`SourceAccount` 统一摘要 + `sourceAccountProvider(p)` 按平台 switch（酷狗读 `AuthController`、网易读 `NeteaseLoginController`）。**不加 `UserSessionSource` 能力接口**（UI 只是平台 switch，加了是空抽象）；`loginRouteFor(p)` / `logoutSource(ref,p)` 负责登录路由与退出 |
| 页面级账号源 | `accountSourceProvider`（`StateProvider<MusicPlatform?>`，null = 跟随默认源）+ `effectiveAccountSourceProvider`（选中源被停用时回落 `effectiveDefaultSource`）+ `accountSourcePlatformsProvider`（启用集 ∩ 已注册）。两页共用；切源条复用 `SourceFilterBar(showAll: false)`，`length > 1` 才挂 |
| 组件补丁 | `SourceFilterBar` 新增 `horizontalPadding`（默认 `KugoSpacing.lg`）：放进已带左右留白的页面（「我的」）传 0，避免双份留白 |
| 网易歌单读取 | 能力契约新增 `UserPlaylistReadSource.userPlaylists()` → `UserPlaylistsPage{created, collected, more}`；`NeteaseSource` 走 `userPlaylistsRaw(uid)`（`/weapi/user/playlist`，limit 给足一次取全量）。分流：`subscribed==true` = 收藏，其余自建；`specialType==5`（「我喜欢的音乐」）归自建并打 `isDefault` |
| 歌单数据流 | 网易新增 `features/profile/netease_collections_controller.dart`（与 `netease_likes_controller` 同构：登录回填后自动拉、登出清空、未登录只给「需登录后查看」且**不发请求**）；酷狗仍走 `userCollectionsProvider`。「我的」页按账号源取一份，**「我喜欢」计数直接取默认单 `trackCount`，不额外发曲目请求** |
| 个人中心差异 | 等级 / 歌龄 / 关注粉丝 / 签名 / IP / 账号档案 / 会员状态都是酷狗口径 → 网易侧整块不挂（`_IdentityCard.grade` 改可空 + `if (g != null)`），改挂 `_SourceAccountNotice` 说明 |
| 验证 | 新增 `test/account_source_dispatch_test.dart` 19 用例（mapper 分流 4 / notifier 4 / 账号源 provider 5 / 两页按源渲染 6）；`flutter analyze` 无问题；`flutter test` **394 项全过**（基线 375） |

**待实测**（App 手动跑）：① 只登网易云（酷狗未登）→「我的」页头部应显示网易昵称/头像，
歌单统计应是网易云端歌单数，「我喜欢」是网易红心数；② 两源都登 → 切源条来回切，「我的」页
头部/歌单/计数整块跟着换，且「个人中心」页记得的是同一份选择；③ 网易源下个人中心不应出现
等级/歌龄/账号档案/会员状态，应显示「暂不提供等级 / 歌龄 / 关注粉丝等档案信息」；④ 网易源
未登录时点「立即登录」应进 `/netease-login`（不是酷狗登录页）。

**待实测项（本期不建入口）**：网易「最近播放」与「云盘」当前 `NeteaseClient` 里**没有对应对口**
（已核对，现只有 `userPlaylistsRaw`/`likedSongIdsRaw`/`likeSongRaw`/`addSongsToPlaylistRaw`/
`removeSongsFromPlaylistRaw`/`userAlbumsRaw`）。**未实测就不建假入口**；将来要补需先补协议探针。
网易「取消喜欢」（`likeSongRaw(like:false)`）客户端已有口，但未暴露到能力/UI，属后续项。

---

### 7.16 阶段 N 探索发现页分发（2026-09-25 落地）

**诉求**：`/discovery`（探索发现）五个 Tab（歌单 / 排行榜 / 新碟上架 / 新歌速递 / 歌手）原先全部直连
酷狗 repository，停用酷狗后整页废；网易云虽已实现 G5/G6/G7b 协议，UI 层一个都没用上。

**做法**：

| 项 | 内容 |
| --- | --- |
| 能力契约 | `capabilities.dart` 新增 `PlaylistCatalogSource`（`playlistTagGroups()` + `categoryPlaylists(cat:)`）与 `NewSongFeedSource`（`newSongs()`）；**榜单 Tab 复用既有的 `RankSource`**（本页是它的第一个 UI 调用方，无需新契约） |
| 网易侧 | `NeteaseSource implements PlaylistCatalogSource, NewSongFeedSource`：标签 G7b、分类歌单 G6（`cat` 传中文标签名）、新歌 G5；mapper 新增 `mapNeteaseTopPlaylists` / `mapNeteasePlaylistTags`（**一级扁平拍成单组 + 前置「全部」**）/ `mapNeteasePersonalizedNewSongs`（`result[].song` 内层解包） |
| 酷狗侧 | `KugouSource implements` 同两项：标签包 `DiscoveryRepository.fetchPlaylistTags()`（失败退回推荐分类兜底）、分类歌单包 `RecommendRepository.fetchRecommendPlaylists(categoryid:)`、新歌包 `DiscoveryRepository.fetchNewSongs()`；repository 的 `error` 字符串转抛 `NetworkFailure`，UI 统一读 `SourceFailure.message` |
| 模型上移 | `PlaylistTagGroup` / `PlaylistTag` 由 data 层 `discovery_repository.dart` 上移 `core/models/catalog_models.dart`（原 `DiscoveryTagGroup`/`DiscoveryTag` 改名）——core 契约不能反向依赖 data |
| 页面 | 可用源 = `enabledSources` ∩ registry 中**具备三项任一能力**的平台（各 Tab 能力不同，用「任一」判整页可用）；首选 `effectiveDefaultSource`；可用源 >1 出 `SourceFilterBar(showAll: false)`，=0 才出整页 `SourceDisabledView`；切源 `_resetData()` 清五个 Tab 缓存 + 旧响应丢弃防串源；非酷狗源点歌单深链带 `?src=netease` |
| 酷狗限定 Tab（**2026-09-26 已解除**，见 §7.23） | 当时新碟上架 / 歌手暂为酷狗限定 → 非酷狗源出「XX暂不支持「新碟上架」，可切换其他音源查看」空态，**不建假入口、不搬 `DiscoveryArtist` 到 core**（依据见 §7.22 更正：原写「网易无实测口」实为**未验证假设**） |
| 榜单说明 | 同源 `rankTypeName` 唯一时补「网易官方榜 · 共 N 个榜单」文案（当时 N=3；**2026-09-26 起 N=20**，见 §7.21） |
| 验证 | 新增 `test/discovery_dispatch_test.dart` 8 用例（两源切源栏与默认源 / 切源清缓存 / 单源隐藏 / 网易新碟空态 / 网易新歌速递取数 / 两源歌单深链 `?src=` / 无能力源整页空态）；`test/netease_client_parse_test.dart` 追加 5 个 G 组 mapper 用例；`flutter analyze` 无问题；`flutter test` **407 项全过**（基线 394）。后经 §7.18 补 `discovery` 子开关 2 用例 → 该文件 10 用例；§7.23 再补新碟/歌手两能力 4 用例 → 14 用例 |

**一期已知边界**：`SourceFeature` 当时尚无 `discovery` 项，本页只接**整源开关**——
`kugou:rank` 单独关掉时 `/ranks` 会少一个源、本页榜单 Tab 不受影响。
**→ 2026-09-26 已由 §7.18 补齐：本页与首页「发现」共用 `discovery` 子开关。**
（② 原「网易推荐中心无对应接口」已由 §7.17 解除：G1/G7a 已接进「为你推荐」页。）

**待实测**（App 手动跑）：① 停用酷狗、只留网易 → 发现页应能出网易歌单（含「全部/华语/流行…」标签行）、
网易三榜、网易新歌，且不再整页「音源已停用」；② 两源都开 → 顶部切源条可来回切，切完标签行/歌单/榜单
整块跟着换、不串源；③ 网易源下点「新碟上架」「歌手」→ **2026-09-26 起应出真实内容**
（新碟 5 个地区 chips + 专辑网格；歌手三行 chips + 圆形歌手网格），不再是「暂不支持」文案；
④ 点网易歌单 → 进的是网易歌单详情（不是酷狗）；⑤ 停用网易、只留酷狗 → 五个 Tab 与改造前一致。

---

### 7.17 阶段 O 首页「发现」+「为你推荐」分发（2026-09-25 落地）

**诉求**：首页「发现」（App 落地页，`/`）与「为你推荐」推荐中心（`/recommend`）原先直连酷狗
`PlaylistRepository` / `RecommendRepository`，停用酷狗后整页废；网易 G1/G7a 协议早已具备但未接线。

**做法**：

| 项 | 内容 |
| --- | --- |
| 能力契约 | `capabilities.dart` 新增 **`RecommendFeedSource`**：`recommendPlaylists({cat, pageSize})` + `editorialPlaylists({pageSize})`。`cat` = **各源原生推荐分类值**（空串 = 该源默认推荐），与 `PlaylistCatalogSource.cat` 同口径 |
| 网易侧 | `NeteaseSource implements RecommendFeedSource`：`recommendPlaylists()` = **G1** `personalizedPlaylistsRaw(limit:)`（个性推荐歌单**无分类维度**，`cat` 忽略）；`editorialPlaylists()` = **G7a** `highQualityPlaylistsRaw(limit:)`。两者节点形态一致，**复用同一 mapper** `mapNeteaseTopPlaylists`（`root['playlists'] ?? data['playlists'] ?? root['result']`） |
| 酷狗侧 | `KugouSource implements RecommendFeedSource`：推荐歌单复用 `special_recommend`（`categoryid`，空串 = `0`）、编辑精选复用 `musicadservice/top_ip`；repository 的 `error` 字符串转抛 `NetworkFailure`，UI 统一读 `SourceFailure.message` |
| 首页 `/` | **不新增契约**：排行榜横排 + 今日热歌走 `RankSource`，新增「推荐歌单」横排走 `PlaylistCatalogSource.categoryPlaylists(cat: '')`，新增「新歌速递」列表走 `NewSongFeedSource.newSongs()`；三块 `Future.wait` 并行，`if (!mounted \|\| source != _source) return;` 防串源。定位为**浏览落地页**：只按 `effectiveDefaultSource` 取数、**不挂切源栏** |
| 推荐中心 `/recommend` | 可用源 = `enabledSources` ∩ 实现 `RecommendFeedSource` 的平台；首选 `effectiveDefaultSource`，不可用退首个可用源；可用源 >1 出 `SourceFilterBar(showAll: false)`，=0 才整页 `SourceDisabledView`；切源 `_resetData()`（请求号自增作废在途响应） |
| 网易「风格」形态 | 酷狗口径是「风格歌曲流 + 一键播放全部」（`fetchStyleRecommend`，**酷狗专属、无能力契约**）；网易侧退化为**风格歌单** = G7b 标签 chips + G6 分类歌单网格，**复用 `PlaylistCatalogSource`**；标题随形态切「风格歌单」/「风格推荐」，差异由 `_styleAsPlaylists => _source != MusicPlatform.kugou` 收口（**不为它引新契约**，避免把 `StyleTagGroup`/`StyleRecommendResult` 搬进 core/models） |
| 深链 | 卡片点击 `/playlist/:id`，非酷狗源带 `?src=<wireName>`（与搜索/榜单/发现页同口径） |
| 空态 | 可用源为 0 才整页停用；源可用但某块无数据 → 各块自己的 `_SectionStatus` / `AsyncBody` 普通错误态 |
| 验证 | 新增 `test/recommend_dispatch_test.dart` 8 用例（首页 4 + 推荐中心 4）；`flutter analyze` 无问题；`flutter test` **415 项全过**（基线 407） |

**待实测**（App 手动跑）：① 默认源网易、酷狗停用 → 首页四块（排行榜 / 今日热歌 / 推荐歌单 / 新歌速递）
应全为网易内容且不报错；② 默认源酷狗 → 与改造前一致；③ 两源都开进 `/recommend` → 顶部切源条可切、
切完三板块整块换源不串；④ 网易源下「风格」板块显示标签 chips + 歌单网格（标题「风格歌单」）、
没有「Hi-Res」分类 chips；点标签只换歌单、不重取标签；⑤ 点网易歌单卡片 → 进网易歌单详情。

---

### 7.18 阶段 P 补 `SourceFeature.discovery` 子开关（2026-09-26 落地）

**背景**：首页「发现」（`/`）与探索发现页（`/discovery`）当时只受**整源开关**约束，
设置里把 `kugou:rank` 单独关掉，这两页照样把酷狗列为可用源（榜单横排仍会出现），与榜单页 / 日推页
的行为不一致，属两级开关的漏网消费点。

| 项 | 内容 |
| --- | --- |
| 枚举 | `SourceFeature` 新增 `discovery('discovery', '发现')`（追加在末位，不动既有 4 项的落盘 id）。`supportedBy` 判据 = 具备 `PlaylistCatalogSource` / `NewSongFeedSource` / `RankSource` **任一**，与两页 `_availableSources()` 完全对齐 |
| 一项目覆盖两页 | 首页与探索发现页共用 `discovery`：两页取的是同一批「逛内容」的数据面（榜单 / 分类歌单 / 新歌），拆成两项只会让人在两处各关一遍。**不含**「为你推荐」推荐中心（有自己的 `RecommendFeedSource` 数据面），它本期仍只受整源开关约束 |
| 消费点 | `explore_page._availableSources()` 与 `discovery_page._availableSources()` 由 `enabledSources.contains(p)` 改为 `settings.isFeatureEnabled(p, SourceFeature.discovery)`；两处 `ref.listen` 早退条件加上 `disabledFeatures`；两处整页空态补 `feature: settings.featureSwitchCause(SourceFeature.discovery)`，文案从「X音源已停用」变为「X的「发现」已关闭」 |
| 设置页 | 无需改动——子项由 `for (final f in SourceFeature.values) if (f.supportedBy(...))` 自动列出（酷狗/网易都具备判据里的能力，故两源各多出一个「发现」开关） |
| 验证 | `test/discovery_dispatch_test.dart` 与 `test/recommend_dispatch_test.dart` 各加 2 用例：关掉 `kugou:discovery` → 该源从本页可用源移除、页面回落到另一源；两源都关 → 整页停用且文案归因到**功能**而非整源。`flutter analyze` 无问题；`flutter test` **419 项全过**（基线 415） |

**待实测**（App 手动跑）：设置里关掉「酷狗 → 发现」，回首页与探索发现页，应只剩网易内容、
两页都不再请求酷狗；把两源的「发现」都关掉，两页都显示「酷狗的『发现』已关闭」并可跳设置。

---

### 7.19 阶段 Q 网易写口探针补齐（2026-09-26，待手动实测）

本期不改产品行为，只把三个写口的**探针跑法**补齐，供手动执行后回填状态。写操作会真改账号，
故探针默认不发，必须显式 `--write`：

| 口 | 命令要点 | 说明 |
| --- | --- | --- |
| F4 喜欢 / 取消喜欢 | `--suite like --uid <uid> --write --like true\|false --id <songId>` | 用 `--like false` 走**取消喜欢**，两条都跑才算双向 |
| F5 歌单加曲 | `--suite like --uid <uid> --write --playlist <pid> --id <songId>` | 原有能力，无改动 |
| F5b 歌单删曲 | 同上再 `--del`（2026-09-26 新增开关） | 走 `removeSongsFromPlaylistRaw`（`op=del`）；漏传 `--playlist` 会直接提示 skip |
| F6 收藏专辑 | `--suite login,like --qr` | 探针改为**两行输出**：`count` + 首条记录键名；`count=0` 时直接提示「非空形态仍未验证」 |

**判定**：输出 `code=200` 即协议通；F4 需在 App/网页确认喜欢态真的翻转（`like=false` 后 F3 的
id 列表里应不再有它），F5/F5b 需在歌单里确认曲目真的增/减。

**这三项实测通过后**才能把产品层「喜欢」动作从「只写本地」改为**同时回写网易曲库**
（当前非酷狗源的 `Like` 只落本地，见 §7.8 与 `likesProvider.likeLocal()`）。

---

### 7.20 阶段 R「当前音源」全 App 展示 + 切源同步默认源（2026-09-26 落地）

**背景**：多源接入后，界面缺一个「现在用的是哪个源」的统一答案——多源页面之间的切源栏只在
多源时才出现，而首页「发现」、播放器、队列/历史、歌单/专辑/歌手详情、单曲详情这些位置完全看不出
曲目/内容来自酷狗还是网易云；单源用户更是全程零提示。另一个隐性问题：页面内切源只改本地筛选，
不回写 `settings.defaultSource`，导致「我在榜单页切到网易云」之后回首页又是酷狗。

**统一口径**：**多源 → 显示可切换 chips（既有 `SourceFilterBar`）；单源 → 显示一行只读小字。**

| 项 | 内容 |
| --- | --- |
| 新组件 | `SourceLabel`（`shared/widgets/common.dart`）——只读小字 `来源：X`，`prefix` 可覆盖、`compact` 去图标；与行内角标 `SourceBadge` 分工（前者陈述整页/整块来源，后者是行级角标） |
| 底部全局 | `RootShell` 在 mini player 与 Dock 之间插 `_SingleSourceHint`，**仅 `enabledSources.length == 1`** 时渲染（多源由各页 chips 承担）；桌面分支不挂 |
| 播放链路 | `FullPlayerPage._TrackMetaRow` 副标题行、`MiniPlayerBar` 歌手行、队列 sheet 副标题、历史列表 `TrackTile(showSource: true)` —— 混排队列下能看出「这一首」来自哪个源 |
| 详情页头部 | `/playlist/:id`、`/rank/:id`（共用 `RankDetailHeaderSurface`，Hero flight shuttle 同步带值）、`/album/:id`、`/artist/:id` 在**既有信息行**末尾追加 `来源：X`（零新增布局）；`/song` 歌手行给 `SourceBadge` |
| 首页「发现」 | `/explore` 保「浏览落地页」定位**不加**可切 chips，改在页头问候语下方给一行 `SourceLabel` |
| 封面来源角标（仅多源） | `PlaylistCard` 加可选 `platform`，非 null 时在**封面左上角**浮 `SourceBadge`（与右下角播放量胶囊对角，不占额外高度）；探索发现页歌单网格 / 首页推荐歌单条（`_PlaylistStrip` 透传）/ 推荐中心歌单网格，三处都在**多源**时才传当前源 |
| 切源同步默认源 | `SettingsController` 新增 `syncDefaultSourceFromFilter(MusicPlatform?)`：具体源才写 `defaultSource`，「全部」(`null`) 不写、已相同则不重复落盘；由 **9 处** `SourceFilterBar.onSelect` 各自调用（搜索/我喜欢/榜单/探索发现/推荐中心/每日推荐/私人 FM/我的/个人中心）。**不**放进组件内部，避免 `shared/widgets/` 反向依赖 `features/settings/` |
| 边界 | `setEnabledSources` 的「默认源回退」不变；`ref.listen(settingsControllerProvider)` 早退条件只看 `enabledSources`/`disabledFeatures`，写默认源**不会**触发多余重载（仅默认源变量本身变化） |
| 验证 | `flutter analyze` 无问题；`flutter test` **419 项全过**（基线 419，无新增用例——本次为纯 UI 呈现 + 一个 setter 调用；两处 `find.text('网易云')` 断言已收窄到 `SourceFilterBar` 内，避免与新增封面角标重名） |

**待实测**（App 手动跑）：
1. 多源（酷狗 + 网易云都启用）：各页仍显示可切 chips；在榜单页把源切到网易云 → 回首页，页头
   `来源：` 应已是网易云（默认源被同步）。
2. 单源（设置里只留酷狗）：底部 Dock 上方出现 `当前音源：酷狗` 一行小字；把源换成只留网易云 →
   小字随之变化。
3. 播放一首网易云曲目：迷你播放条、全屏播放页副标题行、队列 sheet 都应带「网易云」标识。

---

### 7.21 阶段 S 网易榜单列表动态化（G11 + 白名单精选，2026-09-26 落地）

**背景**：首页「发现」的排行榜区一直只有 3 张卡（飙升 / 新歌 / 热歌），因为 `NeteaseSource.officialBoards`
是**硬编码 3 个 id**，而当时的依据是「网易无榜单列表接口」——这是一条**未探测的假设**（全仓 grep
`toplist` 零命中）。用户追问后加探针验证。

**探针结论（`--suite toplist`，1 个请求）**：`POST /api/toplist/detail`（PLAIN、**免登录**）
`code=200`、`count=63`、`withCovers=63`；旧硬编码 3 个 id 全在集合内（飙升 100 / 新歌 100 / 热歌 200，
与 G10 实测吻合）。全集里大量是**活动/品牌/车友榜**（音乐合伙人 ×5、车友榜 ×8、`星云榜VOL.31…`、
`喜力®星电音派对潮音榜`、`蛋仔派对听歌榜`、`乐夏榜`、`AI歌曲榜` 等）。

**口径（用户确认）**：动态取数 + **白名单精选 20 张**（不全量铺、也不做分组 UI）。

| 项 | 内容 |
| --- | --- |
| 探针 | `G11` `toplistDetailRaw()` + `parseProbeToplist`（出 `code/count/withCovers/bytes/firstTracks/firstTrackKeys/boards`，后三项判体积与 `tracks` 形态）；`--suite toplist` 分发 + 文件头用例 |
| mapper | `mapNeteaseToplistBoards(raw)` → `List<({String id, String name, String coverUrl})>`：只取 UI 需要的三项，**整榜 `tracks` 不解析**（曲目走 G10）；`id` 为空的坏节点跳过。原 `mapNeteasePlaylistMeta`（只服务旧的补元数据）随之删除 |
| 源侧 | `officialBoards`（3 项）→ `boardWhitelist`（20 项，**数组顺序即首页横排优先级**）；`rankBoards()` 由「3 次 `playlistDetailRaw(id, n:1, s:0)`」改为「1 次 G11 + `boardsFromWhitelist()` 过滤排序」；`_boardMeta` 删除 |
| 失败兜底 | G11 抛错或白名单一条未命中 → **不重试、不抛错**，用白名单内置名兜底（封面空，UI 出占位图）；该情况**不写缓存**，下次进页面重试 |
| 缓存 | 成功结果存 `_boardsCache`（首页横排 / `/ranks` / 详情页换榜弹窗三处共用同一份），避免重复拉带 `tracks` 的大响应 |
| UI | **零改动**：首页本来就 `ranks.take(12)`（此前只有 3 张）、`/ranks` 自适应网格、探索发现页下拉选榜、换榜弹窗按 `rankTypeName` 分组——3 → 20 不必动布局 |
| 文档 | `网易云接口文档.md` §三 G11 登记为「✅ 通过」+ 备注；`多音源接入方案.md` 进度表 + 新增「网易榜单列表动态化」口径块；本文件 §7.2.1 表 + §7.16 榜单说明 |
| 验证 | `flutter analyze` 无问题；`flutter test` **423 项全过**（基线 419，新增 4 个 G11 用例：mapper 解析/坏节点跳过、白名单顺序与剔除活动榜、G11 不可用时兜底） |

**待实测**（App 手动跑，默认源=网易云）：
1. 首页「发现」排行榜横排应出现**多样化榜单**（飙升/新歌/原创/热歌/说唱/国风/民谣/摇滚/电音/古典/ACG/欧美热歌…），
   不再只有 3 张，且**看不到车友榜 / 音乐合伙人榜**。
2. 点「全部」进 `/ranks` → 应见 20 张榜、封面齐全；点进任意榜详情能出曲目。
3. 探索发现页「排行榜」Tab ↔ 下拉选榜：应能选到 20 张，切换后曲目跟着变。
4. 「今日热歌」区块仍是**热歌榜** Top 10（`_pickHotBoard` 按名字含「热歌」命中白名单第 4 项）。
5. 断网或接口异常时进榜单页：应退回**内置 20 个榜名**（封面灰块）而不是空页/报错。

---

### 7.22 阶段 T 新碟上架 / 歌手 探针（G12/G13，2026-09-26，**三轮实测双双通过**）

**背景**：用户在探索发现页（网易源）看到「新碟上架」Tab 出「网易云暂不支持「新碟上架」，可切换其他
音源查看」，追问「不支持是不是就不该显示」。评估后发现**「不支持」这个判断没有实测支撑**：
代码注释与三份文档互相引用同一句「网易无 `/weapi/album/new`、`/weapi/artist/list` **实测口径**」，
但这两条端点**从未进过《网易云接口文档.md》§三 总表**，CLI 探针**零覆盖**——与阶段 S 被推翻的
「网易无榜单列表接口」是**同一类错误**（把未探测的假设写成了实测结论）。

**反向证据**：① 网易侧同类口头已实测通过且已在用——歌手详情 D4/D5/D6/D7（`artistDetailRaw` /
`artistSongsRaw` / `artistAlbumsRaw`）、专辑 brief 解析 `mapNeteaseAlbumBrief`，缺的只是**列表入口**；
② 公开文档（NeteaseCloudMusicApi）有 `/api/album/new`（`area` + `limit` + `offset`）
与 `/api/artist/list`（`type` + `area` + `initial` + `limit` + `offset`）。

**本阶段只做探针，不改 UI**（用户明确「先不用开始修改」）。

| 项 | 内容 |
| --- | --- |
| 端点 | `netease_endpoints.dart` 新增 G12 `albumNew = '/api/album/new'`、G13 `artistList = '/api/v1/artist/list'`（**必须带 `v1`**） |
| 客户端 | `albumNewRaw({area='ALL', limit=50, offset=0})`（带 `total=true`）；`artistListRaw`（**明文，生产用**）/ `artistListWeapiRaw`（`callWeApi('/v1/artist/list')`，对照备用）；入参统一走 public static `artistListParams`（`initial` 转 ASCII 码 + 空串省略 + `total=true`） |
| 摘要函数 | `parseProbeAlbumNew(raw, label:)`、`parseProbeArtistList(raw, label:)`：出 `code/count/total|more/first/head/firstKeys/cover/bytes`。**`head` = 前 3 条 `name(id)`**，是判「筛选是否生效」的关键——若换参数后 `head` 与 `bytes` 完全不变，即参数被忽略。兼容 `albums[]`/`artists[]`/`data[]` 与 `result.*`（`_probeList`/`_probeDeep`/`_probeFirstMap`/`_probeHead` 为探针私有小工具） |
| 探针用例 | `--suite albumnew`：5 个 `area`；`--suite artistlist`：三轮迭代后为 11 次（明文 area/type/initial/组合 9 次 + weapi 对照 2 次），非 200 时打印响应原文 |
| 单测 | `netease_client_parse_test.dart` 新增 `G13 歌手列表入参` 组 3 用例（`initial` 字母转码 / 空串省略与数字透传 / `total=true` 必带），锁死易错契约（423 → 426） |
| 文档更正 | 三处「**实测口径**」降级为「**未验证假设**」（本文 §7.16「酷狗限定 Tab」行、`网易云接口文档.md` §五探索发现条与「未实测项」、`多音源接入方案.md` 探索发现口径块），并登记 G12/G13 到 §三 总表 |
| 未改 | `discovery_page.dart` / `discovery_dispatch_test.dart` **一行未动**——引接口能力另开阶段 |

**三轮实测（2026-09-26，全部跑完）**：

- **G12 新碟上架 = ✅ 明文即通，`area` 生效**。5 个 `area` 全 `code=200`、`count=10`、`total=500`、
  封面齐全、`bytes≈13–15KB`。各区首条：`ALL`/`JP`=ぬくもり列車、`ZH`=痴缠(阿YueYue)、
  `EA`=They Forgot(Tee Grizzley)、`KR`=구독연애2 OST（前两者相同属时间倒序巧合）。
- **G13 歌手列表 = ✅ `type`/`area`/`initial` 全部生效**（三轮才通，详见下表）。

**G13 三轮定位过程（踩坑留档）**：

| 轮次 | 做法 | 结果 | 推断 |
| --- | --- | --- | --- |
| 1 | `/api/artist/list` + 明文，6 area + 3 type | 响应**逐字节相同**（`bytes=4544`、恒为陈奕迅 2116）；`initial=a` 单独回 `400` | 参数被忽略，但 `initial` 名被认到 → 疑加密形态问题 |
| 2 | 并测明文 / weapi(`callWeApiAt`) / GET | 仍全相同；weapi 连 `limit` 都丢（`limit=5` 回 10 条） | 「加密体 + 原 `/api/` 路径」服务端不认 |
| 3 | 对照 NeteaseCloudMusicApi 4.32.0 `module/artist_list.js` 修正后复测 | **全通** | 见下三条差异 |

**三条差异（根因）**：

| 差异 | 参考实现 | 我们原来（错） | 后果 |
| --- | --- | --- | --- |
| 路径 | `/api/v1/artist/list` | `/api/artist/list` | 命中**旧路由**：只认 `limit`，`type`/`area`/`initial` 全忽略 |
| `initial` | 转**大写 ASCII 码**（`'a'`→`65`） | 直接传 `'a'` | 服务端要数字 → `code: 400` |
| 必带 | `total: true` | 未传 | 与路径问题叠加，退化为旧路由行为 |

**附带的通用教训**：NeteaseCloudMusicApi 的 `request` 对 weapi 会做
`url.replace(/\w*api/, 'weapi')`，**路径前缀也换成 `/weapi`**（`/api/v1/...` → `/weapi/v1/...`），
不是「加密体 + 原 `/api/` 路径」。G7b `/api/playlist/highquality/tags` 恰好两种都通，掩盖了这个差异；
G13 只有正确形态才通。故本仓 `callWeApiAt`（原路径 + 加密体）只适用于「双认」的老路由。

**第三轮实测值**：`area=96`→Justin Bieber/The Weeknd/Taylor Swift；`type=2`→孙燕姿/G.E.M.邓紫棋/梁静茹；
`type=3`→五月天/Beyond/儿歌多多；`initial=a`→艾志恒Asen/黄丽玲/艾热 AIR；`initial=z`→郑润泽/赵雷/张杰；
叠加 `area=96 type=2 initial=z`→Zara Larsson/Zella Day/Zendaya。

**结论与下一步**：「新碟上架」「歌手」两个 Tab 的**「网易不支持」依据已被彻底推翻**（两个口都通）。
接口层已就绪，UI 接入见 §7.23（同日完成）。

---

### 7.23 阶段 U 新碟上架 / 歌手 两能力接入（G12/G13，2026-09-26 落地）

**诉求**：承接 §7.22 —— G12/G13 探针双双通过后，把「新碟上架」「歌手」两个 Tab 从
「非酷狗源暂不支持」改为按源能力取数，并统一五个 Tab 的判定口径。

**做法**：

| 项 | 内容 |
| --- | --- |
| 能力契约 | `core/source/capabilities.dart` 新增 `NewAlbumFeedSource`（`albumRegions` + `newAlbums(region:)`）与 `ArtistListSource`（`artistGenderOptions` / `artistStyleOptions` / `artistInitialOptions` + `artistList(gender, style, initial)`）。**选项一律由实现给出、首项即默认**，UI 只渲染 chips 并原样回传 id（同 `PlaylistCatalogSource.cat` 口径） |
| 网易侧 | `NeteaseSource implements NewAlbumFeedSource, ArtistListSource`：新碟地区 `ALL/ZH/EA/KR/JP` → G12 `albumNewRaw(area:, limit:)`；歌手 = `type`(-1/1/2/3) + `area`(-1/7/96/8/16/0) + 首字母 A–Z（静态，服务端 `initial` 筛选）→ G13 `artistListRaw(type:, area:, initial:, limit:)`。mapper 新增 `mapNeteaseNewAlbums` / `mapNeteaseArtistList`（`albums[]` / `artists[]`，顶层或 `data` 下） |
| 封面修正 | **G12 新碟节点只给 `picId`（无 `picUrl`）**，`mapNeteaseAlbumBrief` 的封面改走 `_albumCover`（含 `picId` → CDN 直链回退），否则新碟整列灰块（G13 歌手头像走 `img1v1Url`，原映射已覆盖） |
| 酷狗侧 | `KugouSource implements` 同两能力：新碟转 `DiscoveryRepository.albumTypes` + `fetchNewAlbums(type:)`；歌手转 `artistSexTypes` / `artistTypes` + `fetchArtists(...)`，`DiscoveryArtist` **仍在 data 层**、由源内转 `ArtistBrief` 出口（不上移 core、`ArtistBrief` 不加 `letter` 字段）。**字母是响应自带分组**：由 `_artistCache` 回填 `artistInitialOptions`（首次为空 → 该行隐藏，取数后出现）并按字母本地过滤；**切性别/流派后旧字母消失时按「不筛」处理**，页面取数后把 chips 回退首项，避免闪空列表 |
| 页面 | 两 Tab 判定由整页布尔 `_legacyCatalogSupported`（仅酷狗）换成 `_capability<NewAlbumFeedSource/ArtistListSource>(active)`，与另三个 Tab 同口径；新增 `_pickOption`（切源/首帧选中值不在选项里 → 回退首项）、`_filterChips`（通用 chips 行）、`_srcSuffix`（深链源标记，酷狗不带）；新碟/歌手卡片深链 `/album/:id?src=`、`/artist/:id?src=` |
| 验证 | `test/discovery_dispatch_test.dart`：改写原「网易新碟空态」用例为 5 个新用例（新碟走能力且地区 id 原样回传 / 新碟深链带 `?src=` / 歌手走能力且三项筛选原样回传 / 歌手深链带 `?src=` / 缺能力源两 Tab 出「暂不支持」空态），10 → 14 用例；`test/netease_client_parse_test.dart` 追加 5 个 G12/G13 mapper 用例。`flutter analyze` 无问题；`flutter test` **435 项全过**（基线 426） |
| 文档 | `网易云接口文档.md` §三 G12/G13 行标「已接产品/能力」、§五探索发现条改为「五 Tab 全接上」；`多音源接入方案.md` 探索发现口径块删除「暂为酷狗限定」并改写为按能力分发口径 |

**待实测**（App 手动跑，接 §7.16 待实测③）：① 网易源点「新碟上架」→ 出 5 个地区 chips（全部/华语/欧美/日本/韩国）、
切区换列表，封面不是灰块；② 网易源点「歌手」→ 出性别/地区/首字母三行 chips，切换有真实筛选效果，
头像圆形有图；③ 酷狗源同两 Tab 与改造前一致（含字母行「取数后才出现」的时序）；④ 两 Tab 点卡片 →
分别进对应源的 `/album/:id`、`/artist/:id` 详情。

---

## 八、风险与应对（一期）

| 风险 | 应对 |
| --- | --- |
| 风控 -460 / 限频 | 降低请求频率；探针一次跑完全套后停；记 UA/Cookie |
| WEAPI 公钥/常量笔误 | 与 Neri `NeteaseCrypto` **逐字节**对照；单测锁输出形状 |
| Dio 不解 Brotli | 显式 br 解码或要求服务端 gzip（Accept-Encoding） |
| `__csrf` 缺失导致 weapi 失败 | 强制 S2 预热；探针打印 csrf 有无 |
| 无 VIP 无 url | 视为**合法结果**，记 `NoPermission`，不算接口失败 |
| 范围蔓延 | 一期 DoD 冻结；UI/登录一律二期 |

---

## 九、一页纸

> **一期只打穿协议**：weapi/eapi + Cookie/csrf + 5 个业务口（搜索 / 播放 URL×2 / 歌词×2 / 详情）。  
> 产物：`core/api/netease/` + CLI + Debug 探针页。  
> **约 9 人日**；出口是「探针能搜到歌、拿到 url、拿到歌词」，不是「App 里能听网易云」。
