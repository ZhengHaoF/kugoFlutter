# 网易云接口文档

> 文档版本：v0.1 · 全量接口清单 + 实现/测试状态
> 对齐基准：`D:\work\NeriPlayer` `core/api/netease/`
> 本仓实现：`app/lib/core/api/netease/`
> 状态截止：2026-09-24
>
> 配套：[网易云接入对比酷狗.md](网易云接入对比酷狗.md) · [网易云接入排期.md](网易云接入排期.md) · [多音源接入方案.md](多音源接入方案.md)

---

## 一、状态图例

| 符号 | 含义 |
| --- | --- |
| ✅ **通过** | 真机/CLI 打通，拿到预期业务数据 |
| 🟡 **代码就绪 / 待测** | Dart 方法已对齐 Neri 参数，未跑或未用登录态跑 |
| ⚠️ **偏离 / 降级** | 与 Neri 端点或参数不完全一致，见备注 |
| ❌ **未实现** | 尚未写入 `NeteaseClient` |
| 🔒 **写操作未触发** | 方法在，故意不发（避免误改账号） |

**加密/会话状态**：weapi ✅ · eapi ✅ · plain API ✅ · Cookie 会话 ✅ · `__csrf` 预热 ⚠️（首页常无 Set-Cookie，weapi 无 csrf 仍可用）

---

## 二、协议与 Host

| 通道 | Host | 形态 | 用途 |
| --- | --- | --- | --- |
| **WEAPI** | `music.163.com`（部分 `interface.music.163.com`） | POST Form：`params` + `encSecKey`，query `csrf_token` | 搜索、账号、like、用户歌单、专辑、短信 |
| **EAPI** | `interface.music.163.com`（部分 `interface3.music.163.com`） | POST Form：`params` 大写 hex | 播放 URL、歌词、手机登录、收藏专辑 |
| **PLAIN API** | `music.163.com` | POST Form 明文 | 歌单/歌手详情、艺人歌曲专辑 |

**加密常量（与 Neri 逐字一致）**：`PRESET_KEY=0CoJUm6Qyw8W8jud` · `IV=0102030405060708` · `EAPI_KEY=e82ckenh8dichen8` · 盐 `nobody%suse%smd5forencrypt` · 格式 `%s-36cd479b6b5-%s-36cd479b6b5-%s` · RSA 公钥见 `NeteaseCrypto.publicKeyPem`（无填充 modPow）

**实现文件**

| 文件 | 职责 |
| --- | --- |
| `netease_crypto.dart` | weapi / eapi / linux / anonymous / md5 |
| `netease_endpoints.dart` | 路径常量 |
| `netease_client.dart` | callWeApi / callEApi / callPlainApi + 全部业务方法 |
| `netease_failures.dart` | code → `SourceFailure` |
| `tool/probe_netease_api.dart` | CLI 探针 |

---

## 三、接口总表

### A. 搜索

| # | 用途 | 端点 | 通道 | 关键参数 | 本仓方法 | Neri | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| A1 | 搜索单曲 | `POST /weapi/search/get` | WEAPI | `s, type=1, limit, offset` | `searchSongsRaw` | 用 `cloudsearch/get/web` | ✅ **通过**（⚠️ 端点偏离） |
| A1b | 云搜索（Neri 默认） | `POST /weapi/cloudsearch/get/web` | WEAPI | `s, type, limit, offset, total=true` | `searchSongsDebug` 保留 | ✅ | ⚠️ 实测 **50000005**，仅对照 |
| A1c | 分类 type | 同 A1 | WEAPI | `type=1/10/100/1000` 单曲/专辑/歌手/歌单 | 参数已支持 | ✅ | 🟡 只测过 type=1 |

**A1 备注**：Neri 同 weapi 体系；`cloudsearch` 在本环境失败，产品接入默认 `search/get`。

---

### B. 播放

| # | 用途 | 端点 | 通道 | 关键参数 | 本仓方法 | Neri | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| B1 | 播放 URL（主） | `POST /eapi/song/enhance/player/url/v1` | EAPI | `ids=[id], level, encodeType=flac` | `songPlayUrlRaw` | ✅ | ✅ **通过**（出真 mp3；`level=exhigh`） |
| B2 | 播放 URL（备） | `POST /weapi/song/enhance/player/url` | WEAPI | `ids=[id], br=320000` | `songPlayUrlWeapiRaw` | ✅ | ✅ **通过**（协议通） |
| B3 | 301 预热重试 | B1/B2 + `ensureWeapiSession` | — | code==301 重试 | ⚠️ 解析 301，**无自动重试** | ✅ | 🟡 二期补 |
| B4 | 音质 level | B1 | — | `standard/higher/exhigh/lossless/hires/jyeffect/sky/jymaster` | 传参支持 | ✅ | 🟡 只实测 exhigh |
| B5 | 试听识别 | 解析 `freeTrialInfo` | — | — | `isPreviewClip` | ✅ | ✅ 解析就绪 |
| B6 | 无版权 | 解析 `code=404` / `cannotListenReason` | — | — | `mapNeteasePlayFailure` | ✅ | ✅ **通过** |

**level 对照**（接 `AppQuality`）：standard→standard · hq→exhigh · sq→lossless · hiRes→hires；扩展档 jyeffect/sky/jymaster 挂网易侧。

---

### C. 歌词

| # | 用途 | 端点 | 通道 | 关键参数 | 本仓方法 | Neri | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| C1 | 歌词（主） | `POST /eapi/song/lyric/v1` | EAPI | `id, cp=false, lv=0, tv=1, rv=0, yv=1, ytv=1, yrv=0` | `songLyricRaw` | ✅ | ✅ **通过**（lrc/tlyric） |
| C2 | 歌词（备） | `GET /api/song/lyric` | GET | `id, lv=-1, tv=-1, rv=-1, yv=-1, ytv=-1, yrv=-1` | `songLyricPlainRaw` | ✅ SearchApi | 🟡 方法就绪，未单测 |
| C3 | 字段 | 解析 `lrc/yrc/tlyric/romalrc/ytlrc` | — | — | `parseProbeLyric` | ✅ | ✅ lrc/tlyric 有值；YRC 视曲目 |

---

### D. 曲目 / 详情页

| # | 用途 | 端点 | 通道 | 关键参数 | 本仓方法 | Neri | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| D1 | 单曲详情 | `POST /weapi/v3/song/detail` | WEAPI | `c=[{"id":…}], ids=[…]` | `songDetailRaw` | ✅ | ✅ **通过** |
| D2 | 歌单详情 | `POST /api/v6/playlist/detail` | PLAIN | `id, n=100000, s=8` | `playlistDetailRaw` | ✅ | ✅ **通过**（1268 首） |
| D3 | 专辑详情 | `POST /weapi/v1/album/{id}` | WEAPI @interface | `n, s` | `albumDetailRaw` | ✅ | ✅ **通过**（张悬《神的游戏》9 首） |
| D4 | 歌手头部信息 | `POST /api/artist/head/info/get` | PLAIN | `id` | `artistDetailRaw` | ✅ | ✅ **通过**（周杰伦 568 曲 / 44 专） |
| D5 | 歌手动态 | `POST /api/artist/detail/dynamic` | PLAIN | `id` | `artistDynamicRaw` | ✅ | 🟡 就绪待测 |
| D6 | 歌手热门/歌曲 | `POST /api/v1/artist/songs` | PLAIN | `id, private_cloud=true, work_type=1, order=hot, offset, limit` | `artistSongsRaw` | ✅ | ✅ **通过** |
| D7 | 歌手专辑 | `POST /api/artist/albums/{id}` | PLAIN | `limit, offset, total=true` | `artistAlbumsRaw` | ✅ | 🟡 就绪待测 |
| D8 | 相关歌单 | `GET /music.163.com/playlist?id=` HTML 抓取 | GET | — | ❌ | ✅ | ❌ 未实现（可选） |
| D9 | 雷达歌单元数据 | `POST /api/playlist/detail` | PLAIN | `id,n,s,uiPlaylistType=MGC` | ❌ | ✅ | ❌ 未实现（P2 特色） |

---

### E. 登录 / 账号

| # | 用途 | 端点 | 通道 | 关键参数 | 本仓方法 | Neri | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| E1 | 当前账号 | `POST /weapi/w/nuser/account/get` | WEAPI | `{}` | `accountRaw` | ✅ | ✅ **通过**（游客 200、无 profile） |
| E2 | 扫码 unikey | `POST /weapi/login/qrcode/unikey` | WEAPI | `type=1, noCheckToken=true` | `qrUnikeyRaw` | ✅ | ✅ **通过**（拿到 unikey） |
| E3 | 扫码轮询 | `POST /weapi/login/qrcode/client/login` | WEAPI | `type=1, noCheckToken=true, key, ydDeviceToken` | `qrCheckRaw` | ✅ | 🟡 就绪；完整链路要 **yd_token** |
| E4 | 手机+密码 | `POST /eapi/w/login/cellphone` | EAPI | `phone, countrycode, remember, password=md5, type=1` | `loginByPhoneRaw` | ✅ | 🟡 就绪；**未用真实密码测** |
| E5 | 短信登录 | 同 E4 | EAPI | + `captcha` | `loginByCaptchaRaw` | ✅ | 🟡 同上 |
| E6 | 发验证码 | `POST /weapi/sms/captcha/sent` | WEAPI @interface | `cellphone, ctcode` | `sendSmsCaptchaRaw` | ✅ | 🟡 就绪 |
| E7 | 验验证码 | `POST /weapi/sms/captcha/verify` | WEAPI @interface | `cellphone, captcha, ctcode` | `verifySmsCaptchaRaw` | ✅ | 🟡 就绪 |
| E8 | 网页 WebView 登录 | `music.163.com/m/login` 或首页 | WebView | UA 按屏宽 | ❌ | ✅ | ❌ 未实现 |
| E9 | yd 设备指纹 | 页面 `createNEFingerprint` | WebView | `yd_token` / `sDeviceId` | ❌ | ✅ | ❌ 扫码完整链路依赖 |

**扫码状态码**（Neri）：800 生成二维码 · 801 待扫 · 802 待确认 · **803 成功**（`x-refresh-token` → `MUSIC_U`）。

---

### F. 我喜欢 / 用户歌单

| # | 用途 | 端点 | 通道 | 关键参数 | 本仓方法 | Neri | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| F1 | 用户歌单列表 | `POST /weapi/user/playlist` | WEAPI | `uid, offset, limit, includeVideo=true` | `userPlaylistsRaw` | ✅ | 🟡 就绪；游客无 uid 跳过 |
| F2 | 我喜欢 id | 解析 F1：`specialType==5` 或名含「我喜欢」 | — | — | `parseProbeUserPlaylists` | ✅ | 🟡 待登录测 |
| F3 | 喜欢歌曲 id 列表 | `POST /weapi/song/like/get` | WEAPI | `uid` | `likedSongIdsRaw` | ✅ | 🟡 待登录测 |
| F4 | 喜欢 / 取消喜欢 | `POST /weapi/song/like` | WEAPI | `trackId, like=true/false, time?` | `likeSongRaw` | ✅ | 🔒 **写操作未触发** |
| F5 | 歌单加曲 | `POST /weapi/playlist/manipulate/tracks` | WEAPI | `op=add, pid, id, tracks=csv, trackIds=json, imme=true` | `addSongsToPlaylistRaw` | ✅ | 🔒 写操作未触发 |
| F6 | 收藏专辑 | `POST /eapi/mine/rn/resource/list` | EAPI @interface3 | `userId, offset, limit, pageType=3, needRcmd=0, isVistor=false, includeStarPodcast=true` | `userAlbumsRaw` | ✅ | 🟡 待登录测 |
| F7 | 我喜欢回写同步 | F2–F5 组合 | — | 本地收藏 → 云端 | ❌ | ✅ | ❌ 二期产品层 |
| F8 | 收藏歌单 / 关注歌手 | （Neri 资料库） | WEAPI | — | ❌ | ✅ | ❌ 未实现 |

---

### G. 推荐 / 发现（二期产品用）

| # | 用途 | 端点 | 通道 | 关键参数 | 本仓方法 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| G1 | 个性推荐歌单 | `POST /weapi/personalized/playlist` | WEAPI | `limit` | `personalizedPlaylistsRaw` | ✅ **通过**（游客 5 单） |
| G2 | 每日推荐歌单 | `POST /weapi/v1/discovery/recommend/resource` | WEAPI | `{}` | `dailyRecommendResourceRaw` | ⚠️ **301 需登录**（预期） |
| G3 | 每日推荐歌曲 | `POST /weapi/v3/discovery/recommend/songs` | WEAPI | `afresh=true/false` | `dailyRecommendSongsRaw` | ✅ **通过**（游客 24 曲） |
| G4 | 私人 FM | `POST /weapi/v1/radio/get` | WEAPI | `{}` | `personalFmRaw` | ✅ **通过**（出 1 首） |
| G5 | 新歌推荐 | `POST /weapi/personalized/newsong` | WEAPI | `type=recommend, limit, areaId=0` | `personalizedNewSongsRaw` | ✅ **通过** |
| G6 | 热门/分类歌单 | `POST /weapi/playlist/list` | WEAPI | `cat, order=hot, limit, offset, total=true` | `topPlaylistsRaw` | ✅ **通过** |
| G7a | 精品歌单 | `POST /weapi/playlist/highquality/list` | WEAPI | `cat, limit, lasttime, total=true` | `highQualityPlaylistsRaw` | ✅ **通过** |
| G7b | 精品标签 | `POST /api/playlist/highquality/tags` | **WEAPI 加密 + 非 weapi 路径** | `{}` | `highQualityTagsRaw` / `callWeApiAt` | ✅ code=200（结构待解析） |
| G9 | 雷达/官单元数据 | `POST /api/playlist/detail` | PLAIN | `id, n=1, s=0, uiPlaylistType=MGC` | `radarPlaylistMetaRaw` | ✅ **通过**（时光雷达 35 曲） |
| G8 | 评论 / 热搜 | （Netease 包未覆盖） | — | — | ❌ | ❌ 待自研 |
| G10 | 官方榜单曲目 | 复用 `playlistDetailRaw`（飙升 19723756 / 新歌 3779629 / 热歌 3778678） | PLAIN | id | ✅ | 🟡 同 D2，可直接用 |

**G 组备注**：G3/G4 游客也有数据（与 Neri「需登录」标注略有出入，以实测为准）；G2 是唯一强制登录的推荐口。

---

## 四、失败码映射（已实现 `mapNeteaseCode` / `mapNeteasePlayFailure`）

| 业务现象 | 码 / 字段 | 统一错误 |
| --- | --- | --- |
| 需登录 / 会话失效 | body `code==301` | `LoginRequired` |
| 扫码中间态 | 800 / 801 / 802 | `LoginRequired`（探针另报） |
| 扫码成功 | 803 | —（写 Cookie） |
| 无版权 / 不可播 | 曲目 `code=404` 或无 `url`；`cannotListenReason=1`；`fee>0` | `NoPermission` |
| 试听 | `freeTrialInfo` 非空 | `isPreviewClip=true` |
| 风控 / 限频 | `-460` / `-462` / 405 | `RateLimited` |
| 未找到 | 404 | `NotFound` |
| 解析失败 | 非 JSON / 未知 code | `UpstreamChanged` |

**与酷狗对照**：酷狗 **没有 301**；风控是 `errcode=20028`，勿混用。

---

## 五、状态汇总（截至 2026-09-24）

| 类 | ✅ 通过 | 🟡 就绪待测 | 🔒 写未触发 | ❌ 未实现 |
| --- | --- | --- | --- | --- |
| 搜索 | 1（A1） | 2（A1b/A1c） | — | — |
| 播放 | 3（B1/B2/B6） | 2（B3/B4） | — | — |
| 歌词 | 2（C1/C3） | 1（C2） | — | — |
| 详情 | 5（D1–D4、D6） | 2（D5/D7） | — | 2（D8/D9） |
| 登录账号 | 2（E1/E2） | 5（E3–E7） | — | 2（E8/E9） |
| 我喜欢 | — | 3（F1/F2/F3/F6） | 2（F4/F5） | 2（F7/F8） |
| 推荐发现 | 7（G1/G3–G7a/G7b/G9） | 1（G10） | — | 2（G2 待登录 / G8） |

**一句话**：搜索 / 播放 / 歌词 / 详情 / 账号 / 扫码 unikey / **推荐发现（G 组）** 协议已通；**登录后**的我喜欢列表与写操作待测；评论/热搜与完整扫码链待实现。

---

## 六、探针用法

```bash
# 一期：搜索 + 播放 + 歌词 + 单曲详情
dart run tool/probe_netease_api.dart --keyword love

# 登录 / 我喜欢 / 详情
dart run tool/probe_netease_api.dart --suite login,like,detail

# 登录后复测我喜欢
dart run tool/probe_netease_api.dart --suite like --uid <你的uid>

# 推荐 / 发现
dart run tool/probe_netease_api.dart --suite discover

# 指定实体
dart run tool/probe_netease_api.dart --suite detail --playlist 24381616 --album 32311 --artist 6452
```

---

## 七、已知坑

| 坑 | 说明 |
| --- | --- |
| 搜索口 | `cloudsearch/get/web` → `50000005`；默认 `/weapi/search/get` |
| search type | 单曲 1 / 专辑 10 / 歌手 100 / **歌单 1000** |
| weapi JSON | 字符串字段用 `"1"` 不要裸数字时，与 Neri 对齐更稳 |
| RSA | **无填充** modPow，不是 PKCS1 |
| eapi 签名 | path 去 `/eapi`→`/api` 再拼盐 MD5；params **大写 hex** |
| 表单 | `application/x-www-form-urlencoded` UTF-8，勿用 multipart |
| 首页 GET | 常 **无 Set-Cookie**，`__csrf` 可能一直空 |
| 无版权曲 | 协议成功 + 业务 `404`，不算接口失败 |
| 播放 headers | 下载封面/音频时 Referer 按 CDN（163 vs kugou） |

---

## 八、一页纸

> **已通**：weapi/eapi 加密、搜索(`search/get`)、播放 URL、歌词、单曲/歌单/专辑/歌手详情、账号、扫码 unikey。  
> **待测（需登录）**：我喜欢列表 / 红心 / 加曲 / 收藏专辑。  
> **待实现**：扫码完整链（yd_token）、WebView 登录、每日推荐 / FM / 雷达 / 精品歌单、我喜欢回写、评论/热搜。  
> 写方法已就绪但 **🔒 不自动触发**。
