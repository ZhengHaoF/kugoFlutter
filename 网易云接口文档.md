# 网易云接口文档

> 文档版本：v0.1 · 全量接口清单 + 实现/测试状态
> 对齐基准：`D:\work\NeriPlayer` `core/api/netease/`
> 本仓实现：`app/lib/core/api/netease/`
> 状态截止：2026-09-24
>
> 配套：[网易云接入对比酷狗.md](网易云接入对比酷狗.md) · [网易云接入排期.md](网易云接入排期.md) · [多音源接入方案.md](多音源接入方案.md)

---

## 一、状态图例

| 符号 | 含义 |
| --- | --- |
| ✅ **通过** | 真机/CLI 打通，拿到预期业务数据 |
| 🟡 **代码就绪 / 待测** | Dart 方法已对齐 Neri 参数，未跑或未用登录态跑 |
| ⚠️ **偏离 / 降级** | 与 Neri 端点或参数不完全一致，见备注 |
| ❌ **未实现** | 尚未写入 `NeteaseClient` |
| 🔒 **写操作未触发** | 方法在，故意不发（避免误改账号） |

**加密/会话状态**：weapi ✅ · eapi ✅ · plain API ✅ · Cookie 会话 ✅ · `__csrf` 预热 ⚠️（首页常无 Set-Cookie，weapi 无 csrf 仍可用）

---

## 二、协议与 Host

| 通道 | Host | 形态 | 用途 |
| --- | --- | --- | --- |
| **WEAPI** | `music.163.com`（部分 `interface.music.163.com`） | POST Form：`params` + `encSecKey`，query `csrf_token` | 搜索、账号、like、用户歌单、专辑、短信 |
| **EAPI** | `interface.music.163.com`（部分 `interface3.music.163.com`） | POST Form：`params` 大写 hex | 播放 URL、歌词、手机登录、收藏专辑 |
| **PLAIN API** | `music.163.com` | POST Form 明文 | 歌单/歌手详情、艺人歌曲专辑 |

**加密常量（与 Neri 逐字一致）**：`PRESET_KEY=0CoJUm6Qyw8W8jud` · `IV=0102030405060708` · `EAPI_KEY=e82ckenh8dichen8` · 盐 `nobody%suse%smd5forencrypt` · 格式 `%s-36cd479b6b5-%s-36cd479b6b5-%s` · RSA 公钥见 `NeteaseCrypto.publicKeyPem`（无填充 modPow）

**实现文件**

| 文件 | 职责 |
| --- | --- |
| `netease_crypto.dart` | weapi / eapi / linux / anonymous / md5 |
| `netease_endpoints.dart` | 路径常量 |
| `netease_client.dart` | callWeApi / callEApi / callPlainApi + 全部业务方法 |
| `netease_failures.dart` | code → `SourceFailure` |
| `tool/probe_netease_api.dart` | CLI 探针 |

---

## 三、接口总表

### A. 搜索

| # | 用途 | 端点 | 通道 | 关键参数 | 本仓方法 | Neri | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| A1 | 搜索单曲 | `POST /weapi/search/get` | WEAPI | `s, type=1, limit, offset` | `searchRaw` | 用 `cloudsearch/get/web` | ✅ **通过**（⚠️ 端点偏离） |
| A1b | 云搜索（Neri 默认） | `POST /weapi/cloudsearch/get/web` | WEAPI | `s, type, limit, offset, total=true` | `searchSongsDebug` 保留 | ✅ | ⚠️ 实测 **50000005**，仅对照 |
| A1c | 分类 type | 同 A1 | WEAPI | `type=1/10/100/1000` 单曲/专辑/歌手/歌单 | `searchRaw(type:)` | ✅ | ✅ **通过**（四类各 10 条；字段名 2026-09-25 全部实测，单曲为旧结构） |

**A1 备注**：Neri 同 weapi 体系；`cloudsearch` 在本环境失败，产品接入默认 `search/get`。

**A1c 备注**：`NeteaseSource` 四个搜索（单曲/歌单/专辑/歌手）共用 `searchRaw`，仅 `type` 不同，
响应经 `netease_mappers.dart` 的 `mapNeteaseSearchSongs/Playlists/Albums/Artists` 映射为
`SearchPageResult<统一 Brief>`。`total` 取 `result.<x>Count`，取不到则返回 null；mapper 一律
**多键回退** + 跳过无效节点（id 非数字 → null）。

**A1c 字段名实测（2026-09-25，`--types 1,10,1000`）**：**四类里只有单曲是旧结构**，其余三类字段齐全。

| type | 首条 `keys` | 封面字段 | mapper 现状 |
| --- | --- | --- | --- |
| 1 单曲 | `album,fee,duration,rtype,ftype,artists,copyrightId,mvid,name,alias,id,mark,status` | ⚠️ **无 `picUrl`**，`album` 只有 `picId` | 已修（见下） |
| 10 专辑 | `name,id,type,size,picId,blurPicUrl,companyId,pic,picUrl,…` | ✅ `picUrl` | 直接命中 |
| 100 歌手 | 含 `picUrl` / `img1v1Url` | ✅ | 直接命中 |
| 1000 歌单 | `id,name,coverImgUrl,creator,subscribed,trackCount,…` | ✅ `coverImgUrl` | 直接命中 |

**旧口单曲封面（2026-09-25 修复）**：`search/get` 的曲目把专辑塞在 `album` 里，键为
`publishTime,size,artist,copyrightId,name,id,picId,mark,status`——**只有数字 `picId`，没有 URL**
（新口 `cloudsearch` 的 `al.picUrl` 才有）。故 `mapNeteaseSong` 的封面改为
`album.picUrl` → 空则 `NeteaseCrypto.picUrl(album.picId)`：按官方算法（`picId` 与盐
`3go8&$8*3*3h0k(2)2` 逐字节异或 → MD5 → base64Url）拼 `https://p3.music.126.net/{hash}/{picId}.jpg`。
**实测**：`picId=109951163038292176` → `200 image/jpg`，内容为对应专辑封面（见单测已知向量）。
修复前该字段为空串，列表 `CoverBox` 会退化成灰色渐变块（表现为「封面没显示出来」）。

**核对办法**：`dart run tool/probe_netease_api.dart --types 1,10,100,1000`
（探针打印首条记录的 `keys`；单曲额外打印 `albumKeys` / `albumCover` 以便核对 `album` 内层字段）。


---

### B. 播放

| # | 用途 | 端点 | 通道 | 关键参数 | 本仓方法 | Neri | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| B1 | 播放 URL（主） | `POST /eapi/song/enhance/player/url/v1` | EAPI | `ids=[id], level, encodeType=flac` | `songPlayUrlRaw` | ✅ | ✅ **通过**（出真 mp3；游客请求 `exhigh` → 下发 `standard`） |
| B2 | 播放 URL（备） | `POST /weapi/song/enhance/player/url` | WEAPI | `ids=[id], br=320000` | `songPlayUrlWeapiRaw` | ✅ | ✅ **通过**（协议通） |
| B3 | 301 预热重试 | B1/B2 + `ensureWeapiSession(force:)` | — | body `code==301` 预热后重试一次 | `_retryOnceOnLoginRequired` | ✅ | 🟡 **已实现**，未遇 301 实测 |
| B4 | 音质 level | B1 | — | `standard/higher/exhigh/lossless/hires/jyeffect/sky/jymaster` | 传参支持 | ✅ | ✅ **通过**（游客态三档全被降到 `standard`；高音质需 VIP 才能验） |
| B5 | 试听识别 | 解析 `freeTrialInfo` | — | — | `isPreviewClip` | ✅ | ✅ **通过**（付费曲 `fee=1` + `preview=true`，720KB≈45s） |
| B6 | 无版权 | 解析 `code=404` / `cannotListenReason` | — | — | `mapNeteasePlayFailure` | ✅ | ✅ **通过** |

**level 对照**（接 `AppQuality`）：standard→standard · hq→exhigh · sq→lossless · hiRes→hires；扩展档 jyeffect/sky/jymaster 挂网易侧。

---

### C. 歌词

| # | 用途 | 端点 | 通道 | 关键参数 | 本仓方法 | Neri | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| C1 | 歌词（主） | `POST /eapi/song/lyric/v1` | EAPI | `id, cp=false, lv=0, tv=1, rv=0, yv=1, ytv=1, yrv=0` | `songLyricRaw` | ✅ | ✅ **通过**（lrc/tlyric） |
| C2 | 歌词（备） | `GET /api/song/lyric` | GET | `id, lv=-1, tv=-1, rv=-1, yv=-1, ytv=-1, yrv=-1` | `songLyricPlainRaw` | ✅ SearchApi | ✅ **通过**（lrc=704；与 C1 的 1014 不等长，属 `lv` 档差异） |
| C3 | 字段 | 解析 `lrc/yrc/tlyric/romalrc/ytlrc` | — | — | `parseProbeLyric` | ✅ | ✅ lrc/tlyric 有值；YRC 视曲目 |

---

### D. 曲目 / 详情页

| # | 用途 | 端点 | 通道 | 关键参数 | 本仓方法 | Neri | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| D1 | 单曲详情 | `POST /weapi/v3/song/detail` | WEAPI | `c=[{"id":…}], ids=[…]` | `songDetailRaw` | ✅ | ✅ **通过** |
| D2 | 歌单详情 | `POST /api/v6/playlist/detail` | PLAIN | `id, n=100000, s=8` | `playlistDetailRaw` | ✅ | ✅ **通过**（`trackCount=1268`，但 `tracks` 只回 **1000**，见 §七 上限） |
| D3 | 专辑详情 | `POST /weapi/v1/album/{id}` | WEAPI @interface | `n, s` | `albumDetailRaw` | ✅ | ✅ **通过**（张悬《神的游戏》9 首） |
| D4 | 歌手头部信息 | `POST /api/artist/head/info/get` | PLAIN | `id` | `artistDetailRaw` | ✅ | ✅ **通过**（周杰伦 568 曲 / 44 专） |
| D5 | 歌手动态 | `POST /api/artist/detail/dynamic` | PLAIN | `id` | `artistDynamicRaw` | ✅ | ✅ **通过**（followed / concert / videoNum） |
| D6 | 歌手热门/歌曲 | `POST /api/v1/artist/songs` | PLAIN | `id, private_cloud=true, work_type=1, order=hot, offset, limit` | `artistSongsRaw` | ✅ | ✅ **通过** |
| D7 | 歌手专辑 | `POST /api/artist/albums/{id}` | PLAIN | `limit, offset, total=true` | `artistAlbumsRaw` | ✅ | ✅ **通过** |
| D8 | 相关歌单 | `GET /music.163.com/playlist?id=` HTML 抓取 | GET | — | ❌ | ✅ | ❌ 未实现（可选） |
| D9 | 雷达歌单元数据 | `POST /api/playlist/detail` | PLAIN | `id,n,s,uiPlaylistType=MGC` | `radarPlaylistMetaRaw`（见 G9） | ✅ | ✅ **通过**（与 G9 同口，已实现） |

---

### E. 登录 / 账号

| # | 用途 | 端点 | 通道 | 关键参数 | 本仓方法 | Neri | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| E1 | 当前账号 | `POST /weapi/w/nuser/account/get` | WEAPI | `{}` / `noCheckToken=true, csrf_token?` | `accountRaw` · `mapNeteaseAccount` | ✅ | ✅ **通过**（游客 200、无 profile） |
| E2 | 扫码 unikey | `POST /weapi/login/qrcode/unikey` | WEAPI | `type=1, noCheckToken=true` | `createQrSession` | ✅ | ✅ **通过**（拿到 unikey + chainId + scanlogin URL） |
| E3 | 扫码轮询 | `POST /weapi/login/qrcode/client/login` | WEAPI | `type=1, noCheckToken=true, key, ydDeviceToken` + 头 `x-loginmethod: QrCode` · `x-login-chain-id: <chainId>` | `pollQrLogin` | ✅ | ✅ **通过**（2026-09-24 实机扫码：801→802→803，`refreshToken` 162 字节，确认登录 `hasLogin=true`） |
| E4 | 手机+密码 | `POST /eapi/w/login/cellphone` | EAPI | `phone, countrycode, remember, password=md5, type=1` | `loginByPhoneRaw` | ✅ | 🟡 探针已覆盖（`--phone --password`），待凭据 |
| E5 | 短信登录 | 同 E4 | EAPI | + `captcha` | `loginByCaptchaRaw` | ✅ | 🟡 探针已覆盖（`--captcha`），待凭据 |
| E6 | 发验证码 | `POST /weapi/sms/captcha/sent` | WEAPI @interface | `cellphone, ctcode` | `sendSmsCaptchaRaw` | ✅ | 🟡 探针已覆盖（`--send-sms`），待凭据 |
| E7 | 验验证码 | `POST /weapi/sms/captcha/verify` | WEAPI @interface | `cellphone, captcha, ctcode` | `verifySmsCaptchaRaw` | ✅ | 🟡 探针已覆盖（`--verify-sms`），待凭据 |
| E8 | 网页 WebView 登录 | `music.163.com/m/login` 或首页 | WebView | UA 按屏宽 | ❌ | ✅ | ❌ 未实现（当前用扫码替代） |
| E9 | yd 设备指纹 | 页面 `createNEFingerprint` | WebView | `yd_token` / `sDeviceId` | ❌ | ✅ | ✅ **结论已定：可省略**。2026-09-24 实机扫码 `ydDeviceToken` 发空串即走到 803 → **不实现** WebView 指纹（E9 保持未实现，无阻塞） |

**扫码状态码**：800 生成二维码/过期 · 801 待扫 · 802 待确认 · **803 成功**（`x-refresh-token` → `MUSIC_U`）。

**实机扫码记录（2026-09-24）**：801 `等待扫码` → 802 `授权中` → 803 `授权登陆成功`；
`x-refresh-token` 长度 162；确认登录 `hasLogin=true`；随后 `account/get` 返回
`userId=1593114455 / nickname=MZHENGHF`；最终 cookie 含 `MUSIC_U`（登录凭据）、`__csrf`、`NMTID`、
`MUSIC_A_T/R_T/R_U` 等。**全程 `ydDeviceToken` 发空串**（见 E9）。

**扫码链路的三个关键点**（对齐 Neri `NeteaseQrLoginClient`，本仓已实现）：

1. **二维码内容不是裸 unikey**，是 URL：
   `https://music.163.com/st/platform/scanlogin?codekey={key}&chainId={chainId}&hdw_device=web&hdw_appid=web&hitExp=1`
   （`NeteaseClient.buildScanLoginUrl`）
2. **chainId** 格式 `v1_{sDeviceId|unknown-N}_web_login_{timestampMs}`，轮询时必须放进
   `x-login-chain-id` 头，并同时带 `x-loginmethod: QrCode`（`buildLoginChainId`）。
3. **扫码链路用桌面 UA**（Chrome/Edg on macOS）+ `x-os: web`、`x-channelsource: undefined`、
   `nm-gcore-status: 1`，且**不做 weapi 预热、不带 csrf**（`usePersistedCookies: false`）。

**803 后的登录态确认**（`NeteaseClient.confirmQrLogin`）：先直接 `account/get` 验账号；
失败则把响应头 `x-refresh-token` 当 `MUSIC_U` 合并后再验一次；仍失败则保留该凭据（尽力而为）。

---

### F. 我喜欢 / 用户歌单

| # | 用途 | 端点 | 通道 | 关键参数 | 本仓方法 | Neri | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| F1 | 用户歌单列表 | `POST /weapi/user/playlist` | WEAPI | `uid, offset, limit, includeVideo=true` | `userPlaylistsRaw` | ✅ | ✅ **通过**（登录态 21 单）；**已接产品**（`UserPlaylistReadSource`，2026-09-25） |
| F2 | 我喜欢 id | 解析 F1：`specialType==5` 打 `isDefault`；`subscribed==true` 判收藏 | — | — | `parseProbeUserPlaylists`（探针）/ `mapNeteaseUserPlaylists`（产品） | ✅ | ✅ **通过**（`likedPlaylistId=2412580948`，名 `MZHENGHF喜欢的音乐`） |
| F3 | 喜欢歌曲 id 列表 | `POST /weapi/song/like/get` | WEAPI | `uid` | `likedSongIdsRaw` | ✅ | ✅ **通过**（`ids` 字段名正确，**1009 首**） |
| F4 | 喜欢 / 取消喜欢 | `POST /weapi/song/like` | WEAPI | `trackId, like=true/false, time?` | `likeSongRaw` | ✅ | 🔒 探针已覆盖（`--write --like true|false`），**默认不发**，待真机跑一次 |
| F5 | 歌单加曲 | `POST /weapi/playlist/manipulate/tracks` | WEAPI | `op=add, pid, id, tracks=csv, trackIds=json, imme=true` | `addSongsToPlaylistRaw` | ✅ | 🔒 探针已覆盖（`--write --playlist`），**默认不发** |
| F5b | 歌单删曲 | 同 F5 端点 | WEAPI | `op=del`，其余同 F5 | `removeSongsFromPlaylistRaw` | ✅ | 🔒 探针已覆盖（`--write --playlist --id --del`，2026-09-26 补），**默认不发**，待真机跑一次 |
| F6 | 收藏专辑 | `POST /eapi/mine/rn/resource/list` | EAPI @interface3 | `userId, offset, limit, pageType=3, needRcmd=0, isVistor=false, includeStarPodcast=true` | `userAlbumsRaw` | ✅ | ⚠️ **返回 `count: 0`**：协议通（`code` 正常），但该账号无收藏专辑 → **空/非空两种形态都未验证**；探针已补「count + 首条键名」双行输出（2026-09-26），收藏一张专辑后重跑即可复核 |
| F7 | 我喜欢回写同步 | F2–F5 组合 | — | 本地收藏 → 云端 | ❌ | ✅ | ❌ 二期产品层 |
| F8 | 收藏歌单 / 关注歌手 | （Neri 资料库） | WEAPI | — | ❌ | ✅ | ❌ 未实现 |

**F 组登录态跑法（2026-09-24 起）**：`--suite login,like --qr` 一次扫码即带登录态跑 F1/F2/F3/F6，
uid 自动从 `account/get` 取（免手工传）。其中 **F3 只回 id 列表**，探针额外用 `songDetailRaw` 取前 5 首
验证「id → 详情」链路（`[F3b]`）；**F6 输出两行**——`_summary` 的 `count` 与 `_firstItemKeys` 的首条
键名（`count=0` 时会直接提示「非空形态仍未验证」）。

**写口（F4 / F5 / F5b）跑法（2026-09-26 补 F5b）**：写操作会**真改账号**，故默认不发，必须显式 `--write`。
三条命令一一对应三个写口，跑完请把结果回填上表「状态」列：

```bash
# F4 喜欢 / 取消喜欢（两条都跑才算双向实测）
dart run tool/probe_netease_api.dart --suite like --uid <uid> --write --like true  --id <songId>
dart run tool/probe_netease_api.dart --suite like --uid <uid> --write --like false --id <songId>
# F5 歌单加曲（先记下 pid 与 songId，方便下一行删掉）
dart run tool/probe_netease_api.dart --suite like --uid <uid> --write --playlist <pid> --id <songId>
# F5b 歌单删曲（同端点换 op=del；漏传 --playlist 会直接提示 skip）
dart run tool/probe_netease_api.dart --suite like --uid <uid> --write --playlist <pid> --id <songId> --del
```

判定：输出 `code=200` 即协议通；F4 还要在 App/网页确认该曲的喜欢态真的翻转（`like=false` 后
F3 的 id 列表里应不再有它），F5/F5b 要在歌单里确认曲目真的增/减。**只有这三项实测通过后**，
才能把产品层「喜欢」动作从「只写本地」改为**同时回写网易曲库**（当前 `Like` 对非酷狗源仅落本地）。

**F 组实测记录（2026-09-24 · uid 1593114455）**

| 项 | 结果 |
| --- | --- |
| F1 | `code=200, count=21`；首单 `MZHENGHF喜欢的音乐` |
| F2 | **「我喜欢」歌单 id = `2412580948`**（`specialType==5` 命中，非名字匹配） |
| F3 | `code=200, count=1009`；顶层字段名就是 **`ids`**（纯数字数组，`_summary` 的 `first` 为 null 属正常） |
| F3b | 「我喜欢」→ 详情链路通：前 5 首取到 `id=65536 / name=爱情转移 / ar=[{id:2116, name:陈奕迅}]` |
| F6 | `count=0`（该账号无收藏专辑，形态未验证） |

**`song/detail` 字段名已确认（F3b 实测）**：详情走**新字段结构**，不是旧版 `artists`/`album`：

- 歌手 = **`ar`**（数组，元素含 `id` / `name` / `tns` / `alias`）
- 专辑 = **`al`**（对象，封面在 **`al.picUrl`**，**不是**顶层 `picUrl` —— 探针 `_firstItemKeys` 只取顶层故显示 `cover: null`，非无封面）
- 时长 = **`dt`**（毫秒）；其余：`fee` / `noCopyrightRcmd` / `mv` / `publishTime` / `alia`(别名) / `t`(音轨号)
- 注意：`F3b` 里 `artist: [{id:2116,…}]` 正是 `ar` 命中，说明**统一 mapper 必须读 `ar`/`al`**

**产品层接线（2026-09-24）**：`NeteaseSource.likedTracks()`（能力接口 `UserLibrarySource`）=
F3 `likedSongIdsRaw` → `mapNeteaseLikedSongIds` → 按 **100 id/批** `songDetailRaw` → `mapNeteaseSongDetails`。
未登录抛 `LoginRequired`（F3 未登录会回 `code=301`）。UI 侧见 [多音源接入方案.md](多音源接入方案.md) §11.1。

---

### G. 推荐 / 发现（二期产品用）

| # | 用途 | 端点 | 通道 | 关键参数 | 本仓方法 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| G1 | 个性推荐歌单 | `POST /weapi/personalized/playlist` | WEAPI | `limit` | `personalizedPlaylistsRaw` | ✅ **通过**（游客 5 单；已接「为你推荐」页推荐歌单） |
| G2 | 每日推荐歌单 | `POST /weapi/v1/discovery/recommend/resource` | WEAPI | `{}` | `dailyRecommendResourceRaw` | ⚠️ **301 需登录**（预期） |
| G3 | 每日推荐歌曲 | `POST /weapi/v3/discovery/recommend/songs` | WEAPI | `afresh=true/false` | `dailyRecommendSongsRaw` | ✅ **通过**（游客 24 曲） |
| G4 | 私人 FM | `POST /weapi/v1/radio/get` | WEAPI | `{}` | `personalFmRaw` | ✅ **通过**（出 1 首） |
| G5 | 新歌推荐 | `POST /weapi/personalized/newsong` | WEAPI | `type=recommend, limit, areaId=0` | `personalizedNewSongsRaw` | ✅ **通过**（已接「新歌速递」Tab） |
| G6 | 热门/分类歌单 | `POST /weapi/playlist/list` | WEAPI | `cat, order=hot, limit, offset, total=true` | `topPlaylistsRaw` | ✅ **通过**（已接「歌单」Tab；`cat` 传中文标签名） |
| G7a | 精品歌单 | `POST /weapi/playlist/highquality/list` | WEAPI | `cat, limit, lasttime, total=true` | `highQualityPlaylistsRaw` | ✅ **通过**（已接「为你推荐」页编辑精选） |
| G7b | 精品标签 | `POST /api/playlist/highquality/tags` | **WEAPI 加密 + 非 weapi 路径** | `{}` | `highQualityTagsRaw` / `callWeApiAt` | ✅ **通过**（结构已解析：`tags[].name` 一级扁平，**无 `son[]` 二级**） |
| G9 | 雷达/官单元数据 | `POST /api/playlist/detail` | PLAIN | `id, n=1, s=0, uiPlaylistType=MGC` | `radarPlaylistMetaRaw` | ✅ **通过**（时光雷达 35 曲） |
| G8 | 评论 / 热搜 | （Netease 包未覆盖） | — | — | ❌ | ❌ 待自研 |
| G10 | 官方榜单曲目 | 复用 `playlistDetailRaw`（飙升 19723756 / 新歌 3779629 / 热歌 3778678） | PLAIN | id | ✅ | ✅ **通过**（100 / 100 / 200 首） |

**G 组备注**：G3/G4 游客也有数据（与 Neri「需登录」标注略有出入，以实测为准）；G2 是唯一强制登录的推荐口。

---

## 四、失败码映射（已实现 `mapNeteaseCode` / `mapNeteasePlayFailure`）

| 业务现象 | 码 / 字段 | 统一错误 |
| --- | --- | --- |
| 需登录 / 会话失效 | body `code==301` | `LoginRequired` |
| 扫码中间态 | 800 / 801 / 802 | `LoginRequired`（探针另报） |
| 扫码成功 | 803 | —（写 Cookie） |
| 无版权 / 不可播 | 曲目 `code=404` 或无 `url`；`cannotListenReason=1`；`fee>0` | `NoPermission` |
| 试听 | `freeTrialInfo` 非空 | `isPreviewClip=true` |
| 风控 / 限频 | `-460` / `-462` / 405 | `RateLimited` |
| 未找到 | 404 | `NotFound` |
| 解析失败 | 非 JSON / 未知 code | `UpstreamChanged` |

**与酷狗对照**：酷狗 **没有 301**；风控是 `errcode=20028`，勿混用。

---

## 五、状态汇总（截至 2026-09-24 · 第二轮实测后）

| 类 | ✅ 通过 | 🟡 就绪待测 | 🔒 写未触发 | ❌ 未实现 |
| --- | --- | --- | --- | --- |
| 搜索 | 2（A1/A1c） | 1（A1b 实测 50000005） | — | — |
| 播放 | 5（B1/B2/B4/B5/B6） | 1（B3 未遇 301） | — | — |
| 歌词 | 3（C1/C2/C3） | — | — | — |
| 详情 | 8（D1–D7、D9） | — | — | 1（D8） |
| 登录账号 | 3（E1/E2/E3） | 4（E4–E7 待凭据） | — | 1（E8；E9 已实测可省略，无需实现） |
| 我喜欢 | 3（F1/F2/F3） | 1（F6 返回 0，形态未验证） | 2（F4/F5） | 2（F7/F8） |
| 推荐发现 | 9（G1/G3–G7b/G9/G10） | — | — | 2（G2 需登录 / G8） |

> **本轮（游客态）实测结论**：
> - **A1c 四类搜索全通**（单曲/专辑/歌手/歌单各 10 条）
> - **B4 游客态统一降档**：请求 `standard/exhigh/lossless` 三档，下发**全是 `standard`** → 高音质必须 VIP，属权限不属接口问题
> - **付费曲**：`fee=1` + `freeTrialInfo` → `preview=true`，720KB ≈ 45s（B5 试听识别实测生效）
> - **C2 明文歌词口可用**（lrc=704；与 C1 的 1014 不等长，属 `lv` 档差异）
> - **D5/D7/G10 补测通过**；**E3 扫码轮询通**（未扫码回 `801 等待扫码`）
> - **E2/E3 扫码登录实机走通 803**（2026-09-24）：`ydDeviceToken` 发空串即可 → **E9 WebView 指纹确认可省略**；
>   `x-refresh-token`（162）合并成 `MUSIC_U` 后 `account/get` 出账号（见 E 组「实机扫码记录」）
> - **顺带修掉一个真问题**：首页 GET 常无 Set-Cookie，`__csrf` 恒空，而预热判断写成
>   `_preheated && csrf.isNotEmpty` → **每次 weapi 都白跑一次首页**（一轮探针多 14 次请求）。
>   已改为只判 `_preheated`（`ensureWeapiSession({force})`）。

**一句话**：游客态接口**已全部实测通过**（搜索四类 / 播放 / 歌词 / 详情 / 推荐发现 / 扫码 unikey），
**扫码登录（E2/E3）也已实机走通 803 并拿到账号**；
只剩密码/短信登录（E4–E7 需凭据）、**F 组（已有 `MUSIC_U`，可开跑）**，以及评论/热搜、WebView 登录这类未实现项。

---

## 六、探针用法

```bash
# 默认（search）：搜索 → 单曲详情 → 播放 URL → 歌词(A3) + 明文歌词(C2)
dart run tool/probe_netease_api.dart --keyword love

# A1c 分类搜索（1 单曲 / 10 专辑 / 100 歌手 / 1000 歌单）
dart run tool/probe_netease_api.dart --types 1,10,100,1000

# B4 音质档位扫描 / 指定单曲
dart run tool/probe_netease_api.dart --levels standard,exhigh,lossless,hires --id 1234567

# A1b cloudsearch 对照（实测 50000005，仅排查）
dart run tool/probe_netease_api.dart --suite cloudsearch

# 登录（E1/E2/E3 游客可跑；E4–E7 需凭据，默认不发）
dart run tool/probe_netease_api.dart --suite login
dart run tool/probe_netease_api.dart --suite login --phone 13800000000 --password xxx
dart run tool/probe_netease_api.dart --suite login --phone 13800000000 --captcha 1234
dart run tool/probe_netease_api.dart --suite login --phone 13800000000 --send-sms
dart run tool/probe_netease_api.dart --suite login --phone 13800000000 --captcha 1234 --verify-sms

# E2/E3 交互式扫码登录：终端打印二维码 → 每 2s 轮询到 803（✅ 2026-09-24 实机走通，默认不带 yd_token）
dart run tool/probe_netease_api.dart --suite login --qr
dart run tool/probe_netease_api.dart --suite login --qr --qr-timeout 180
dart run tool/probe_netease_api.dart --suite login --qr --yd-token <token>   # 实测可省略，保留仅为对照

# 我喜欢（F1/F2/F3/F6 需登录或 --uid）
dart run tool/probe_netease_api.dart --suite like --uid <你的uid>

# F 组登录态推荐跑法：同进程「扫码登录 → 跑我喜欢」，uid 自动取，免手工传 MUSIC_U
dart run tool/probe_netease_api.dart --suite login,like --qr

# F4 / F5 写操作：必须 --write 显式打开（会真改账号）
dart run tool/probe_netease_api.dart --suite like --uid <uid> --write --like true --id 1234567
dart run tool/probe_netease_api.dart --suite like --uid <uid> --write --playlist 24381616 --id 1234567

# 详情（D1–D7）+ 推荐发现（G1–G10）
dart run tool/probe_netease_api.dart --suite detail --playlist 24381616 --album 32311 --artist 6452
dart run tool/probe_netease_api.dart --suite discover
```

> 多个 suite 可逗号并列：`--suite login,like,detail,discover`。
> **suite 不再隐含 search**：默认只跑 `search`，要跑什么就写什么（减少请求数，避开风控）。

---

## 七、已知坑

| 坑 | 说明 |
| --- | --- |
| 搜索口 | `cloudsearch/get/web` → `50000005`；默认 `/weapi/search/get` |
| 搜索单曲封面 | 旧口 `search/get` 的 `album` **只有 `picId`，无 `picUrl`** → 需按官方算法自行拼 CDN 直链（见 §三 A1c）；专辑/歌单/歌手搜索不受影响 |
| search type | 单曲 1 / 专辑 10 / 歌手 100 / **歌单 1000** |
| weapi JSON | 字符串字段用 `"1"` 不要裸数字时，与 Neri 对齐更稳 |
| RSA | **无填充** modPow，不是 PKCS1 |
| eapi 签名 | path 去 `/eapi`→`/api` 再拼盐 MD5；params **大写 hex** |
| 表单 | `application/x-www-form-urlencoded` UTF-8，勿用 multipart |
| 首页 GET | 常 **无 Set-Cookie**，`__csrf` 可能一直空 → 预热判断别带上 `csrf.isNotEmpty`，否则每次 weapi 都白跑一次首页（已修） |
| 无版权曲 | 协议成功 + 业务 `404`，不算接口失败 |
| 游客降档 | 未登录请求 `exhigh/lossless` 一律下发 `standard`；VIP 曲目还要叠加 `fee=1` 试听片段 |
| 歌单上限 | `/api/v6/playlist/detail` 的 `tracks` **最多回 1000 首**（实测 1268 首的歌单只回 1000），更多需分页/换口 |
| 播放 headers | 下载封面/音频时 Referer 按 CDN（163 vs kugou） |

---

## 八、一页纸

> **已实测通过（游客态全覆盖）**：weapi/eapi 加密、搜索（`search/get`，含四类 `type`）、播放 URL、歌词（eapi + 明文）、单曲/歌单/专辑/歌手详情（含动态/专辑列表）、账号、推荐发现（G1/G3–G7b/G9/G10）。
> **已实测通过（登录态）**：**扫码登录 E2/E3 实机走通 801→802→803**，`x-refresh-token` → `MUSIC_U` 后 `account/get` 出账号；`ydDeviceToken` 发空串即可 → **E9 可省略**。
> **已实测通过（登录态 · 我喜欢）**：F1 21 单 / **F2「我喜欢」id=`2412580948`** / F3 1009 首（字段 `ids`）；F3b 详情链路通，确认详情字段是 **`ar` / `al`（封面 `al.picUrl`）/ `dt`**。**已接进产品**（`UserLibrarySource`，F3+A4 分批；F1 → `UserPlaylistReadSource`，见 §九）。
> **待凭据**：密码/短信登录（E4–E7）。
> **待复核**：F6 收藏专辑返回 `count: 0`（该账号无收藏专辑，非空形态未验证）。
> **待实现**：WebView 登录（E8）、我喜欢**回写**、收藏歌单/关注歌手、评论/热搜、相关歌单。
> 写方法已就绪但 **🔒 必须 `--write` 才发**。

---

## 九、阶段 B 适配层（2026-09-24 落地）

协议层收口后，网易云已接进「多音源」抽象（见 [多音源接入方案.md](多音源接入方案.md)）：

| 文件 | 职责 |
| --- | --- |
| `core/api/netease/netease_mappers.dart` | 响应 → 统一模型：搜索/歌单/每日推荐/FM → `Track`；`data[0]` → `PlayUrlResult`；`lrc/yrc/tlyric/romalrc` → `LyricPayload`；F3 `ids` → `List<int>`、A4 `songs[]` → `[Track]`；D 详情页（2026-09-25）：D2 `mapNeteasePlaylistDetail` → `(PlaylistBrief, [Track])`、D3 `mapNeteaseAlbumDetail` → `AlbumDetail`、D4 `mapNeteaseArtistDetail` → `ArtistDetail`、D6 `mapNeteaseArtistSongs` → `ArtistSongsPage`；F1 用户歌单（2026-09-25）：`mapNeteaseUserPlaylists` → `UserPlaylistsPage{created, collected, more}`（`subscribed==true` = 收藏，`specialType==5` 归自建并打 `isDefault`）；G 发现（2026-09-25）：G5 `mapNeteasePersonalizedNewSongs` → `[Track]`（`result[].song` 内层解包）、G6 `mapNeteaseTopPlaylists` → `[PlaylistBrief]`（复用搜索歌单 brief 映射）、G7b `mapNeteasePlaylistTags` → `[PlaylistTagGroup]`（单组扁平 + 前置「全部」）；推荐聚合（2026-09-25）：G1/G7a **复用** `mapNeteaseTopPlaylists`（它同时吃 `result[]` 与 `playlists[]`） |
| `data/sources/netease/netease_source.dart` | `NeteaseSource`：`MusicSource` + `DailyRecommendSource`(G3) / `RankSource`(G10) / `PersonalFmSource`(G4) / `PlaylistCatalogSource`(G6+G7b) / `NewSongFeedSource`(G5) / `PlaylistDetailSource`(D2，歌单/榜单共用) / `AlbumDetailSource`(D3) / `ArtistDetailSource`(D4+D6) / `UserPlaylistWriteSource`(F5) / `UserPlaylistReadSource`(F1) / `DeviceLoginSource`(E2/E3) / `UserLibrarySource`(F3+A4) / `RecommendFeedSource`(G1+G7a) |
| `data/storage/netease_auth_store.dart` | 登录态落盘：`netease.auth.v1` 存 `{cookies,savedAt}`，启动时 `restoreInto` 灌回客户端 |
| `data/sources/sources.dart` | 启动装配：`registerDefaultMusicSources()` 注册酷狗 + 网易云 |

**适配层口径**

- **播放**：单次请求即可。网易服务端按权限下发最高可用档（游客态一律 `standard`），故**不做**酷狗式候选降档循环。
- **`level` → 抽象档**：`higher`(192k) 归 `standard`（抽象档无对应，保守展示）；`jyeffect`/`sky` → `sq`；`jymaster` → `hiRes`。
- **歌词**：优先 `yrc`（逐字，格式 `[行起点,行时长](字偏移,字时长,0)字…`，**圆括号**，与酷狗 KRC 的 `<>` 不同）；译文/音译取 `tlyric`/`romalrc` 并按时间戳对齐主行。
- **封面**：详情/云搜索走 `al.picUrl`；**旧搜索口 `search/get` 的单曲只给 `album.picId`**，
  由 `NeteaseCrypto.picUrl()` 按官方算法拼 CDN 直链（`mapNeteaseSong` 内回退，见 §三 A1c）。
- **榜单**：网易无「榜单列表」接口，官方榜是固定三个 id（飙升 `19723756` / 新歌 `3779629` / 热歌 `3778678`），曲目复用歌单详情。
  `RankSource.rankBoards()` 统一返回 `PlaylistBrief`（`isRank: true` / `platform: netease` /
  `rankTypeName: '网易官方榜'`），名与封面由 `playlistDetailRaw(id, n:1, s:0)` 并行补齐，失败退回内置名。
  **榜单列表页已按源分发（2026-09-25）**：`/ranks` 只列「已启用且具备 `RankSource`」的源，单源取数、
  双源出切源栏，非酷狗源点击带 `?src=netease` 进详情。
- **每日推荐（G3）**：网易无酷狗那套「公开兜底 / 未登录」分叉，G3 本身即个性化歌单（游客 24 曲），
  故映射为 `DailyRecommendResult(tracks, personalized: true)`（`needLogin`/`error` 恒空）。
  **日推页已按源分发（2026-09-25）**：`DailyRecommendSource` 契约由 `dailyRecommendedSongs(): List<Track>`
  升级为 `dailyRecommend(): DailyRecommendResult`（模型上移 `core/models/daily_recommend.dart`），
  酷狗透传 `/top/ip` 链路、网易走 G3，UI 不按源分支；`/daily-recommend` 单源取数、双源出切源栏。
- **详情页**（2026-09-25 接线）：搜索结果点歌单/专辑/歌手 → 详情路由带 `?src=netease` 按源分发
  （不带 `src` 默认酷狗，深链兼容）。歌单/榜单共用 D2 `playlist/detail`（`PlaylistDetailSource`，
  一次拿 brief + tracks，上限 1000 首）；专辑 D3 读 `album` 头 + **顶层** `songs[]`；歌手 D4 读
  `data.artist` 头部 + D6 `artist/songs` 分页（网易是 `offset=(page-1)*pageSize` + `more` → hasMore，
  与酷狗 page 分页的差异在 `ArtistDetailPage._fetchSongsPage` 分流处收口）。统一模型
  `AlbumDetail`/`ArtistDetail`/`ArtistSongsPage` 上移至 `core/models/catalog_models.dart`，能力契约
  为 `PlaylistDetailSource`/`AlbumDetailSource`/`ArtistDetailSource`（`capabilities.dart`）。
- **「我喜欢」**：`UserLibrarySource.likedTracks()` = **F3 + A4 两跳**——先 `song/like/get` 取顶层 `ids`
  （纯数字数组），再按 **100 id/批** 调 `song/detail` 拿详情（实测形态安全，1009 首共 11 批）；
  详情走新字段结构，**必须读 `ar` / `al.picUrl` / `dt`**（见 §三 F 组）。未登录抛 `LoginRequired`。
  网易侧无「取消喜欢」写口实测（F4 探针覆盖但默认不发），故 UI 红心按钮不对网易曲目开放。
- **FM**：`radio/get` 一次一批（通常 1 首），`remain` 不生效；`like` 走 F4（已实测），`skip`/`trash` **无实测端点**，暂只做本地行为。
  **FM 页已按源分发（2026-09-25）**：`FmController` 经 `registry.capability<PersonalFmSource>(会话源)`
  取数，网易**一次 1 首**的差异在控制器层循环拼装到 `kFmSeedBatch = 10`（`kFmSeedBatchMaxCalls = 10`
  限流），酷狗一次一整批不进循环；`FmSession.source` 落盘、切源 = 结束会话并以新源重开（不混源），
  `/fm` 单源取数、双源出切源栏（`SourceFilterBar(showAll: false)`）。
  **档位轴按能力显隐**：`HeartRadioSource` 是酷狗独有，网易无此能力 → FM 页/播放页 FM 面板/发现页
  快捷入口的模式胶囊与歌池轴（Alpha/Beta/Gamma）整块不挂，来源角标写「来源：网易云私人 FM」。
  **失败不回落关键词池**：网易无本地兜底，`SourceFailure.message`（如 `LoginRequired`）直接进
  `gatewayError`，不用公开搜索冒充个性化流。
  **喜欢只写本地**：`like()` 对非酷狗源走 `likesProvider.likeLocal()`，**不写网易曲库**（F4 落盘写口
  未实测，且不能把网易曲目写进酷狗歌单污染曲库），留待 E 阶段实测后再接。
- **用户歌单（F1）**：`UserPlaylistReadSource.userPlaylists()` 走 `userPlaylistsRaw(uid, limit: 1000)`
  （一次取全量，歌单数极少过千），映射 `mapNeteaseUserPlaylists` → `UserPlaylistsPage{created, collected, more}`。
  分流口径对齐 NeriPlayer：`subscribed == true` = **收藏**，其余（含 `specialType == 5` 的「我喜欢的音乐」）
  归**自建**，默认单额外打 `isDefault` 供「我的」页出红心封面。「我喜欢」计数直接取该默认单的 `trackCount`，
  **不再额外发一次曲目请求**；uid 取自 `currentAccount()`，未登录抛 `LoginRequired`。
  **已接进产品（2026-09-25）**：「我的」页按**账号源**切「酷狗 `userCollectionsProvider` / 网易
  `neteaseCollectionsController`」两份数据，账号身份由 `features/profile/source_account.dart` 按源取
  （酷狗 `AuthController`、网易 `NeteaseLoginController`），见 [多音源接入方案.md](多音源接入方案.md) §11.1。
- **探索发现（G5/G6/G7b，2026-09-25 接线）**：网易侧只接「歌单 / 排行榜 / 新歌速递」三个 Tab，
  **新碟上架与歌手不接**（无 `/weapi/album/new`、`/weapi/artist/list` 实测口径）——非酷狗源这两个 Tab
  出「暂不支持」空态，不建假入口。口径：
  - 歌单 = `PlaylistCatalogSource`：标签走 G7b `highQualityTagsRaw()` → `mapNeteasePlaylistTags`
    （**网易标签是一级扁平**，拍成单组并把「全部」前置）；分类歌单走 G6 `topPlaylistsRaw(cat:)` →
    `mapNeteaseTopPlaylists`。**`cat` 收的是中文标签名**（实测 `cat=华语`），故 `PlaylistTag.id`
    直接用标签名、UI 原样回传；UI 传空串时实现回退 `cat=全部`。
  - 新歌速递 = `NewSongFeedSource.newSongs()`：G5 `personalizedNewSongsRaw(limit:)`。**注意 `result[]`
    节点把曲目嵌在 `song` 里**，`mapNeteaseSong` 已做内层解包；网易无地区 chips，UI 只渲染曲目列表。
  - 排行榜 = 复用 `RankSource`（G10 固定三榜），见上「榜单」条。
  - `PlaylistTagGroup`/`PlaylistTag` 模型上移至 `core/models/catalog_models.dart`（原 data 层
    `DiscoveryTagGroup`/`DiscoveryTag` 改名），避免 core 能力契约反向依赖 data 层。
- **推荐聚合（G1/G7a，2026-09-25 接线）**：`RecommendFeedSource` 服务「为你推荐」页两块——
  `recommendPlaylists()` = G1 `personalizedPlaylistsRaw(limit:)`（个性推荐歌单，**无分类维度**，
  契约上的 `cat` **忽略**）；`editorialPlaylists()` = G7a `highQualityPlaylistsRaw(limit:)`（精品歌单）。
  两者响应节点形态一致（`result[]` / `playlists[]`），**复用同一 mapper** `mapNeteaseTopPlaylists`。
  同页「风格」板块在网易侧退化为**风格歌单**（`PlaylistCatalogSource`：G7b 标签 + G6 分类歌单，
  `cat` 传中文标签名），因为酷狗那套「风格歌曲流」无能力契约、网易也无对应口。
  首页「发现」的「推荐歌单」横排不新增契约，直接复用 `PlaylistCatalogSource.categoryPlaylists(cat: '')`。
  口径详见 [多音源接入方案.md](多音源接入方案.md) §11.1。
- **未实测项**：F4 取消喜欢 / F5b 歌单删曲（`op=del`）；`PersonalFmSource` 的 trash 上报（网易侧空实现）；
  **「最近播放」与「云盘」无对应对口**（客户端未实现，故不建 UI 入口）；新碟上架
  `/weapi/album/new`、歌手 `/weapi/artist/list`（发现页两 Tab 暂为酷狗限定）。
