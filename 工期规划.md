# 酷狗概念版 Flutter 客户端 — 工期规划

> 项目：`kugo`（仓库 `kugoFulutter`）
> 依据：`设计方案.md` + 已确认视觉稿 `docs/design/01~04`
> 文档版本：**v1.2 · M4 收口 + 酷狗二期排期**（已完成项以 ~~删除线~~ 标记）
> 工作量口径：按 **1 名全栈 Flutter 开发 · 每周 5 人日 · 有效编码约 6h/日** 估算；若多人并行，见 §6。
> **范围**：本排期只含**酷狗侧**产品能力；多音源抽象见 `多音源接入方案.md`。**网易云排期另出独立文档，不在此表。**

## 当前进度（快照）

| 里程碑 | 状态 | 说明 |
| --- | --- | --- |
| M0 脚手架 | **已完成** | 工程 / 主题 / 路由 / 队列状态机可跑 |
| M1 假数据 UI | **已完成** | 四屏 + 组件库 + MiniBar/Hero + 歌单详情 |
| M2 真源出声 | **已完成** | 搜索、播放、锁屏、杀进程恢复队列真机验收通过（可达网络） |
| M3 体验收口 | **已完成** | FM / 取色·动效 / 设置 / 我喜欢·历史 / 每日推荐 / 排行榜 / 音质·定时·缓存 / 歌曲详情+评论 |
| M4 可发布包 | **收口中** | 正式签名 Release APK；性能主观验收；analyze 零问题 |
| M5 酷狗音源抽象 | **待启动** | 多音源阶段 A：`MusicSource`/`KugouSource`/身份与库迁移（**不含网易**） |

代码位置：`app/`。质量基线：`flutter analyze` 零问题；测试套件见 `app/test/`。

> 网络注意：开发网对 `*.kugou.com` 可能「URL过滤」。真源验收需热点/白名单环境（见 `docs/api-notes.md`）。

---

## 1. 总览

| 项 | 内容 |
| --- | --- |
| 目标交付 | Android 可安装 APK：听歌闭环 MVP |
| 总工期（串行） | **约 8 周**（40 人日） |
| 总工期（双人并行关键路径） | **约 5.5–6 周** |
| 里程碑 | M0 脚手架 → M1 假数据 UI → M2 真源出声 → M3 体验收口 → M4 可发布包 |
| 明确不做（本规划内） | 插件、一起听、听歌识曲、云盘、iOS 签名上架、桌面端 |

```text
周次   1     2     3     4     5     6     7     8
      ┌─────┬─────┬─────┬─────┬─────┬─────┬─────┬─────┐
P0    │███░░│     │     │     │     │     │     │     │ 脚手架
P1    │░░███│████ │██   │     │     │     │     │     │ 假数据 UI（视觉稿落地）
P2    │     │     │░████│████ │██   │     │     │     │ 真源 + 播放链路
P3    │     │     │     │     │░████│████ │██   │     │ FM/榜单/歌词打磨
P4    │     │     │     │     │     │     │░████│██░░ │ 稳定、性能、打包
      └─────┴─────┴─────┴─────┴─────┴─────┴─────┴─────┘
里程碑  M0    M1----              M2----        M3----  M4
```

---

## 2. 里程碑定义（可验收）

### M0 · 工程可跑（第 1 周中）

- `flutter create` 成功，真机/模拟器 Debug 启动。
- 主题 Token、路由壳、Riverpod 根 Provider、Dio/Drift 骨架就位。
- CI 雏形：`analyze` + `test` 空套件可绿。

**验收**：冷启动进空白 Shell，底栏三 Tab 可切换。

### M1 · 视觉稿落地（第 3 周初）

- 四屏（首页 / 发现 / 我的 / 播放）按 `docs/design` 实现，**假数据驱动**。
- MiniBar、全屏播放手势（下拉关闭）、歌词静态滚动。
- Design Tokens 组件库：`TrackTile` / `PlaylistCard` / `SectionHeader` / `AsyncBody`。

**验收**：不连网可完整点播假队列，UI 与设计稿一致度可主观评审通过。

### M2 · 真源出声（第 5 周中）

- 登录或游客态、搜索、歌单/专辑/歌手详情。
- 播放地址解析 + `just_audio` + `audio_service`（锁屏/通知栏）。
- 队列 / 四种播放模式 / 音质降级 / 失败自动跳过。

**验收**：搜索任意可播歌曲 → 出声 → 切歌 → 锁屏可控制 → 杀进程重进恢复队列。

### M3 · 体验收口（第 7 周初）

- 每日推荐、排行榜、私人 FM。
- 歌词：滚动同步、翻译开关、偏移；封面取色渐变。
- 我喜欢 / 历史 / 设置（主题、音质默认、定时停止、缓存清理）。

**验收**：MVP 功能清单（§3）全部勾选，主路径无阻塞 Bug。

### M4 · 可发布包（第 8 周末）

- Release APK（签名）、崩溃日志开关、关于页与免责声明。
- 性能：冷启动、滚动、切歌卡顿达标（§5）。
- 文档：`README` 构建说明 + 已知限制。

**验收**：侧载安装包可用，演示脚本 10 分钟内走完主路径。

---

## 3. 一期功能范围（本工期锁定）

| 优先级 | 功能 | 里程碑 | 状态 |
| --- | --- | --- | --- |
| P0 | ~~启动、主题、底栏导航、MiniBar~~ | M1 | 完成 |
| P0 | ~~首页推荐（Hero + 每日推荐 + 推荐歌单）~~ | M1/M3 | 完成（首页已并入发现） |
| P0 | ~~发现：搜索、热搜、排行榜入口、分类~~ | M1/M2 | 完成 |
| P0 | ~~我的：用户卡、我喜欢、历史、设置入口~~ | M1/M3 | 完成 |
| P0 | ~~全屏播放 + 歌词滚动~~ | M1/M2 | 完成 |
| P0 | ~~播放引擎、队列、模式、锁屏控制~~ | M2 | 完成 |
| P0 | ~~登录（或游客降级）~~ | M2 | 完成：游客默认可用 + 真网关登录（失败不造假会话）+ token 注入 |
| P0 | ~~歌单详情~~ / 专辑 / 歌手详情 | M2 | 完成（真源优先 + mock 策略已收口为明确报错） |
| P0 | ~~我喜欢 / 播放历史 / 本地持久化~~ | M2/M3 | 完成：红心 + /likes + /history + 云端我喜欢；队列/历史 **Drift** |
| P1 | ~~每日推荐页、排行榜页、私人 FM~~ | M3 | 完成 |
| P1 | ~~封面取色、更多动效~~ | M3 | 完成：CoverPalette；Hero 切歌/MiniBar 文案淡入/进度缓动/FM 黑胶 |
| P1 | ~~音质切换、定时停止、缓存管理~~ | M3 | 完成：音质已接入 `/v5/url`；定时停止真实生效；缓存清理（设置内） |
| P1 | ~~歌曲详情（评论可后置）~~ | M3+ | 完成：`/song` 详情 + 评论列表（公开接口） |

## 后续增强（已做）

- 设置：清理图片缓存与播放历史
- 歌词：全屏歌词自动滚到当前行
- 搜索四 Tab（歌曲/歌单/专辑/歌手）、歌手 id 跳转修复
- 我的页统计真值、云端我喜欢/收藏/关注、Windows 桌面壳

| P2 | 分享、评论完整体验 | 规划外 | — |
| P2 | 听歌识曲、云盘、一起听、插件 | 不做 | — |

---

## 4. 分阶段任务拆解

### Phase 0 · 脚手架（第 1 周 · 5 人日）

| # | 任务 | 产出 | 人日 | 状态 |
| --- | --- | --- | --- | --- |
| ~~0.1~~ | ~~初始化 Flutter 工程、目录分层、lint、格式化~~ | `app/` 可跑 | 0.5 | 完成 |
| ~~0.2~~ | ~~Design Tokens（色/字/距/圆角）+ 主题框架~~ | `core/theme` | 1 | 完成 |
| ~~0.3~~ | ~~go_router 路由壳 + RootShell（底栏 + MiniBar）~~ | 导航可点 | 1 | 完成 |
| ~~0.4~~ | ~~Riverpod 根注入~~ | Provider 根 | 0.5 | 完成 |
| ~~0.5~~ | ~~Dio 封装 + 失败模型~~ | `core/api` | 1 | 完成 |
| 0.6 | ~~Drift 骨架 + 启动迁移~~ | `kugo_db` | 0.5 | 完成：队列/历史 SQLite；prefs 一次性迁移 |
| ~~0.7~~ | ~~假数据 Fixture 包~~ | `core/data/mock_data.dart` | 0.5 | 完成 |

**出口**：M0 验收通过（Drift 顺延）。

---

### Phase 1 · 假数据 UI（第 1 周末–第 3 周初 · 约 10 人日）

| # | 任务 | 依赖 | 人日 | 状态 |
| --- | --- | --- | --- | --- |
| ~~1.1~~ | ~~核心组件：TrackTile / PlaylistCard / SectionHeader / Badge / AsyncBody / GlassSurface~~ | 0.2 | 2 | 完成 |
| ~~1.2~~ | ~~首页四区（问候/搜索入口/Hero/每日推荐/推荐歌单）~~ | 1.1 | 2 | 完成 |
| ~~1.3~~ | ~~发现页（热搜 chips、排行榜卡、分类、新歌列表）~~ | 1.1 | 1.5 | 完成 |
| ~~1.4~~ | ~~我的页（用户卡、快捷宫格、设置列表）~~ | 1.1 | 1.5 | 完成 |
| ~~1.5~~ | ~~全屏播放：封面模式 + 进度 + 控制栏（规范图标）+ 队列 Sheet~~ | 1.1 | 1.5 | 完成 |
| ~~1.6~~ | ~~歌词组件（假 LRC 滚动、当前行高亮）~~ | 1.5 | 1 | 完成 |
| ~~1.7~~ | ~~MiniBar Hero 过渡 + 播放切换动画~~ | 1.5 | 0.5 | 完成 |
| ~~1.8~~ | ~~歌单详情 Sliver 头图 + 播放全部 + 序号列表~~ | 1.1 | 1 | 完成（原 1.8 走查并入实现） |

**出口**：M1 评审通过（视觉已对照 `docs/design` 落地）。

**并行建议**：UI（1.1–1.7）与 Data 模型/DTO 定义可两人拆分。

---

### Phase 2 · 真源 + 播放链路（第 3–5 周 · 约 12 人日）

| # | 任务 | 依赖 | 人日 | 状态 |
| --- | --- | --- | --- | --- |
| ~~2.1~~ | ~~接口调研 + 端点常量 + Dio 客户端 + 移动端公开 API 封装~~ | 0.5 | 1.5 | 完成（无网关签名，走 public CDN/wwwapi） |
| ~~2.2~~ | ~~Auth：登录流 / token 持久化 / 401 重登~~ | 2.1 | 1.5 | UI+本地会话完成；正式短信/扫码 API 待网关可达后接 |
| ~~2.3~~ | ~~Search + 热词 + 建议接入搜索页~~ | 2.1 | 1 | 完成 |
| ~~2.4~~ | ~~首页推荐 / 歌单分类 / 歌单详情真源数据~~ | 2.1 | 1.5 | 完成：歌单/推荐聚合真源；部分公开分类接口被拒（见 `docs/api-notes.md`） |
| ~~2.5~~ | ~~专辑 / 歌手详情~~ | 2.4 | 1 | 完成（真源优先 + mock 回退 + 路由/入口） |
| ~~2.6~~ | ~~播放地址解析（wwwapi + tracker 备用链）~~ | 2.1 | 1.5 | 完成 |
| ~~2.7~~ | ~~`AudioPlayerPort` + just_audio 实现~~ | — | 1.5 | 完成 |
| ~~2.8~~ | ~~`audio_service` 锁屏/通知栏/耳机键~~ | 2.7 | 1 | 完成 |
| ~~2.9~~ | ~~PlayerController：队列、模式、Intent(seq) 防竞态~~ | 2.6–2.8 | 1.5 | 完成 |
| ~~2.10~~ | ~~播放失败自动跳过 / 错误态~~ | 2.9 | 0.5 | 完成 |
| ~~2.11~~ | ~~队列与历史持久化，冷启动恢复~~ | 0.6, 2.9 | 1 | 完成：Drift SQLite（自动迁移旧 SharedPreferences） |
| ~~2.12~~ | ~~歌词接口 + LRC 解析 + 播放页同步预览~~ | 2.7 | 1.5 | 完成（全屏歌词滚动画廊可再抛光） |

**出口**：M2 代码链路齐备。当前开发网络对酷狗域名有「URL过滤/TLS 掐断」，真源需换网验证（见 `docs/api-notes.md`）。

**风险缓冲**：2.1/2.6 各预留 0.5 人日（签名与防盗链不确定性）。

---

### Phase 3 · 体验加深（第 5–7 周 · 约 8 人日）

| # | 任务 | 人日 | 状态 |
| --- | --- | --- | --- |
| ~~3.1~~ | ~~每日推荐页~~ | 1 | 完成：按日种子 + 榜单优先 + 主题热词填充 |
| ~~3.2~~ | ~~排行榜列表 + 详情~~ | 1 | 完成：列表页 + 发现页「全部」入口；详情复用歌单 |
| ~~3.3~~ | ~~私人 FM（唱片机式 UI + 动态队列）~~ | 2 | 完成：黑胶 UI、三池切换、红心/不喜欢、真源搜索歌池 |
| ~~3.4~~ | ~~封面取色 → 播放页/MiniBar 渐变~~ | 1 | 完成：CoverPalette + 播放页/MiniBar 跟随曲目 |
| ~~3.5~~ | ~~音质选择、定时停止、倍速（可选）~~ | 1 | 完成：音质已接入播放请求；定时停止；倍速本阶段不做 |
| ~~3.6~~ | ~~设置页完整：主题/播放/缓存/关于/免责~~ | 1 | 完成（音质、定时、歌词翻译、Wi-Fi 封面、账号、关于） |
| ~~3.7~~ | ~~我喜欢 / 历史列表交互完善~~ | 0.5 | 完成：本地红心 + /likes + /history |
| ~~3.8~~ | ~~空态/错误态/骨架统一~~ | 0.5 | 完成：AsyncBody + 网关拦截明确提示 |

**出口**：M3 功能清单勾完。

---

### Phase 4 · 稳定与发布（第 7–8 周 · 约 5 人日）

| # | 任务 | 人日 | 状态 |
| --- | --- | --- | --- |
| ~~4.1~~ | ~~单测：队列推进、seek 钳制、播放切换、LRC 解析~~ | 1 | 完成 |
| ~~4.2~~ | ~~Widget 测试扩展：MiniBar、TrackTile、播放按钮状态~~ | 0.5 | 完成 |
| ~~4.3~~ | ~~集成/冒烟：播队列 → 切歌 → 切换 → seek（mock）~~ | 1 | 完成 `play_smoke_test.dart` |
| ~~4.4~~ | ~~性能：列表滚动、启动耗时、内存~~ | 1 | 完成：MuMu 冷启动主观达标；滚动/切歌无卡死（见 `app/docs/release-signing.md`） |
| ~~4.5~~ | ~~Release APK + 正式签名~~ | 0.5 | 完成：`key.properties` + `upload-keystore.jks`（勿提交仓库） |
| ~~4.6~~ | ~~README / 运行说明~~ | 0.5 | 完成 |
| 4.7 | Bug 熄火缓冲 | 0.5+ | 持续 |

**出口**：M4 可侧载；正式签名见 `app/docs/release-signing.md`。

---

## 5. 质量门槛（进入下一阶段前）

| 门槛 | 标准 |
| --- | --- |
| 静态 | `flutter analyze` 零 error |
| 测试 | 关键路径单测绿；M2 后集成冒烟绿 |
| UI | 与 `docs/design` 四屏主观一致；无明显布局溢出 |
| 播放 | 切歌无 UI 卡死；锁屏控制有效；失败可自动下一首 |
| 数据 | 杀进程重进恢复队列与主题 |
| 性能（中端机参考） | 冷启动 < 2.5s；列表 60fps 主观流畅；切歌到出声 P50 < 3s（正常网） |

---

## 6. 人力与并行方案

### 6.1 单人串行

按 P0→P4 顺序执行，**总 8 周**。接口逆向若阻塞 >2 天，先用 mock 推进 UI，不空等。

### 6.2 双人（推荐）

| 角色 | A · UI/产品向 | B · 数据/播放向 |
| --- | --- | --- |
| W1 | Tokens、组件、首页/发现/我的 | 工程骨架、Dio/Drift、DTO、假数据 |
| W2 | 全屏播放、歌词 UI、走查 | 登录、搜索、详情 API、播放引擎 |
| W3 | FM/榜单/取色/设置 UI | 队列策略、audio_service、歌词解析、持久化 |
| W4 | 收口动效、空错态 | 测试、性能、打包 |

双人关键路径约 **5.5–6 周**，预留一周缓冲更稳。

### 6.3 依赖阻塞点

| 阻塞 | 应对 |
| --- | --- |
| 酷狗签名/风控变更 | 隔离在 `SignInterceptor`；先 mock 保 UI 进度 |
| 音质/部分曲目不可播 | 降级链 + 明确提示，不阻塞整包 |
| 厂商后台杀进程 | `audio_service` 规范 + 文档引导；不承诺全机型 |
| 封面/歌词防盗链 | Referer/UA 策略；失败占位 |

---

## 7. 交付物清单

| 阶段 | 交付物 |
| --- | --- |
| M0 | 可运行工程、主题、路由、存储骨架 |
| M1 | 四屏 UI + 组件库 + 假数据演示 |
| M2 | 可听歌 APK（Debug）+ 播放链路说明 |
| M3 | MVP 功能完整 + 设置 |
| M4 | Release APK、README、演示脚本、测试报告 |

---

## 8. 会议与节奏（建议）

| 节奏 | 内容 |
| --- | --- |
| 每日 | 自更新任务板；阻塞当日暴露 |
| 每周五 | 里程碑对照、砍范围决策（只砍 P1/P2） |
| M1 | **视觉冻结**：设计稿微调只允许 token/间距，不推翻 IA |
| M2 | **接口冻结**：封装签名与方法名，避免 UI 反复 |
| M4 前 3 天 | 只修 Bug，不加功能 |

---

## 9. 范围变更规则

1. 新需求默认进 Backlog，不进当前 Phase。
2. 插入 P0 必须等价置换出等量人日（或明确加周）。
3. 接口能力缺失导致的功能缩水，当周周五书面确认，不默默延后。

---

## 10. 下一步（当前优先 · v1.2）

1. ~~**真机验证 M2 链路**：搜索真实歌曲 → 出声 → 锁屏控制 → 杀进程恢复队列（Android；需可达网络）。~~
2. ~~补齐 M3：每日推荐页（3.1）、排行榜列表页（3.2）。~~
3. ~~**M4 收口**：性能（冷启动/滚动/切歌）、正式签名、Bug 缓冲。~~
   - 正式签名 Release APK 已就绪；性能在 MuMu 上主观验收通过。
4. ~~可选债：SharedPreferences → Drift~~（队列/历史已迁 Drift；见 `lib/data/storage/kugo_db.dart`）
5. **进入 Phase 5 · 酷狗音源抽象**（多音源阶段 A，见下节；任务拆解以 `多音源接入方案.md` §8.1 为准）。
6. 网易云接入排期：**另出独立文档**，本表不写。酷狗产品二期（歌单 CRUD / 逐字 UI / 分享等）另立 M6。

> 本节已按代码实况刷新：登录、首页/详情真源、FM、取色、动效、音质接入播放、设置、红心历史、歌曲详情评论等不再列为「待做」。

---

### Phase 5 · 酷狗音源抽象（M5 · 多音源阶段 A · 约 10 人日 ≈ 2 周）

> 依据：`多音源接入方案.md`（架构）+ 其 **§8.1 任务拆解**。  
> **只做「把酷狗收成 MusicSource」**：接口、注册表、身份/模型、DB 迁移、播放/歌词/封面走 Source。  
> **明确不进本 Phase**：网易云（排期另文）、酷狗产品二期（歌单 CRUD / 逐字 UI / 分享…）、Windows 集成、MV/云盘等。

| # | 任务 | 人日 | 验收 |
| --- | --- | --- | --- |
| 5.1 | `core/source` 契约：Platform / MusicSource / 能力接口 / Failure / PlayUrlResult / quality_map | 1.5 | 接口可编译；FakeSource 可测 |
| 5.2 | `MusicSourceRegistry` + 启动装配（仅 Kugou） | 0.5 | UI 能 `of`/`capability` |
| 5.3 | 模型：`Track.platform`、id=mixSongId 语义钉死、hash→`extras`；`PlaylistBrief.source`→`listKind` | 1.5 | 全库无裸 hash 身份 |
| 5.4 | `KugouSource` 委托现有 Repository（搜索/播放/歌词/详情/FM/榜单/推荐/我喜欢） | 2 | 功能回归与 M3 持平 |
| 5.5 | Player / 歌词 / CoverCache：headers 与 resolve 走 Source | 1 | 去掉写死酷狗 Referer |
| 5.6 | 酷狗 errcode→SourceFailure（20028/20006…）+ `PlayUrlResult` | 0.5 | 失败可被上层统一处理 |
| 5.7 | Drift 迁移 `platform` 列 + 队列/历史序列化 | 1 | 杀进程恢复混源键正确（单源先） |
| 5.8 | `AuthTokenHolder`→`KugouSession`（薄封装即可） | 0.5 | 为第二源留位 |
| 5.9 | 网络日志统一 sink | 0.5 | 日志不丢播放 URL |
| 5.10 | 测试 + `analyze` + 主路径冒烟 | 1 | `app/test` 绿；功能无回退 |
| 5.11 | 熄火缓冲 | 0.5 | — |

**出口（M5）**：酷狗全部现有能力只经 `MusicSource`/`KugouSource` 达到；加第二源时**不必**再改 Player/歌词/UI 骨架。

```text
周次        1              2
         ┌──────────────┬──────────────┐
5.1–5.2  │██            │              │ 契约 + Registry
5.3–5.4  │░████         │              │ 模型/命名 + KugouSource（关键路径）
5.5–5.7  │    ░████     │              │ 播放/歌词/封面 + 失败映射 + DB
5.8–5.9  │        ░██   │              │ Session + 日志
5.10–5.11│              │████          │ 回归与缓冲
         └──────────────┴──────────────┘
```

**裁剪顺序**：5.9 → 5.8 可先 `typedef` 兼容；**不可砍** 5.1/5.3/5.4/5.7/5.10。

**并行建议**：A 做 5.1–5.3 契约与模型；B 做 5.4 委托层；5.5+ 汇合。

#### Backlog（本 Phase 不承诺）

| 项 | 备注 |
| --- | --- |
| 网易云接入 | **排期另出独立文档**；架构见 `多音源接入方案.md`（契约/§7 实现要点） |
| 酷狗产品二期（自建歌单 CRUD、逐字 UI、分享、倍速…） | `docs/gap-vs-echomusic.md`，另立 M6 |
| Windows 系统集成缺口 | `windows-gap-vs-echomusic.md` |

---

## 附录 · 人日汇总

| Phase | 人日 | 累计 |
| --- | --- | --- |
| P0 脚手架 | 5 | 5 |
| P1 假数据 UI | 10 | 15 |
| P2 真源+播放 | 12 | 27 |
| P3 体验 | 8 | 35 |
| P4 发布 | 5 | 40 |
| P5 酷狗音源抽象（多音源 A） | 10 | 50 |

**缓冲建议**：再加 10%–15%（约 4–6 人日）应对接口与设备兼容，故对外承诺按 **9 周** 更稳妥，内部按 8 周冲刺。
