# 网易云接入对比酷狗

> 文档版本：v0.1 · 功能对比（**不含排期**；排期另文《网易云接入排期》）
> 调研对象：`D:\work\NeriPlayer`（网易云 weapi/eapi 全链路，Kotlin）
> 对照对象：`D:\work\kugoFulutter`（酷狗概念版 Flutter 客户端）
> 基准日：2026-09-24
> 配套：[多音源接入方案.md](多音源接入方案.md)（契约与架构）· [docs/api-notes.md](docs/api-notes.md)（酷狗接口）· [docs/gap-vs-echomusic.md](docs/gap-vs-echomusic.md)

图例：

| 符号 | 含义 |
| --- | --- |
| ✅ | 酷狗侧 / kugo 已有等价能力 |
| 🟡 | 部分有 / 形态不同 / 需映射 |
| ❌ | kugo 没有，接网易时要新做 |
| ⚙️ | 协议/加密层，必做基础设施 |
| 🚫 | 明确不建议本期做 |

---

## 一、结论速览

| 问题 | 结论 |
| --- | --- |
| 网易云怎么接？ | **复刻 NeriPlayer 的 weapi/eapi 客户端**，不是开放平台 SDK；加密常量可 Dart 直译 |
| 和酷狗比缺什么？ | 缺的是**第二音源整包**：Crypto/Client/会话/登录 UI + 播放降级链 + 网易侧资料库与推荐 |
| 界面要不要重做？ | **不用**。搜索/播放/歌词/歌单骨架复用；加音源切换、来源角标、网易登录页 |
| 最大差异点 | ① 加密协议（weapi/eapi）② 登录态（Cookie vs Token）③ 音质档名 ④ 无版权时的**多级降级/切源**（网易侧产品能力更强） |
| 建议首刀 | 探针（搜索 + 播放 URL + 歌词）→ 游客可听闭环；登录/我喜欢同步放后 |

---

## 二、NeriPlayer 网易云接入方式（必须先看懂）

### 2.1 协议栈

```text
UI / PlayerManager
    │
NeteaseClient（业务方法）
    │  request / callWeApi / callEApi / callLinuxApi
    ▼
NeteaseCrypto
    weapi  = 双重 AES-CBC + RSA（params + encSecKey）  → music.163.com/weapi/*
    eapi   = AES-ECB + MD5 拼接签名                    → interface.music.163.com/eapi/*
    linux  = AES-ECB                                   → music.163.com/api/*
    plain  = 明文表单/GET                              → 部分 /api/*
    ▼
OkHttp + CookieJar（MUSIC_U / __csrf / _ntes_nuid / NMTID…）
    · WEAPI 前可能 ensureWeapiSession() 拿 __csrf
    · 响应 Brotli / Gzip
```

| 模块 | 职责 | 移植难度 |
| --- | --- | --- |
| `NeteaseCrypto` | weapi/eapi/linux/匿名设备串 | 低（纯算法直译，常量勿改） |
| `NeteaseClient` | 全部业务接口 + Cookie 会话 | 中（约 1300 行） |
| `NeteaseQrLoginClient` | 扫码 unikey / 轮询 / 账号校验 | 中 |
| `NeteaseYdDeviceTokenProvider` | 隐藏 WebView 跑 `createNEFingerprint` 拿 `yd_token` | **高**（Windows 无 WebView） |
| `NeteaseWebLoginActivity` | 网页登录（移动/桌面 UA 分支） | 中 |
| `NeteasePlaylistTrackParams` | 歌单加曲参数 | 低 |
| 播放解析 `NeteasePlaybackResponseParser` | 200/301/无 URL/试听片段 | 低 |
| 智能切源 / 本地兜底 | 无版权 → 降档 → B 站 / 本地同曲 | 中高（策略层） |

### 2.2 会话模型（和酷狗的本质差异）

| | 酷狗（kugo 现状） | 网易云（NeriPlayer） |
| --- | --- | --- |
| 登录凭证 | `token` + `userid`（Authorization 头） | Cookie **`MUSIC_U`** + `__csrf` |
| 设备 | `dfid` / `mid` / `guid` | `_ntes_nuid` / `NMTID`；扫码另有 `yd_token` |
| 未登录 | 游客可搜可播（部分接口有限） | 游客可搜可播；个性化（每日推荐/我喜欢/私人 FM）要登录 |
| 失败码 | `errcode` 20028（风控）/ 20006 | **`code=301` 需登录**；曲目级 `fee` / 无 url / `cannotListenReason` |
| 退出 | 清 token | 清 Cookie 快照 |

→ 接入时必须 **分源 Session**（方案里 `KugouSession` / `NeteaseSession`），不能共用一套 Auth。

---

## 三、功能对比矩阵

### 3.1 播放主链路

| 能力 | NeriPlayer 网易云 | kugo 酷狗 | 接网易要做什么 |
| --- | --- | --- | --- |
| 搜索（单曲） | ✅ `cloudsearch` type=1，分页 | ✅ `/api/v3/search/song` | ⚙️ 新 Client 方法 + mapper |
| 搜索（歌单/专辑/歌手） | ✅ type=1000/100/10；分类 Tab | ✅ 四 Tab 已有 | 🟡 复用搜索 UI，换 Source |
| 搜索（歌词 / MV） | 🟡 歌词接口存在；MV 未主推 | ❌ 歌词搜索 404 已砍 | 🚫 本期不做 |
| 播放 URL | ✅ EAPI `song/enhance/player/url/v1`（level）+ WEAPI 回退 | ✅ `/v5/url` 签名链 | ⚙️ 必做；失败映射见方案 §3.4 |
| 多音质 | ✅ standard/higher/exhigh/lossless/hires/jyeffect/sky/jymaster **8 档** | 🟡 128/320/flac/hires **4 档** | 🟡 `quality_map` 折叠到 4 档 + 网易扩展档 |
| 音质降级重试 | ✅ 无权限/无 URL 自动向下 | ✅ `candidatesDownward` | 🟡 策略对齐即可 |
| 试听片段识别 | ✅ `freeTrialInfo` → 预览提示 | ❌ 无 | ❌ 建议做（体验差别大） |
| 301 会话预热重试 | ✅ | ❌ 无 301 | ❌ 网易侧必做 |
| HTTP/2 失败回退 HTTP/1.1 | ✅ | ❌ | 🟡 可选增强 |
| Range / 分段播放 | ✅ `NeteasePlaybackRangeSupport` | 🟡 依赖引擎 | 🟡 可选 |
| 歌词 LRC | ✅ | ✅ | 🟡 映射 `LyricLine` |
| 歌词逐字 YRC | ✅ 优先 YRC | 🟡 酷狗 KRC 已有逐字解析/部分 UI | ❌ 网易 YRC 解析要移植（可参考 NeriPlayer） |
| 翻译 / 音译 | ✅ ytlrc / romalrc | 🟡 KRC language 块 + LRC 翻译 | 🟡 字段已在 `LyricLine` |
| 歌词缓存 | ✅ 按 songId LRU + 落盘 | 🟡 本地缓存目录 | 🟡 key 用 `platform:id` |
| 链接/分享口令识别 | ✅ 网易分享链 | 🟡 酷狗/B 站链接 | ❌ 网易短链/分享文解析 |
| 外部歌词 LRCLIB | ✅ | ❌ | 🟡 可选（多源通用） |

### 3.2 内容与资料库

| 能力 | NeriPlayer 网易云 | kugo 酷狗 | 接网易要做什么 |
| --- | --- | --- | --- |
| 歌单详情 | ✅ `/api/v6/playlist/detail` + 缓存回退 | ✅ special 系 | 🟡 复用详情页骨架 |
| 专辑详情 | ✅ weapi `/v1/album/{id}` | ✅ | 🟡 |
| 歌手详情 | ✅ head/info + dynamic + songs + albums | ✅ singer 系 | 🟡 |
| 歌手关注 | ✅ | ✅ 云端关注 | ❌ 网易关注 API 要补（Neri 侧有艺术家关注 UI） |
| 相关歌单 | ✅ 抓网页 HTML | ❌ | 🟡 可选 |
| 精品歌单 + 标签 | ✅ | 🟡 部分分类被网关拒 | 🟡 |
| 榜单 | ✅ 飙升/新歌/热歌等固定榜 ID + 排行榜接口语义 | ✅ rank 系 | 🟡 榜单 id 映射 |
| 私人 FM | ✅ `/weapi/v1/radio/get` | ✅ 红心/小众/速览三档 + 曲库池 | 🟡 只做通用 `PersonalFmSource`；**红心 Radio 档位是酷狗独有能力接口** |
| 每日推荐（歌曲） | ✅ `/v3/discovery/recommend/songs` | ✅ everyday_song_recommend | 🟡 |
| 每日推荐（歌单） | ✅ `/v1/discovery/recommend/resource` | 🟡 风格/分类推荐 | 🟡 |
| 新歌推荐 | ✅ `/personalized/newsong` | ✅ 新碟/新歌发现 | 🟡 |
| 个性推荐歌单 | ✅ `/weapi/personalized/playlist` | ✅ | 🟡 |
| **私人雷达 / 时光雷达等** | ✅ 固定雷达歌单 ID 列表 + 元数据接口 | ❌ 酷狗无此产品形态 | ❌ 网易特色；接网易后才有 |
| 用户歌单（自建） | ✅ 列表 + **加曲** `playlist/manipulate/tracks` | ✅ 加曲/删曲已有 | 🟡 能力位对齐 |
| 收藏歌单 / 收藏专辑 | ✅ | ✅ | 🟡 |
| 我喜欢的音乐 | ✅ liked playlist + `song/like` + **本地收藏回写同步** | ✅ 云端我喜欢 + 红心 | ❌ 网易同步逻辑要新做（Neri 约 262 行） |
| 歌单批量加曲 | ✅ `NeteasePlaylistBatchAdd` | 🟡 单曲 add | 🟡 |
| DJ 电台 | ✅ `getDjRadioDetail` / 订阅列表 | ❌ | 🟡 可选 |

### 3.3 登录与账号

| 能力 | NeriPlayer 网易云 | kugo 酷狗 | 接网易要做什么 |
| --- | --- | --- | --- |
| 扫码登录 | ✅ unikey + 轮询 + `yd_token` 设备指纹 | ✅ 酷狗扫码 | ❌ **整套新做**（含 WebView 指纹） |
| 短信验证码 | ✅ | ✅ | 🟡 接口不同 |
| 账号密码 | ✅ EAPI cellphone（密码 MD5） | ✅ | 🟡 |
| 网页 WebView 登录 | ✅ 移动/桌面 UA 自适应 | ❌ | 🟡 可选 |
| 账号信息 | ✅ `w/nuser/account/get` | ✅ usercenter 系 | 🟡 |
| 登录态刷新/轮换 | ✅ Cookie 指纹 + 预热 | 🟡 token 刷新 | ⚙️ 会话层 |
| Windows 登录 | 🟡 WebView 指纹难做 | ✅ 酷狗扫码/账密 | ❌ 网易 Windows 建议先游客 |

### 3.4 播放策略与高级能力（网易侧明显更强）

| 能力 | NeriPlayer 网易云 | kugo 酷狗 | 接网易要做什么 |
| --- | --- | --- | --- |
| 无版权降音质 | ✅ | ✅ 向下候选 | 🟡 已有抽象可复用 |
| **自动切 B 站同曲** | ✅ `tryResolveNeteaseAutoBiliSource` | ❌ 无 B 站 | ❌ 若做多源策略才有意义 |
| **本地同曲兜底** | ✅ 时长容差 8s 匹配本地文件 | ❌ 无本地音乐（已砍） | 🚫 依赖本地库 |
| 下载管理（多源音频） | ✅ 并发/断点/标签 | 🚫 已砍下载管理 | 🚫 与 kugo 产品边界冲突，不建议跟 |
| 一起听 | ✅ | 🚫 | 🚫 |
| 试听/版权提示文案 | ✅ | 🟡 VIP/版权文案 | 🟡 |
| 歌单详情缓存（失败回退） | ✅ | ✅ 歌单缓存已有 | 🟡 |

### 3.5 网易特色 vs 酷狗特色（产品层，不是 bug）

| 网易侧（Neri 有 / 酷狗无） | 酷狗侧（kugo 有 / 网易无或弱） |
| --- | --- |
| 私人/时光/宝藏/新歌/乐迷/神秘 **雷达歌单** | **红心 Radio** 模式/曲库（`HeartRadioSource`） |
| 精品歌单标签体系 | 酷狗概念版权心电台产品形态 |
| YRC 逐字 + 音译官方字段全 | KRC 逐字 + language 块（实现已半成品） |
| 我喜欢 **本地↔云端计划同步** | 我喜欢即时加/删曲 |
| 多级智能切源（B 站/本地） | 单源 + 降档 |
| 一起听 | （kugo 无，且明确不做） |

---

## 四、「没有的」清单（kugo 相对网易云接入所需）

按**接入必要性**分三档。

### P0 · 不做就接不了（协议/骨架）

| # | 项 | 说明 |
| --- | --- | --- |
| 1 | `NeteaseCrypto`（weapi/eapi/linux） | Dart 直译 NeriPlayer `NeteaseCrypto` |
| 2 | `NeteaseClient` + Cookie 会话 + `__csrf` 预热 | 含 Brotli/Gzip |
| 3 | 播放 URL（EAPI v1 + 降档）+ 301 处理 | 对齐 `NeteasePlaybackResponseParser` |
| 4 | 歌词（YRC 优先 + LRC/翻译） | 映射现有 `LyricLine` |
| 5 | `NeteaseSource` 挂进 `MusicSourceRegistry` | 阶段 A 抽象已就绪 |
| 6 | 失败码映射（301/fee/无 url） | 见 `多音源接入方案.md` §3.4 |
| 7 | 音质档映射 8→4 | `quality_map.neteaseLevel` |

### P1 · 可听闭环要加（体验完整）

| # | 项 | 说明 |
| --- | --- | --- |
| 8 | 搜索四类 mapper + 音源切换 UI + 来源角标 | 界面复用 |
| 9 | 歌单/专辑/歌手详情（网易 JSON → 现有模型） | |
| 10 | 试听片段 / 无版权提示 | 网易 `freeTrialInfo` |
| 11 | 封面防盗链 Referer（`music.163.com`） | CoverCache 已按域下发 |
| 12 | 分享链接识别（163 短链/分享文） | 搜索页入口 |
| 13 | 榜单 / 每日推荐 / 新歌 / 个性歌单 | 复用发现/推荐页 |

### P2 · 账号与特色（登录后再做）

| # | 项 | 说明 |
| --- | --- | --- |
| 14 | 扫码登录（unikey + 轮询 + yd_token） | Android 优先；Windows 可降级 |
| 15 | 短信 / 密码登录 | |
| 16 | 网易「我喜欢」读取 + `likeSong` | |
| 17 | 本地收藏 → 网易我喜欢回写同步 | 对齐 Neri `NeteaseLikeSync*` |
| 18 | 用户歌单加曲 `playlist/manipulate/tracks` | 能力位已有 |
| 19 | 收藏歌单/专辑、歌手关注 | |
| 20 | 私人 FM（`PersonalFmSource`） | 无红心档位 |
| 21 | **雷达歌单**（私人/时光/宝藏…） | 网易特色推荐 |
| 22 | 精品歌单标签 | |
| 23 | 歌单详情缓存回退 | 可复用 kugo 缓存 |

### P3 · 可选 / 不建议本期

| # | 项 | 建议 |
| --- | --- | --- |
| 24 | 评论只读（网易） | 🟡 自建 `/comment/music`，Neri 未覆盖 |
| 25 | 热搜词 | 🟡 Neri 未覆盖 |
| 26 | DJ 电台 | 可后置 |
| 27 | 外部 LRCLIB 歌词 | 可选 |
| 28 | 智能切 B 站 / 本地兜底 | 🚫 依赖多源+本地库，另立项 |
| 29 | 音频下载管理 | 🚫 与 kugo 产品边界冲突 |
| 30 | 一起听 | 🚫 |
| 31 | 网页 WebView 登录 | 可选 |

---

## 五、「已经有了、接网易时可复用」清单

| kugo 现状 | 复用方式 |
| --- | --- |
| `MusicSource` / 能力接口 / `MusicSourceRegistry` | 直接 `NeteaseSource implements MusicSource, …` |
| `Track.platform` + `identityKey` | 网易 id = songId 字符串 |
| `PlayUrlResult` / `SourceFailure` / 降档候选 | 网易 301/fee 映射进 Failure |
| 播放页 / 歌词 UI / 队列 / MiniBar | 不改视觉；歌词吃 `LyricLine`（含 `chars`） |
| 搜索四 Tab、歌单/专辑/歌手页骨架 | 换数据源 + 角标 |
| 每日推荐 / 榜单 / 发现页 | 换数据源 |
| 红心 / 我喜欢 UI 模式 | 网易只接 `likeSong`，**不**显示红心 Radio 控件 |
| `KugouSession` 边界 | 平行加 `NeteaseSession` |
| `NetworkLogHub` | 网易请求同样打进网络日志 |
| CoverCache 按 CDN 选 Referer | 已支持 163 域 |

---

## 六、建议接入顺序（功能视角，非人日排期）

```text
1) 协议底座     Crypto + Client + Cookie + 探针（搜索/URL/歌词）
2) 可听闭环     NeteaseSource + 搜歌/播放/歌词 + 音源切换 + 角标
3) 内容页       歌单/专辑/歌手/榜单/每日推荐/新歌
4) 账号         扫码/短信/密码 + 我喜欢读写 + 回写同步 + 收藏/关注
5) 网易特色     私人 FM、雷达歌单、精品标签、试听提示完善
6) 策略         同源降档已够；跨源切 B 站/本地另立项
```

**首刀验收（建议定义成「接入成功」）**  
游客态：搜「周杰伦」→ 出歌单列表 → 点歌出声 → 歌词滚动（有 YRC 则逐字）→ 切歌正常。

---

## 七、实现提示（踩坑点）

| 坑 | 说明 |
| --- | --- |
| weapi 常量 | `PRESET_KEY` / `IV` / RSA 公钥 / `EAPI_KEY` / 盐 **照抄**，勿「优化」 |
| RSA 无填充 | Neri 用 `modPow` 裸 RSA，不是 PKCS1 |
| WEAPI query `csrf_token` | 与 Cookie 里 `__csrf` 一致；缺则先 GET 首页 |
| EAPI 拼接 | `url-api` + data + `nobody…md5…` 的 md5，AES-ECB hex 大写 |
| 响应编码 | Brotli/Gzip 都要解 |
| 播放 `level` | 请求档 ≠ 下发档，用响应 `level` 写 `grantedQuality` |
| 试听 | `freeTrialInfo` 存在时长显著短于原曲 → `isPreviewClip` |
| 301 | 不是 HTTP 301，是 body `code==301` |
| 酷狗 20028 | 不是 301；勿混用映射表 |
| 扫码 `yd_token` | 依赖页面 `createNEFingerprint`；Windows 无 WebView 则先游客 |
| 身份 | 永远 `(platform, id)`；酷狗 id 仍是 mixSongId，不是 hash |
| 搜索 type | 1 单曲 / 10 专辑 / 100 歌手 / **1000 歌单**（写 100 是错的） |
| 评论/热搜 | Neri `core/api/netease/` **未实现**，不要写成「已对齐」 |

---

## 八、对照 NeriPlayer 方法清单（Client 级）

便于写 `NeteaseSource` 时勾范围：

| 分组 | NeriPlayer 已有 | kugo 对应能力 |
| --- | --- | --- |
| 登录 | loginByPhone / sendCaptcha / verifyCaptcha / loginByCaptcha / QR / Web / getCurrentUserAccount | 酷狗登录三件套 ✅ |
| 搜索 | searchSongs(type) | SearchRepository ✅ |
| 播放 | getSongDownloadUrl(level) / getSongUrl(br) | PlayRepository ✅ |
| 歌词 | getLyricNew | LyricRepository（KRC/LRC）🟡 |
| 曲目 | getSongDetail | song_detail 🟡 |
| 歌单 | getPlaylistDetail / getTopPlaylists / getHighQuality* / getRelatedPlaylists / addSongsToPlaylist | playlist_repository + addPlaylistTrack ✅ |
| 专辑 | getAlbumDetail / getUserStaredAlbums | catalog + 收藏 ✅ |
| 歌手 | getArtistDetail/Dynamic/Songs/Albums | catalog ✅ |
| 用户 | getUserPlaylists / Created / Subscribed / getLikedPlaylistId / getUserLikedSongIds / likeSong | user_repository + likes ✅🟡 |
| 推荐 | personalized/playlist / daily resource+songs / personal FM / new song / radar metadata | recommend + fm ✅🟡（雷达 ❌） |
| 电台 | getDjRadioDetail / getUserDjRadios | ❌ |

---

## 九、一页纸

> **网易云 = 第二套协议栈 + 第二套账号，不是酷狗接口换皮。**  
> 能复用的是 UI 骨架、`MusicSource` 抽象、歌词行模型、队列/播放器。  
> 必须新做：weapi/eapi、Cookie 会话、播放 URL/歌词/搜索 mapper、（后期）扫码登录与我喜欢回写。  
> 酷狗独有：红心 Radio；网易独有：雷达歌单、8 档音质、YRC 官方字段、试听识别、（Neri 的）跨源兜底。  
> 下载管理/一起听/本地库兜底：**不跟** NeriPlayer。
