# 酷狗概念版 Flutter 第三方客户端 — 设计方案

> 项目代号：`kugo`（仓库目录 `kugoFulutter`）
> 参考项目：[EchoMusic](D:/work/EchoMusic)（Electron 桌面端酷狗第三方客户端）
> 文档版本：v0.3 · **立项设计稿 + 实现对照**
>
> **阅读提示**：本文保留立项时的产品与架构意图；与代码不一致处以 `app/` 为准。
> 主要落地偏差（详见 §4.2 / §2 / §6 / §14）：单包 `app/lib` 分层（未抽 monorepo packages）、
> 首页并入发现、Windows 桌面壳已可用、FM 为播放器会话而非独立业务页、
> **多音源阶段 A（M5）已落地**（`core/source` + `KugouSource`）、歌词已含 **KRC 逐字**。

---

## 1. 项目定位

### 1.1 一句话

做一个**移动端优先**的酷狗概念版第三方客户端：轻量、沉浸、可玩性强，数据走酷狗公开/逆向接口，不自建音频中转。

### 1.2 与参考项目的关系

| 维度 | EchoMusic | 本项目（kugo） |
| --- | --- | --- |
| 形态 | 桌面端（Electron + Vue 3） | 移动端（Flutter，Android 优先） |
| UI 语言 | 侧边栏 + 多窗口 + 桌面歌词 | 底栏 + 沉浸式播放页 + 滑动手势 |
| 音频引擎 | Rust napi-rs（FFmpeg + SoundTouch） | Flutter 插件 / 原生音频后端 |
| 持久化 | SQLite（native addon） | `drift` / `sqflite` |
| 插件系统 | 完整（页面/音源/歌词/浮窗） | **一期不做**，预留扩展点 |
| 平台能力 | 托盘、任务栏、全局快捷键 | 锁屏控制、通知栏、后台播放 |

EchoMusic 已经验证了「基于酷狗生态的第三方客户端」在账号、歌单、播放链路上的可行性；本项目**复用其领域模型与接口设计思路**，但 UI、交互、架构按移动端重做。

### 1.3 产品原则

1. **概念版气质**：信息密度低于官方完整版，强调封面、渐变、动效与沉浸播放。
2. **本地优先**：播放队列、历史、收藏缓存落本地；弱网可浏览已缓存元数据。
3. **数据路径透明**：应用内直接请求平台接口，不代理音频、不托管账号。
4. **可演进**：一期收敛到「听歌闭环」，二期再上社区/插件/桌面端。

### 1.4 合规与边界（必读）

- 本项目为**个人学习 / 研究向第三方客户端**，不提供官方 SDK，接口来自公开页面与社区逆向，随时可能失效。
- 不内置破解、不绕过付费音质鉴权、不提供音频文件二次分发。
- 歌词、封面、音频 URL 均为运行时向平台获取；应用不落盘分发受版权保护的完整音源（缓存策略见 §7.4）。
- 发布（侧载 / 商店）前需自行评估当地法律与平台 ToS；文档不构成法律建议。
- 不得使用「酷狗」注册商标作为应用名对外发布；内部代号 `kugo`，对外名称建议另拟（如「回声概念版」等中性名）。

---

## 2. 目标平台与版本范围

| 平台 | 优先级 | 说明 |
| --- | --- | --- |
| Android | P0 | 主目标，API 26+；`just_audio` + `audio_service` |
| Windows | P0（已落地） | 桌面壳 + 侧栏导航 + `media_kit` 出声 + `audio_service_win` SMTC/托盘/Thumbar；缺口见 `docs/windows-gap-vs-echomusic.md` |
| iOS | P1 | 结构预留，签名与后台策略另评估 |
| macOS | P2 | 复用 Flutter desktop，未开工 |

**一期（MVP）必须可用场景**

1. 登录 / 游客浏览
2. 首页推荐 + 发现
3. 搜索（歌曲 / 歌单 / 歌手 / 专辑）
4. 歌单 / 专辑 / 歌手详情
5. 播放：队列、模式、进度、音质、锁屏与通知栏控制
6. 歌词页：逐行滚动 + 翻译
7. 我喜欢 / 播放历史 / 本地设置

**明确不做（一期）**

- 插件系统、一起听、MV 弹幕、听歌识曲、云盘上传、桌面歌词窗口、Automix 级切歌混音。

---

## 3. 技术选型

### 3.1 总览

| 层 | 选型 | 理由 |
| --- | --- | --- |
| 框架 | Flutter / Dart 3 | 单代码库、成熟音频生态、自绘 UI 适合概念版动效 |
| 状态管理 | `flutter_riverpod` | 编译期安全、异步友好、可测试 |
| 路由 | `go_router` | 声明式、深链、嵌套导航 |
| 本地存储 | `drift` + `sqlite3_flutter_libs` | 队列/历史 SQLite（含 `platform` 列）；启动自动迁移旧 SharedPreferences |
| 简单 KV | `shared_preferences` | 主题、开关、token、FM 会话快照 |
| 网络 | 自封装 `KugoClient`（签名/日志/网关识别） | 统一签名、错误映射、URL 过滤识别 |
| 音频播放 | Android：`just_audio` + `audio_service`；Windows：`media_kit` + `audio_service_win` | 经 `AudioPlayerPort` 注入，可换后端 |
| 后台任务 | `audio_service`（Android）+ `audio_service_win`（Windows SMTC） | 系统媒体控制、锁屏；音频焦点仍仅 Android |
| 缓存图片 | `cached_network_image` / 自研 `cover_cache` | 封面磁盘缓存 |
| 取色 | 自研 `CoverPalette` | 播放页/MiniBar 渐变 |
| 国际化 | **默认 zh-CN 硬编码文案**（未上 ARB） | 一期如此，二期再抽 |
| 测试 | `flutter_test` | 队列/LRC+KRC/搜索/FM/桌面布局/MusicSource 契约等 `app/test/` |

### 3.2 为何不用 / 缓用

| 方案 | 结论 |
| --- | --- |
| `media_kit` / libmpv | 移动端一期不用（`just_audio` 已覆盖）；**Windows 已采用** media_kit 出声 |
| 自建 Rust 音频引擎（对齐 EchoMusic） | 移动端无 Automix/独占输出刚需；二期若需 EQ/变速再评估 |
| 插件热加载 | 一期架构预留接口，不做运行时加载 |
| GraphQL / 自建 BFF | 直接对接平台 REST + 本地适配层，减少运维 |

---

## 4. 总体架构

```text
┌─────────────────────────────────────────────────────────┐
│  Presentation（Flutter Widgets）                         │
│  pages / widgets / theme / router                        │
├─────────────────────────────────────────────────────────┤
│  Application（Riverpod Notifier / UseCase）              │
│  player_controller · library_controller · search…        │
├─────────────────────────────────────────────────────────┤
│  Source（多音源契约 · M5 已落地）                          │
│  MusicSource · Registry · 能力接口 · SourceFailure       │
│  KugouSource（委托现有 Repository）                       │
├─────────────────────────────────────────────────────────┤
│  Domain（纯 Dart）                                        │
│  entities · failures · policies（队列推进/音质选择）       │
├─────────────────────────────────────────────────────────┤
│  Data                                                    │
│  kugou_api（dio + 签名） · drift DAO · cache · mapper    │
├─────────────────────────────────────────────────────────┤
│  Platform                                                │
│  audio_service · just_audio/media_kit · path_provider    │
└─────────────────────────────────────────────────────────┘
```

### 4.1 分层职责

- **Presentation**：只依赖 Application 暴露的 `AsyncValue` / 不可变状态；不直接碰 dio/sqlite。
- **Application**：编排用例（播放一首、切换队列、收藏），持有会话级状态；跨页面共享的 player / auth / theme 放根 Provider。
- **Domain**：实体与纯函数策略（下一首怎么推进、音质降级顺序），**零 Flutter 依赖**，便于单测。
- **Data**：接口封装、DTO → Entity 映射、Drift 表、内存/磁盘双层缓存。
- **Platform**：仅通过接口注入（如 `AudioPlayerPort`），方便测试替换与后续换引擎。

### 4.2 模块边界（落地形态：单包分层）

```text
kugoFulutter/
├─ app/                      # Flutter 工程（UI + 数据 + 播放，按文件夹分层）
│  └─ lib/
│     ├─ core/               # theme · models · api · cache · utils · **source（MusicSource 契约）**
│     ├─ data/               # repositories · **sources/kugou** · storage（Drift/设备身份）
│     ├─ features/           # explore · discovery · search · player · fm · profile · …
│     └─ shared/             # widgets · shell · tray · taskbar
├─ docs/                     # 设计、接口笔记、差距清单、视觉稿
└─ app/tool/                 # 接口探测脚本
```

> 立项时曾规划 monorepo `packages/{kugo_core,kugo_api,kugo_db,kugo_player}`。
> **实际未抽 package**，全部在 `app/lib` 内按文件夹分层；`AudioPlayerPort` / `MusicSource` 等端口仍按可测试边界组织。
> 多音源阶段 A 已在 `core/source` + `data/sources/kugou` 落地；详细契约见 `多音源接入方案.md`。

---

## 5. 领域模型（对齐 EchoMusic，按移动端裁剪）

```text
User
 ├─ id, nickname, avatar, vipType, level

Track（歌曲）
 ├─ platform（MusicPlatform；身份键 = platform:id）
 ├─ id（酷狗 = mixSongId 优先）, name, durationMs
 ├─ artists[], album
 ├─ coverUrl, coverSize
 ├─ qualities[]（standard/hq/sq/hi_res…）
 ├─ hash / albumId / mixSongId…（平台私有字段，Source 内部读）
 ├─ hasMv, isCloud, isVipOnly

Artist / Album / Playlist（歌单）
 ├─ id, name, cover, description, trackCount, creator
 ├─ platform, listKind（原 source 字段，避免与音源撞名）

Queue（播放队列）
 ├─ id, title, tracks[], currentIndex, mode
 ├─ isDynamic（如私人 FM）

LyricLine
 ├─ startMs, durationMs, text
 ├─ chars[]（KRC 逐字：LyricChar start/end）
 ├─ translation?, roma?
```

关键策略（从 EchoMusic `playbackQueueDecision` 思路迁移，需移动端化）：

- **队列推进**：顺序 / 列表循环 / 随机 / 单曲循环；随机使用可复现种子，避免「连点两下同一首」。
- **音质降级链**：用户默认音质 → 平台可播音质 → 标准；失败自动尝试 backup URL（EchoMusic 已处理 `backup_url`）。
- **播放意图（PlaybackIntent）**：每次播放带 `seq`，过期响应丢弃，防止快速切歌竞态。
- **过渡**：一期仅系统 gapless 能力 + 短交叉淡入（若播放器支持）；不做 BPM 混音。

---

## 6. 界面与信息架构（酷狗概念版气质）

### 6.1 视觉方向

| 项 | 规格 |
| --- | --- |
| 风格锚点 | 酷狗概念版 + Apple Music「现在就听」的沉浸感；深色优先 |
| 背景 | 深空蓝灰 `#0B0E14`；浅色模式 `#F5F6F8` |
| 主墨色 | 白 `#FFFFFF` / 次级 `#9AA3B2` |
| 强调色 | 概念蓝紫渐变 `#5B7CFF → #A855F7`（封面取色可覆盖） |
| 表面 | 卡片 `rgba(255,255,255,0.06)` + 1px 描边；大圆角 16–20 |
| 字体 | 标题 22–28 / 正文 14–16 / 辅助 12；歌词行高 1.6 |
| 动效 | 封面旋转、页面 Hero、列表滑入；时长 200–350ms，尊重系统减动效 |

**签名时刻（2 个）**

1. **沉浸播放页**：封面色提取生成全屏渐变背景，黑胶/封面悬浮 + 歌词双栏切换，下拉关闭手势。
2. **首页「此刻推荐」横滑大卡**：封面 + 半透明信息条，首卡轻微视差。

### 6.2 导航结构（落地）

```text
RootShell（Android 底栏 2 Tab / Windows 侧栏）
 ├─ 发现 Explore（原首页已并入；搜索入口 + FM 舞台 + 推荐）
 ├─ 我的 Profile
 ├─ MiniPlayerBar / DesktopPlayerBar → FullPlayer
 └─ FullPlayer
      ├─ 封面模式 ↔ 歌词模式（/player/lyrics）
      ├─ 队列抽屉
      └─ FM 入口药丸 / 面板（会话进行中）

Stack / Shell 分支页面
 ├─ Search（多 Tab）
 ├─ PlaylistDetail / AlbumDetail / ArtistDetail / TrackDetail
 ├─ DailyRecommend / Ranks / RecommendHub
 ├─ FmPage（/fm，桌面侧栏主入口；Android 以会话 + 播放页为主）
 ├─ Likes / History
 ├─ Login
 └─ Settings / About
```

> 与初稿差异：三 Tab「首页/发现/我的」中的 **Home 已并入 Explore**（路由 `/`、`/home` 重定向到 `/explore`）。

### 6.3 页面清单（一期）

| 路由 | 页面 | 要点 |
| --- | --- | --- |
| `/explore` | 发现（含原首页） | 搜索入口、FM 舞台、每日/推荐/榜单入口 |
| `/discovery` | 探索发现 | 独立发现页（个人中心拆分后的探索入口） |
| `/login` | 登录 | 扫码 / 验证码 / 账号密码 |
| `/search` | 搜索 | 建议、**歌曲/歌单/专辑/歌手** 多 Tab、历史 |
| `/playlist/:id` | 歌单详情 | 头图、播放全部、歌曲列表 |
| `/album/:id` | 专辑详情 | 同上 |
| `/artist/:id` | 歌手详情 | 热门歌曲 / 专辑（`singerid`） |
| `/track` `/song` | 歌曲详情 | 封面、音质、评论只读 |
| `/daily` | 每日推荐 | 日期封面、播放全部 |
| `/ranks` `/rank/:id` | 排行榜 | 列表 + 详情 |
| `/fm` | 私人 FM | 桌面主入口；Android 侧为会话视图 |
| `/player` `/player/lyrics` | 全屏播放 / 歌词 | 见上 |
| `/likes` `/history` | 我喜欢 / 历史 | 本地 + 云端同步 |
| `/profile` `/profile/detail` | 我的 / 个人中心 | 账号卡、统计、收藏模块、设置入口 |
| `/settings` | 设置 | 主题、播放、音质、缓存、关于 |

---

## 7. 关键模块设计

### 7.1 接口层（`kugo_api`）

对齐 EchoMusic 渲染层调用面（`src/renderer/api/*`），移动端适配：

```text
AuthApi      登录、二维码、token 刷新、用户信息
HomeApi      推荐歌单、风格推荐、编辑精选
SearchApi    搜索 / 热词 / 默认词 / 建议
PlaylistApi  歌单详情、分类、收藏
AlbumApi / ArtistApi / TrackApi
PlayApi      播放地址解析（含 backup_url、音质）
LyricApi     LRC / KRC / 翻译
RankApi      榜单列表与详情
FmApi        私人 FM 队列
```

**请求封装要点**

- 统一 `Dio`：BaseUrl 可配置、超时、重试（仅幂等 GET）、错误映射为 `KugoFailure`。
- 签名 / 公共参数（`appid`、`clientver`、`clienttime`、签名串等）集中在 `SignInterceptor`；具体算法实现时以当前可用逆向为准，**文档不固化易过期的密钥细节**。
- 登录态：Cookie / token 注入拦截器；401 触发全局 re-auth 流。
- 域名容错：主备 gateway 列表（参考 EchoMusic 对 `gateway.kugou.com` 的使用）。
- 所有响应 DTO 与 Domain Entity 分离，API 字段变更不直接炸 UI。

### 7.2 播放子系统（`kugo_player`）

```text
PlayerController (Notifier)
  ├─ 解析播放地址（PlayApi）
  ├─ 维护 Queue + currentIndex + mode
  ├─ 前瞻预取下一首 URL（防切歌卡顿）
  ├─ 写入 audio_service MediaItem（封面、标题、时长）
  └─ 进度节流广播（100–250ms）给 UI / 歌词

AudioPlayerPort
  ├─ load(url, headers)
  ├─ play / pause / seek / stop
  ├─ position / duration / state streams
  └─ setSpeed / setVolume

JustAudioPlayer 实现 AudioPlayerPort
AudioServiceHandler 桥接系统媒体控制
```

**状态机（简化）**

```text
idle → resolving → loading → ready ⇄ playing ⇄ paused
                 ↘ failed（可重试 / 自动下一首）
```

**与 EchoMusic 的差异**：不做原生 DSP 图、频谱帧、独占设备；系统音效交由平台。

### 7.3 歌词

- 解析 LRC（逐行）与 **KRC（逐字时间轴 + `[language:]` 译文/音译）**；翻译/罗马音独立副行。
- 时间轴：以播放器 `position` 驱动，二分查找当前行；逐字做卡拉 OK 已唱/未唱着色；滚动用 `ScrollablePositionedList` 或自实现。
- 设置：字号与行间距（播放页入口 `lyric_display_sheet` 连续滑块，倍率 0.8–1.6 / 0.5–2.0；拖动即时预览、松手落盘；「恢复默认」一键回到 100%；双端共用同一套值）、翻译/音译开关。行盒高度取「倍率结果」与「文字实际高度」的较大者，行间距压到下限也不会裁字。
- 待做：偏移 ms（±5s，步进 0.5s）。
- 一期不做写真模式、桌面歌词、弹幕歌词、换肤。

### 7.4 缓存与离线

| 数据 | 策略 |
| --- | --- |
| 封面 | `cached_network_image` 磁盘缓存，按尺寸 key |
| 元数据（歌单/歌曲） | 内存 LRU + Drift 表；带 `fetchedAt`，TTL 例如 6h |
| 播放 URL | 不落库；每次播放现取（防盗链过期） |
| 音频本体 | **默认不持久化整曲**；可选「边下边播内存/临时缓存」，退出即清 |
| 设置 / 队列 | 启动恢复上次队列与 index |

### 7.5 本地数据库（Drift 表草案）

```text
tracks(id PK, hash, name, duration_ms, album_id, cover, raw_json, updated_at)
artists / albums / playlists / playlist_tracks
play_queue_items(queue_id, position, track_id, is_current)
play_history(id, track_id, played_at, duration_played_ms)
favorites(kind, ref_id, created_at)
search_history(id, keyword, searched_at)
app_kv(key, value)
```

---

## 8. 主题与组件规范

### 8.1 Design Tokens

```dart
// 概念示意
class KugoColors {
  static const bg = Color(0xFF0B0E14);
  static const surface = Color(0xFF151A24);
  static const primary = Color(0xFF5B7CFF);
  static const secondary = Color(0xFFA855F7);
  static const textPrimary = Color(0xFFFFFFFF);
  static const textSecondary = Color(0xFF9AA3B2);
}

class KugoRadius { static const card = 20.0; static const chip = 999.0; }
class KugoSpacing { /* 4 基准网格: 4/8/12/16/24/32 */ }
```

封面取色：`palette_generator` 或自实现主色提取，驱动播放页渐变与 MiniBar 指示色。

### 8.2 核心组件库（一期）

- `TrackTile` / `TrackTileWide`
- `PlaylistCard`（竖版封面 + 双行标题）
- `SectionHeader`（标题 + 更多）
- `GradientAppBar`
- `MiniPlayerBar`
- `PlayerControls`
- `LyricView`
- `QualityBadge` / `VipBadge`
- `AsyncBody`（loading skeleton / empty / error retry）

列表性能：长列表用 `ListView.builder`；歌单详情超 200 首考虑分页加载。

---

## 9. 错误、日志与可观测性

- 失败模型：`NetworkFailure` / `AuthFailure` / `ApiFailure(code, msg)` / `PlayResolveFailure` / `DbFailure`。
- UI 统一 `Result` / `AsyncValue` 展示；播放失败 toast + 自动跳过（可配次数上限，对齐 EchoMusic 思路）。
- 日志：`talker` 或自封装；**默认不落敏感 header**；Debug 开关输出 API 摘要。
- 崩溃：接入 `sentry`（可选，Release 且用户同意后）；一期可仅本地 log 文件。

---

## 10. 工程与质量

### 10.1 目录（`app/lib` 实况）

```text
lib/
├─ main.dart
├─ app.dart                 # MaterialApp.router + ProviderScope + 路由
├─ core/                    # theme · models · api · cache · utils · platform · **source/**
├─ data/
│  ├─ repositories/         # catalog/search/play/lyric/fm/user/…
│  ├─ sources/kugou/        # KugouSource · KugouSession（M5）
│  └─ storage/              # kugo_db（Drift）· queue_store · device_identity
├─ features/
│  ├─ explore/ discovery/ search/ recommend/ rank/
│  ├─ playlist/ album/ artist/ song/
│  ├─ player/               # 全屏播放、歌词、队列、AudioPlayerPort
│  ├─ likes/ history/ profile/ settings/ auth/ debug/
│  └─ fm/                   # 会话控制器 + 舞台 UI
└─ shared/                  # widgets · shell · tray · taskbar
```

### 10.2 规范

- `analysis_options.yaml`：`flutter_lints` + `strict-casts` + `prefer-single-widget-per-file` 渐进启用。
- 生成代码：`build_runner watch`；CI 跑 `dart analyze` + `flutter test`。
- 提交：conventional commits；功能分支 → PR。
- 环境：`--dart-define=API_BASE=...` / `DEBUG_NET=true`。

### 10.3 测试策略

| 层 | 内容 |
| --- | --- |
| Unit | 队列推进、音质降级、歌词索引、签名参数拼装（mock） |
| Widget | MiniBar、TrackTile、播放按钮状态 |
| Integration | 冷启动 → 搜歌 → 播放 10s → 切下一首（mock API） |

---

## 11. 路线图

### Phase 0 — 脚手架（0.5–1 周）

- Flutter 工程、路由、主题、Riverpod、Drift、Dio 骨架
- 假数据驱动首页 + 播放页 UI（不接真 API）

### Phase 1 — 听歌闭环（2–4 周）

- 登录 / 游客、搜索、歌单/专辑/歌手详情
- 播放地址解析 + just_audio + audio_service
- 队列、模式、MiniBar、全屏播放、歌词滚动
- 我喜欢、历史、基础设置

### Phase 2 — 体验加深（2–3 周）

- 每日推荐、排行榜、私人 FM
- 封面取色渐变、更多动效
- 音质切换、定时停止、缓存管理
- 评论、分享

### Phase 3 — 扩展（按需）

- 听歌识曲、云盘、一起听
- iOS 打磨、桌面布局
- 插件点位（音源 / 歌词替换接口）

### 里程碑验收（MVP）

- 冷启动 < 2.5s（中端机）
- 搜索到出声 < 3s（正常网络）
- 切歌无 UI 卡死；锁屏可控制
- 无登录可浏览公开歌单并试听（若接口允许）

---

## 12. 风险与对策

| 风险 | 影响 | 对策 |
| --- | --- | --- |
| 接口签名/风控变更 | 全功能不可用 | 签名层隔离 + 版本化；多 gateway；快速热更配置（远程 JSON 可选） |
| 音质/版权校验 | 部分曲目无法播 | 明确提示；自动降级；不实现破解 |
| 后台播放被杀 | 移动端体验差 | `audio_service` 规范 + 前台服务通知；文档指导厂商后台白名单 |
| 封面/歌词 CDN 防盗链 | 图裂、词缺 | 请求头 Referer/UA 模拟；失败占位图 |
| 法律与商标 | 发布风险 | 中性应用名；声明第三方；个人侧载为主 |
| 范围蔓延 | 做不完 | 严格按 Phase 砍功能；插件/社区后置 |

---

## 13. 对 EchoMusic 的可借鉴清单（实施时对照）

| EchoMusic 资产 | 移动端动作 |
| --- | --- |
| `docs/features.md` 功能地图 | 作为功能 backlog，按 Phase 过滤 |
| `src/renderer/api/*` | 对照实现 `kugo_api` 方法签名 |
| `stores/player/*` Intent/状态机 | 迁移为 Dart 策略与单测 |
| 播放队列多队列模型 | 一期简化为「当前队列 + 历史」 |
| 音质 / backup_url 处理 | 必须复刻 |
| 歌词 LRC/KRC + 翻译 | 必须复刻解析能力 |
| 桌面歌词 / 插件 / DSP | 不做，仅保留接口注释 |
| SQLite 表设计 | 简化后用于 drift |

---

## 14. 实现现状与后续（刷新）

**已落地（对照代码）**：听歌闭环、真源播放（含 `/v5/url`）、登录、FM 真推荐、
搜索四 Tab、榜单/每日推荐/推荐聚合、歌词（**LRC + KRC 逐字 + 译文/音译**）、评论只读、
设置、云端我喜欢/收藏/关注、Drift 队列历史、Android 锁屏通知、Windows 壳 + media_kit +
SMTC/托盘/Thumbar（含收藏）/任务栏进度、**多音源阶段 A（M5）**：`MusicSource` 契约 +
`KugouSource` + `KugouSession` + `platform:id` 身份 + Drift `platform` 列。

**后续（与 `docs/gap-vs-echomusic.md` 一致）**：

1. 自建歌单 CRUD；倍速 UI；分享（本地音乐 / 下载管理两个占位入口已于 2026-09 砍掉）
2. 评论写入；歌词换肤/写真
3. Windows 系统集成（全局快捷键/Mini 窗/材质/自启…，见 `docs/windows-gap-vs-echomusic.md`）
4. 网易云第二源（排期见 `网易云接入排期.md`）；功能页 Repository 调用收薄到 Source
5. 真机性能复测与歌词画廊抛光

---

## 附录 A · 与官方「酷狗概念版」的功能对照（指导取舍）

| 官方概念版常见能力 | 本项目 |
| --- | --- |
| 推荐 Feed / 每日推荐 | ✅ |
| 歌单广场 / 分类 | ✅（部分公开接口被拒，见 `api-notes.md`） |
| 搜索 | ✅ 四 Tab |
| 沉浸播放 + 歌词 | ✅（LRC + KRC 逐字 + 译文/音译） |
| 私人 FM | ✅ 真推荐 + 关键词回落 |
| 排行榜 | ✅ |
| 我喜欢 / 收藏 | ✅ 本地 + 云端 |
| 听歌识曲 | 不做（成本高） |
| 一起听 / 社区 | 不做 |
| 概念会员权益 | 不实现购买；仅展示 VIP 曲目不可播提示 |

## 附录 B · 术语

- **Track hash**：酷狗体系中歌曲内容哈希，常用于取播放地址。
- **Gateway**：酷狗网关域名，多环境容灾。
- **PlaybackIntent**：带序号的播放请求，用于丢弃过期异步结果。
- **MiniBar**：底部常驻迷你播放条。
