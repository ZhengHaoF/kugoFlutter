# 多音源接入方案

> 项目：`kugoFulutter`（酷狗概念版 Flutter 客户端）
> 文档版本：v0.4 · 架构方案（多源契约；**不含网易云排期**——排期另文）
> v0.2 复核：修正交叉引用 / 搜索 type；补 `PlaylistBrief.source` 命名冲突、播放 headers 硬编码、歌词模型对齐
> v0.3 外审修订：纠正歌单写能力、301 归属、封面缓存误判、酷狗 id 语义；收紧「已对齐 NeriPlayer」表述
> 参考实现：`D:\work\NeriPlayer`（网易云 weapi/eapi 全链路）
> 配套文档：[设计方案.md](设计方案.md) · [docs/api-notes.md](docs/api-notes.md) · [docs/gap-vs-echomusic.md](docs/gap-vs-echomusic.md)
>
> **阅读提示**：本文描述「从单酷狗源升级为多音源」的目标架构与落地路径。
> 与现有代码不一致处，以本文定义的目标态为准；迁移步骤兼容当前 `app/lib` 分层。

---

## 1. 结论与原则

### 1.1 结论

| 问题 | 结论 |
| --- | --- |
| 要不要调架构？ | **要**。当前 Repository / 登录 / `Track` 身份都是酷狗硬编码，直接塞网易云会互相污染。 |
| 要不要做成「插件」？ | **不要运行时热加载插件**（EchoMusic 式插件系统）。只做**编译期多音源模块**：`MusicSource` 接口 + 各平台 adapter + 注册表。 |
| 界面要不要重做？ | **不重做**。播放页 / 歌词 / 队列 / 主题壳 / 列表骨架可复用；只加音源切换、来源角标、平台登录页。 |
| 特殊能力怎么办？（如酷狗红心 FM） | **能力接口**（capability interface）按需实现；UI 运行时 `is` 判断显隐，不塞进基类。 |

### 1.2 设计原则

1. **基类最小**：`MusicSource` 只放「所有源都有」的能力；特殊能力进可选接口。
2. **身份含平台**：一切可引用的实体主键是 `(platform, id)`，禁止再用裸 `hash` 当全局身份。
3. **适配器收边界**：平台 JSON / 加密 / 音质档 / 登录态只出现在 `*_source` 包内；UI 只认统一模型。
4. **编译期组合**：启动时 `MusicSourceRegistry` 注册内置源；不做动态加载、不做脚本沙箱。
5. **能力驱动 UI**：入口显隐看「实现了哪些能力接口」，不在 UI 写 `if (platform == kugou)` 业务分支（平台角标除外）。
6. **播放器保持音源无关**：`AudioPlayerPort` 只吃 URL + headers；FM 会话规则留在 transport（`PlaybackQueueSource`）。

---

## 2. 目标架构

```text
┌──────────────────────────────────────────────────────────────┐
│ Presentation                                                   │
│  pages / widgets / theme / router                              │
│  （能力显隐：source is PersonalFmSource → 显示 FM 入口）        │
├──────────────────────────────────────────────────────────────┤
│ Application                                                    │
│  player_controller · search_controller · likes · fm …          │
│  （只依赖 MusicSource / 统一模型 / 统一错误）                    │
├──────────────────────────────────────────────────────────────┤
│ Source Layer（新增，核心）                                      │
│  MusicSourceRegistry                                           │
│   ├── KugouSource   : MusicSource, PersonalFmSource, …         │
│   ├── NeteaseSource : MusicSource, DailyRecommendSource, …     │
│   └── (LocalSource / 第三源 …)                                 │
│  能力接口：PersonalFmSource · HeartRadioSource · …              │
├──────────────────────────────────────────────────────────────┤
│ Platform Clients（各源私有，不导出给 UI）                       │
│  KugoClient + KugoSign + DeviceIdentity                        │
│  NeteaseCrypto + NeteaseClient + Cookie 会话                   │
├──────────────────────────────────────────────────────────────┤
│ Domain Models + Storage                                        │
│  Track(platform, id, …) · AppQuality · 统一 Failure            │
│  drift：队列 / 历史 / 收藏（带 platform 列）                     │
└──────────────────────────────────────────────────────────────┘
```

### 2.1 目录建议（增量，不强制 monorepo）

```text
app/lib/
  core/
    source/                     # 新增
      music_platform.dart       # enum MusicPlatform
      music_source.dart         # 基类 + 统一结果/错误
      capabilities.dart         # 可选能力接口
      registry.dart             # MusicSourceRegistry
      quality_map.dart          # AppQuality ↔ 平台档位
    models/                     # Track 等加 platform；去掉 UI 对 hash 的硬依赖
    api/
      kugo/…                    # 现有 Kugo* 可原地留或挪入 kugou/
      netease/                  # 新增：crypto + client + endpoints
  data/
    sources/
      kugou/kugou_source.dart   # 现 Repository 逻辑迁入或委托
      netease/netease_source.dart
    repositories/               # 过渡期保留；最终变薄或删除
```

---

## 3. 核心抽象

### 3.1 平台与统一实体

```dart
enum MusicPlatform { kugou, netease }

class Track {
  final MusicPlatform platform;
  /// 平台侧**歌曲**稳定 id（不是文件 hash）。
  /// - 酷狗：沿用现有语义 **`mixSongId` 优先**，否则 `audioId`/`album_audio_id`，再否则才落到 hash
  ///   （见 `mappers.dart`；`likes_controller` 删曲的 fileid 也用 `mixSongId`）。
  /// - 网易：数字 songId 字符串。
  /// 阶段 A **不**把 id 改成 hash——hash 是音频文件哈希，relate_goods 各音质可不同，不能当歌曲身份。
  final String id;
  final String name;
  final String artist;
  final String album;
  final String coverUrl;
  final int durationMs;
  // …通用字段

  /// 平台私有扩展。酷狗必带 **`hash`**（播放 `/v5/url`、歌词 search 的 key）以及
  /// `albumId` / `mixSongId` / `relateGoods` / `privilege`…；不进 UI 业务判断，只由对应 Source 内部读取。
  final Map<String, Object?> extras;
}
```

**身份约定**

| 用途 | 键 |
| --- | --- |
| 队列 / 历史 / 收藏 / 歌词缓存 | `(platform, id)` |
| 媒体会话 MediaItem.id | `'$platform:$id'` |
| 歌词请求去重 | `'$platform:$id'`（**废弃** 现有 `id\|hash`） |
| 深链 / 路由参数 | `platform` + `id`（酷狗 id 即 mixSongId 语义）；**不再**单独传 `hash` 等酷狗字段 |

**命名冲突警告**：现有 `PlaylistBrief.source` 是酷狗业务字段（`1`=歌单 / `2`=专辑），与「音源」语义撞名；同类型里还有 `type`（`0` 自建 / `1` 收藏）。迁移时 `source` 改名 **`listKind`**（勿再叫 `type`），平台字段用 `platform`，避免三义撞车。

### 3.2 能力接口（避免上帝接口 / 接口爆炸）

**基类 `MusicSource`（人人实现）**

| 方法 | 说明 |
| --- | --- |
| `MusicPlatform get platform` | |
| `searchSongs / searchPlaylists / searchAlbums / searchArtists` | 分页返回 `SearchPageResult<统一 Brief>`；`total` 拿不到就返回 null（分页由 `hasMore` 启发式兜底） |
| `resolvePlayUrl(Track, AppQuality)` | 短时 URL；失败见 §3.4 |
| `fetchLyric(Track)` | 返回统一歌词包（行/字 + 翻译 + 是否逐字） |
| `playlistDetail / albumDetail / artistDetail` | 曲目列表 + 元数据 |

**可选能力（谁有谁 `implements`）**

| 接口 | 酷狗 | 网易 | 说明 |
| --- | --- | --- | --- |
| `PersonalFmSource` | ✅ | ✅（私人 FM） | `nextFmTracks` / `feedback(like\|skip\|trash)`；红心 Radio 的模式/曲库属酷狗扩展 |
| `HeartRadioSource` | ✅ | ❌ | 红心 Radio 模式切换、曲库池；仅 `KugouSource` |
| `DailyRecommendSource` | ✅ | ✅ | 若返回结构差异大，由各自 adapter 映射为统一「推荐日更包」 |
| `RankSource` | ✅ | ✅ | 榜单列表/详情 |
| `UserLibrarySource` | ✅ | ✅ | 我喜欢 / 收藏歌单 / 收藏专辑 / 关注歌手（**各源账号各管各的**） |
| `UserPlaylistWriteSource` | ✅ 加曲/删曲 | ✅ 加曲/删曲 | 酷狗已有 `addPlaylistTrack` / `deletePlaylistTracks`（`user_repository`，likes 在用）；**建单/改名等仍缺**。网易对齐 NeriPlayer `/playlist/manipulate/tracks` |
| `CommentsReadSource` | ✅ | ⚠️ 待建 | 酷狗 `song_detail` 评论只读已有；网易评论**不在** NeriPlayer `core/api/netease/` 内，端点可自建，勿写成「已对齐参考实现」 |
| `DeviceLoginSource` | ✅ 扫码/短信/密码（`features/auth` 独立链路） | ✅ 扫码（2026-09-24 落地） | `createLoginQr / pollLoginQr / currentAccount / logout`；平台字段（unikey/chainId/MUSIC_U）留在实现内，UI 只见 `LoginQrSession` / `LoginQrStatus`。**酷狗不走这里**（`AuthController` 是独立一套） |
| `SearchHotSource` | ✅ | ⚠️ 待建 | 酷狗 `/api/v3/search/hot` 已有；网易热搜 **NeriPlayer 网易包未覆盖** |

**反模式（禁止）**

- 基类堆 `bool get hasFm` + 空实现 `throw UnimplementedError`。
- 一个 `MusicSource` 接口列出全部 30 个方法。
- UI 里 `switch (platform)` 写业务（平台角标、登录页路由除外）。

### 3.3 注册表

```dart
class MusicSourceRegistry {
  MusicSourceRegistry(List<MusicSource> sources)
      : _byPlatform = {for (final s in sources) s.platform: s};

  final Map<MusicPlatform, MusicSource> _byPlatform;

  MusicSource of(MusicPlatform p) => _byPlatform[p]!;
  MusicSource ofTrack(Track t) => _byPlatform[t.platform]!;
  Iterable<MusicSource> get all => _byPlatform.values;

  /// 能力探测：单源入口显隐用
  T? capability<T>(MusicPlatform p) {
    final s = _byPlatform[p];
    return s is T ? s as T : null;
  }

  /// 任一源具备即显示入口（首页/发现卡在「当前源未知」时用）
  bool anyHas<T>() => _byPlatform.values.any((s) => s is T);
}
```

启动组装（`main` / DI）：

```dart
final registry = MusicSourceRegistry([
  KugouSource(...),
  if (enableNetease) NeteaseSource(...),
]);
```

### 3.4 统一错误与结果

```dart
sealed class SourceFailure implements Exception {
  const SourceFailure();
}
class NetworkFailure extends SourceFailure {
  /// true = 酷狗网关 HTML「URL 过滤」等中间盒拦截（looksLikeUrlFilter）
  const NetworkFailure({this.filtered = false});
  final bool filtered;
}
class LoginRequired extends SourceFailure {}       // 映射见下表，**不要**两边都写 301
class NoPermission extends SourceFailure {}        // 无版权 / VIP 墙 / fee
class NotFound extends SourceFailure {}
class RateLimited extends SourceFailure {}
class UpstreamChanged extends SourceFailure {}     // 解析失败、code 异常

class PlayUrlResult {
  final String url;
  final List<String> backupUrls;
  final Map<String, String> headers;   // 各源防盗链不同；**禁止**播放器写死酷狗 Referer
  final AppQuality? grantedQuality;    // 实际下发档位（可能降级）
  final bool isPreviewClip;            // 试听片段（网易 freeTrialInfo）
}
```

播放器映射：`LoginRequired` → 引导登录；`NoPermission` → 提示并尝试降档 / 切源策略（§4.3）；`RateLimited` → 退避重试。

**失败码映射（分源，禁止混用）**

| 统一错误 | 网易云 | 酷狗 |
| --- | --- | --- |
| `LoginRequired` | body `code == 301`（会话失效） | **无 301**；`errcode == 20028`（SSA 风控）等需重登/换 guest 重试时映射到此或 `RateLimited` |
| `NoPermission` | 曲目 `code==404` / `fee>0` / `cannotListenReason` / 无 url | `errcode == 20006`、`status==0` 且无 url |
| `RateLimited` | 业务限频码（以探针实测为准） | SSA `20028` 若仅需退避也可映射到此 |
| `UpstreamChanged` | 解析失败 / 未知 code | 解析失败 / 未知 errcode |

> 现状锚点：`play_repository.dart` 只认 `errcode`/`status`（含 `20028`/`20006`），**没有**任何 `300` 处理。`code=301` 只属于网易 weapi/eapi 链路。

### 3.5 音质映射

统一 UI 仍用 `AppQuality { standard, hq, sq, hiRes }`；平台档位在 adapter 内映射：

| AppQuality | 酷狗 `/v5/url` | 网易 `level` |
| --- | --- | --- |
| standard | `128` | `standard` |
| hq | `320` | `exhigh` |
| sq | `flac` | `lossless` |
| hiRes | `hires` | `hires` |

- 网易完整档位还有 `jyeffect`（高清环绕）/ `sky`（沉浸环绕）/ `jymaster`（超清母带）——作扩展档挂在网易侧，不进全局 `AppQuality`。
- 用户偏好按 **抽象档** 存全局；adapter 用「向下兼容候选」降档（对齐现有 `AudioQualityUtil.resolveCandidates` + `resolveUrlWithFallback`）。
- 下发结果用统一 `PlayUrlResult.grantedQuality` 回写，**废弃**/收敛现有 `ResolvedAudio.quality` 字符串（`128/320/…`）为映射后的枚举。

### 3.6 歌词统一包

现有 `LyricLine` 已含 `chars` / `translated` / `romanized` / `hasCharTiming`，`LyricsView` 只吃 `List<LyricLine>`。**不要再造平行歌词模型**；Source 返回：

```dart
class LyricPayload {
  final List<LyricLine> lines;  // 复用现有类型（逐字/译文/音译已在行内）
  final String sourceTag;       // 'kugou-krc' / 'netease-yrc' / … 仅调试/缓存元数据
}
```

- 酷狗：现有 KRC→LRC 流程原样迁入 adapter。
- 网易：YRC 优先映射为 `LyricLine.chars`，回退 LRC；译文 `ytlrc`/`tlyric` → `translated`；参考 NeriPlayer `NeteaseYrcParser`。
- UI 歌词组件不动。

---

## 4. 特殊能力处理（红心 FM 等）

### 4.1 分层

| 层 | 职责 |
| --- | --- |
| `HeartRadioSource` / `PersonalFmSource` | **取歌与反馈**（接口调用、参数、鉴权） |
| `FmController`（Application） | 会话缓冲、顶歌、垃圾桶/红心语义 |
| `PlayerController` + `PlaybackQueueSource.fm` | **播放推进规则**（不循环回首曲、上一首限会话内） |

播放规则对所有源的 FM 会话一致，**不**写进某个 `KugouSource`。

### 4.2 UI 显隐

```dart
final fm = registry.capability<PersonalFmSource>(activePlatform);
final heart = registry.capability<HeartRadioSource>(activePlatform);
// fm != null → 显示 FM 入口；heart != null → 显示红心模式切换
```

网易只有普通私人 FM 时：只实现 `PersonalFmSource`，无 `HeartRadioSource`，红心控件整块不出现（不是灰掉的假入口）。

### 4.3 跨源能力

「网易云无版权 → 降档 → 匹配 B 站/本地」属于 **Policy 层**（建议 `SourceFallbackPolicy`），调用多个 `MusicSource`，**不**放进单一 Source。一期可只做「同源降档」，跨源切歌二期再上。

降档与试听：`NoPermission` / `isPreviewClip` 时先走同源 `AppQuality.candidatesDownward`，仍失败再交给 Policy。现有 `PlayRepository.resolveUrlWithFallback` 的「token 失败改 guest 重试」等逻辑留在 `KugouSource` 内部；SSA `20028` 也在此层消化或上抛（映射见 §3.4）。

---

## 5. 登录与会话

| | 酷狗 | 网易云 |
| --- | --- | --- |
| 凭证 | `token` + `KUGOU_API_MID/GUID`（Authorization 头） | Cookie `MUSIC_U` + `__csrf`（weapi 预热） |
| 设备 | `dfid` 注册（现有 DeviceIdentity） | `_ntes_nuid` / `NMTID`；扫码另有 `yd_token` |
| 登录 UI | 现有 `LoginPage` | 扫码优先；WebView 账密可选 |
| 会话类 | 现有 `AuthTokenHolder` **只服务酷狗** | 新 `NeteaseCookieStore`（持久化 + 内存 CookieJar） |

**改造**

1. 废弃「全局唯一 `AuthTokenHolder` 服务所有请求」；改为 `KugouSession` / `NeteaseSession`，由各 Source 持有。
2. `KugoClient` 拦截器注入改为读 `KugouSession`；网易 Client 自管 Cookie 合并（参照 NeriPlayer `mergeNeteaseRequestCookies`）。
3. 会话预热：网易 WEAPI 前若缺 `__csrf` 先 GET 首页（`ensureWeapiSession`）。
4. 失败映射按 §3.4 分源表：网易 `code=301` → `LoginRequired`；酷狗按 `errcode`（如 `20028`）→ `LoginRequired` / `RateLimited`。由 Application 引导登录，**不在** HTTP 层弹窗。

**Windows 注意**：网易扫码 `yd_token` 在 NeriPlayer 里靠隐藏 WebView 跑页面指纹；Flutter Windows 无 WebView 时：P1 允许「游客搜索+播放」，登录仅 Android；或后续换桌面浏览器方案（成本高，单列任务）。

---

## 6. 现状耦合点与改造清单（已对照代码）

| # | 现状 | 问题 | 目标 |
| --- | --- | --- | --- |
| 1 | `Track.hash` / `mixSongId` / `albumId` 为一等公民 | 网易无 hash，无法当身份 | `platform + id`（酷狗 id=**mixSongId** 语义不变）；hash/albumId 等进 `extras` |
| 2 | `PlayerController` 直接依赖 `PlayRepository` + `LyricRepository` | 具体类绑死酷狗 | 依赖 `MusicSource`（或 `registry.ofTrack`） |
| 3 | 歌词去重键 `id\|hash` | 跨源冲突 | `platform:id` |
| 4 | `audio_service_handler` 用 `track.hash` 当 MediaItem.id | 混源会撞 | `platform:id` |
| 5 | `drift` 队列/历史表只有 `trackId/hash/…`，无 `platform` | 恢复队列丢源 | 加 `platform` 列 + 迁移；主键含 platform |
| 6 | `AuthTokenHolder` 全局单例 | 网易 Cookie 无处安放 | 分源 Session |
| 7 | `PlayRepository` / `KugoClient` 各自一套网络日志 | 日志分裂 | 统一 `NetworkLog` sink |
| 8 | 音质 `AppQuality.param` 写死 `128/320/flac/hires` | 网易档名不同 | `quality_map.dart` 双向映射 |
| 9 | `full_player_page` → `/song` 深链带 `hash`/`mixSongId` | 详情深链绑酷狗 | 改 `platform` + `id` |
| 10 | 收藏/我喜欢绑定酷狗账号语义 | 不能与网易我喜欢混同步 | `UserLibrarySource` 分源维护；UI 可按源 Tab |
| 11 | `PlayerController` 播放时写死 UA + `Referer: kugou.com` | 网易防盗链头不同 | 由 `PlayUrlResult.headers` 下发 |
| 12 | 已有 `ResolvedAudio`（url + backupUrls + quality 字符串） | 与统一 `PlayUrlResult` 重复 | 收敛为一种结果类型，mapper 层转换 |
| 13 | `PlaylistBrief.source`（int：1 歌单/2 专辑）+ 同类 `type`（0 自建/1 收藏） | 与「音源」及彼此撞名 | `source`→`listKind`；平台用 `platform`；`type` 保留原义 |
| 14 | 歌词类型已有 `LyricLine.chars/translated/romanized` | 若另造 `LyricPayload` 双轨会分叉 | 只加薄封装，行模型复用 |
| 15 | `cover_cache.dart` `_headers` 写死酷狗 `Referer`/UA | 网易封面 CDN 可能要求 `music.163.com` Referer，下载 403 | 按 URL host 或 `Track.platform` 下发请求头 |
| 16 | 酷狗 `Track.id` 已是 `mixSongId` 优先（`mappers.dart`） | 若误改成 hash 当 id，会破坏删曲 fileid、详情反查 | **保持** `mixSongId` 语义；hash 只进 `extras` |

---

## 7. 网易云 Source 实现要点（对齐 NeriPlayer）

> 范围说明：下表「对齐」指 NeriPlayer **已验证**的 weapi/eapi 业务链路。评论 / 热搜等不在其 `core/api/netease/` 内，表中标 ⚠️ 的项需自研或另找参考，**不要**当成已核实事实。

| 模块 | 做法 |
| --- | --- |
| `NeteaseCrypto` | Dart 直译 weapi/eapi/linux/plain；常量 `PRESET_KEY`/`IV`/公钥/`EAPI_KEY`/盐与 Netease 侧一致（事实型，勿改） |
| 请求 | Form POST；WEAPI 带 `csrf_token` query；处理 Brotli/Gzip（Dio 需显式支持 br 或换 `http`/自定义 transformer） |
| 搜索 | `/weapi/cloudsearch/get/web`，`type` **1/10/100/1000**（单曲/专辑/歌手/歌单） |
| 播放 URL | 主：EAPI `/song/enhance/player/url/v1`（level）；回退：WEAPI `/song/enhance/player/url`；301 会话预热重试 |
| 歌词 | 主：EAPI `/song/lyric/v1`（`NeteaseClient.getLyricNew`）；另有 GET `/api/song/lyric`（`CloudMusicSearchApi` 也用）。YRC→`LyricLine.chars`，失败回退 LRC |
| 歌单/专辑/艺人 | `/api/v6/playlist/detail`、`/weapi/v1/album/{id}`、`/api/v1/artist/...` |
| 歌单写 | `/weapi/playlist/manipulate/tracks`（对齐 `NeteasePlaylistTrackParams`；与酷狗 `addPlaylistTrack`/`deletePlaylistTracks` 同能力位） |
| 私人 FM | `/weapi/v1/radio/get`（`PersonalFmSource`） |
| 每日推荐 | `/v1/discovery/recommend/resource`、`/v3/discovery/recommend/songs` |
| 登录 | 扫码 `/weapi/login/qrcode/unikey` + `client/login`；账号密码 EAPI `/w/login/cellphone`（密码 MD5）。酷狗侧现有扫码/短信/密码三 Tab 可保留，仅换 Controller 绑定 `KugouSession` |
| 评论只读 | ⚠️ Netease 包无现成实现；可选自建 `/comment/music` 等，排期靠后 |
| 热搜 | ⚠️ 同上；酷狗侧 `/api/v3/search/hot` 已有可先共用 UI |

产物建议独立包 `core/api/netease/`，**禁止**被 UI 直接 import。

---

## 8. 分阶段落地

| 阶段 | 内容 | 验收 |
| --- | --- | --- |
| **A. 抽象落地（仍单源）** | `MusicSource` + `KugouSource` 委托现有 Repository；`Track` 加 `platform`（**id 保持 mixSongId 语义**）；`PlaylistBrief.source`→`listKind`；DB 迁移；Player/歌词/封面头改走接口下发 | 现有功能回归全绿（`app/test`） |

**阶段 A 不可跳过**——直接加第二源会把耦合写死，后期迁移成本更高。

> **网易云接入（探针 → 可听 → 账号 → 策略）不在本文档排期**，后续单独出《网易云接入排期》文档。  
> 本文只保留架构契约与 §7 实现要点，不写人日/里程碑。

### 8.1 阶段 A 任务拆解（酷狗抽象 · 已完成）

> 同步写入 `工期规划.md` Phase 5。口径：1 人全栈 · 5 人日/周。**不含**网易与产品二期功能。

| # | 任务 | 产出 | 人日 | 依赖 |
| --- | --- | --- | --- | --- |
| A1 | 契约包 `core/source/`：`MusicPlatform`、`MusicSource`、能力接口、`SourceFailure`、`PlayUrlResult`、`LyricPayload`、`quality_map` | 可编译 + FakeSource 单测 | 1.5 | — |
| A2 | `MusicSourceRegistry` + `main`/DI 装配（仅 `KugouSource`） | Provider 根可取 Source | 0.5 | A1 |
| A3 | 模型改造：`Track.platform`；id 钉死 **mixSongId** 语义；`hash` 等平台字段保留在 Track 旁路（未强迁 `extras` map）；`PlaylistBrief.source`→`listKind` | 全调用点改完 | 1.5 | A1 |
| A4 | `KugouSource` 委托现有 `*Repository`（搜索/播放/歌词/详情/FM/榜单/每日/我喜欢/评论只读） | 行为与 M3 持平 | 2 | A2, A3 |
| A5 | `PlayerController` / `LyricRepository` 入口 / `CoverCache`：**headers 与 resolve 走 Source** | 无写死酷狗 Referer | 1 | A4 |
| A6 | 酷狗 `errcode`→`SourceFailure`（20028/20006…）+ 收敛 `ResolvedAudio`→`PlayUrlResult` | 统一失败可处理 | 0.5 | A1, A4 |
| A7 | Drift：`platform` 列 + 队列/历史/likes 序列化迁移 | 恢复队列身份正确 | 1 | A3 |
| A8 | `AuthTokenHolder`→`KugouSession`（薄封装，可先兼容旧读写） | 第二源 Session 有位 | 0.5 | A4 |
| A9 | 网络日志统一 sink（KugoClient + 播放 URL） | 一套日志 | 0.5 | A4 |
| A10 | 回归：`analyze` + `app/test` + 主路径冒烟 | 零回退 | 1 | A5–A9 |
| A11 | 熄火缓冲 | — | 0.5 | — |

合计 **10 人日 ≈ 2 周**（含缓冲）。裁剪：A9→A8 可并；**不可砍** A1/A3/A4/A7/A10。

**A 出口标准（DoD · 现状）**

1. **Player** 不再 import `PlayRepository` / `LyricRepository` 具体类，改走 `MusicSource`（已达成）；部分功能页仍直接用 `*Repository`，第二源时再收薄（测试 Fake 除外）。
2. 全库身份键为 `(platform, id)`，深链不再传 `hash`（已达成；`hash` 仍作 Track 旁路字段供酷狗 Source 读）。
3. 播放/封面请求头由 Source 下发（已达成）。
4. `flutter analyze` 零问题；`app/test` 绿；M3 主路径人工冒烟通过。
5. 代码目录出现 `core/source/` + `data/sources/kugou/`，第二源可平行加目录而不改 Player。

---

## 9. 已知风险与决策记录

| 风险 | 缓解 |
| --- | --- |
| 网易协议/风控变更（weapi、Cookie、eapi） | Source 隔离；探针页；失败映射 `UpstreamChanged` |
| 酷狗网关「URL 过滤」 | 保留现有识别（`looksLikeUrlFilter`）→ `NetworkFailure(filtered: true)` |
| 双账号「我喜欢」语义混淆 | UI 分源 Tab，禁止静默合并 |
| 混源队列歌词缓存 key 冲突 | 一律 `platform:id` |
| `PlaylistBrief.source`/`type` 等历史字段撞名 | 阶段 A 一并改名，避免 `MusicPlatform` 与 int `source` 并存 |
| 播放 headers 写死酷狗 | `PlayUrlResult.headers` 必填由 Source 下发（网易通常要 `Referer: https://music.163.com`） |
| 封面下载 403 | 见耦合 #15：`CoverCache` 头按源下发；**缓存 key 已是 `sha1(url)`，跨源不会撞**，无需改 key |
| Windows 网易登录 | 一期降级游客；不阻塞 Android |
| 能力接口过多难维护 | 按域合并（FM 与红心可合可分）；新增能力先问「是否两源都有」 |
| 合规 | 维持「个人学习/研究、不中转音频、不绕过付费」声明；多源后 README 同步改产品定位 |

**明确不做（本方案）**

- 运行时插件热加载 / 第三方源市场
- 跨源「同一首歌」自动匹配合并成一条（可作为远期 Policy）
- 把 EchoMusic 全量插件 API 迁过来

---

## 10. 测试策略

| 层 | 做法 |
| --- | --- |
| 单元 | `quality_map`、统一错误映射、`(platform,id)` 稳定性、歌词 payload 映射 |
| 假源 | `FakeMusicSource` / `FakePersonalFmSource` 驱动 `PlayerController`、FM 规则、能力显隐 |
| 适配器 | 各源用 `docs/api-notes` 式固定 JSON fixture 测 mapper（不依赖外网） |
| 冒烟 | 新源接入时保留 debug 探针（对齐现有 `tool/probe_*.dart`） |

---

## 11. 界面改造摘要（不重做视觉）

| 页面 | 动作 |
| --- | --- |
| 搜索 | 音源筛选/分段；结果来源角标 |
| 发现 / 首页 | 卡片按能力生成；无 FM 则无 FM 卡 |
| 播放页 / 歌词 / 队列 | **复用**；仅数据入口改为 `ofTrack` |
| 歌单/专辑/歌手 | 复用骨架；深链参数带 platform |
| 我喜欢 / 资料库 | 按源分组或 Tab |
| 登录 | 酷狗现有页 + 网易扫码页 |
| 设置 | 音源管理（默认源、游客开关、登出分源） |

**决策记录（2026-09-24）：采用「区域分治」，不做全局音源切换。**

理由：两源功能差异**已由可选能力接口消化**——`HeartRadioSource`/`QualityCatalogSource` 酷狗独有，
`DailyRecommendSource`/`RankSource`/`SearchHotSource` 网易侧，基类只留
`searchSongs`/`resolvePlayUrl`/`fetchLyric` 三个同构方法。全局切换不额外解决任何差异，
却会：让搜索退化为单源（两源各有独占曲库，换源要进设置再搜一遍）、发现页需每源各做一套、
把「我喜欢」变成信息孤岛（私有数据本就分源）。

替代方案与取舍：

- **全局切换 + 搜索跨源**（备选）：心智简单，但发现页仍需两套，仅作备选。
- **纯全局切换**：实现最简，但放弃「哪家有版权用哪家」，已否决。

「切换」诉求由三个轻量机制满足：设置里的**默认源**、搜索里的**音源筛选**、我的里的**按源分 Tab**。
边界沿用 §10：**不做**跨源同一首歌自动匹配合并，混排时同一首歌可能两条。

### 11.1 落地进度（2026-09-24）

| 层 | 状态 |
| --- | --- |
| 契约层 `core/source/`（MusicSource / capabilities / registry / quality_map） | ✅ 已完成 |
| 网易协议层 `core/api/netease/`（crypto / client / endpoints / failures） | ✅ 探针实测收口（见 [网易云接口文档.md](网易云接口文档.md) §五） |
| 网易适配层 `core/api/netease/netease_mappers.dart` + `data/sources/netease/netease_source.dart` | ✅ 已完成（`MusicSource` + G3/G4/G10 + F5） |
| 启动装配 `data/sources/sources.dart` | ✅ 酷狗 + 网易云（`registerDefaultMusicSources`，原先在 `kugou_source.dart` 尾部） |
| 搜索混排 / 来源角标 / 音源筛选 | ✅ 已完成（2026-09-24） |
| 网易扫码登录（`DeviceLoginSource` + 登录页 + 登录态落盘） | ✅ 已完成（2026-09-24，实机扫到 803 并拿到账号） |
| 我的 / 我喜欢 按源分 Tab | ✅ 已完成（2026-09-24，改为「歌曲 Tab 内源筛选条」口径） |
| 设置页：默认源 | ✅ 已完成（2026-09-24，只作搜索/我喜欢的**初始值**） |
| 设置页：分源账号（网易云扫码登录入口 + 登出） | ✅ 已完成（2026-09-24） |

**搜索混排口径（2026-09-24 落地）**：`SearchController` 依赖 `MusicSourceRegistry`，对当前
`sourceFilter` 命中的源**并行**取同一页，再按轮询交错（`A0 B0 A1 B1 …`）拼接；**不做**跨源同曲去重。
单源失败静默忽略（记入 error），**全部失败才抛**。混排下没有单一 `total`，改由控制器显式计算
`hasMoreFlag`（任一源还有下一页即为 true），`SearchTabState.hasMore` 优先用它、其次 `total`、
最后退回「整页」启发式。UI 侧：多源时结果行显示来源角标（`SourceBadge`，文案取 `MusicPlatform.label`），
顶部 `_SourceFilterBar` 提供「全部 / 各平台」筛选（仅 ≤1 源时隐藏）。

网易适配层的边界口径（播放不降档循环、`level` 映射、YRC 逐字、榜单固定 id、FM 语义、
**旧口单曲封面 `picId` 回退**）见
[网易云接口文档.md](网易云接口文档.md) §九。

**网易扫码登录口径（2026-09-24 落地）**：链路走 `DeviceLoginSource` 能力接口，UI 只见
`LoginQrSession` / `LoginQrStatus`，`unikey` / `chainId` / `MUSIC_U` 全部留在 `NeteaseSource` 内部。
二维码内容是 `scanlogin` URL（不是裸 unikey）；轮询带 `x-loginmethod: QrCode` + `x-login-chain-id`
且**不做 weapi 预热、不带 csrf**；状态码 800 过期 / 801 待扫 / 802 待确认 / 803 成功。803 后由
`confirmQrLogin` 用 `account/get` 验账号（失败则把响应头 `x-refresh-token` 当 `MUSIC_U` 合并再验）。
登录态由 `data/storage/netease_auth_store.dart` 落盘（key `netease.auth.v1`），`main.dart` 启动时
`restoreInto` 灌回共享客户端，否则重启即掉登录。协议细节见
[网易云接口文档.md](网易云接口文档.md) §三 E 组。**2026-09-24 实机验证**：`yd_token` 可省略
（`ydDeviceToken` 发空串即走到 803 并拿到账号），故网易侧**不实现** WebView 设备指纹。

**「我喜欢」分源口径（2026-09-24 落地）**：不改动既有三维 Tab（歌曲/歌手/专辑），改为在**歌曲 Tab 内**
加一条源筛选条（复用搜索页同款 `SourceFilterBar`，见 `shared/widgets/common.dart`）：`全部` 时酷狗曲目在前、
网易曲目在后顺序拼接（**不做**跨源同曲去重，沿用 §10）；歌手/专辑 Tab 仍只走酷狗。
网易侧数据入口是能力接口 `UserLibrarySource.likedTracks()`（`core/source/capabilities.dart`），
`NeteaseSource` 实现为 F3（`song/like/get` 取 `ids`）→ A4（`song/detail` **100 id/批**）两跳，
映射在 `mapNeteaseLikedSongIds` / `mapNeteaseSongDetails`；页面态由
`features/likes/netease_likes_controller.dart` 承载（登录态异步回填后自动拉取、登出自动清空）。
**红心（取消喜欢）按钮只对 `MusicPlatform.kugou` 曲目显示**：网易写口 F4/F5 未实测，误点会拿网易
`songId` 打酷狗删曲接口。

**「默认源」口径（2026-09-24 落地）**：设置项 `settings.defaultSource`（`MusicPlatform`，落盘 wire 值，
未知回退酷狗），只作**初始值**——搜索页与「我喜欢」页首次进入时用它预选源筛选条，用户当次仍可临时切到
`全部`。**不是**全局音源切换（§11 决策）。搜索侧由 `SearchController.applyDefaultSourceFilter` 施加，
并用 `_sourceFilterTouched` 保证不覆盖用户的手动选择（`setSourceFilter` 一旦被调用即置位，哪怕选回同一值）。

---

## 12. 一页纸心智模型

> **统一契约（`MusicSource`）+ 能力可选（`*Source` 接口）+ 注册表查找（`Registry`）+ 身份带平台（`platform:id`）。**
> 酷狗红心 FM 不进基类；网易没有对应能力就不实现，UI 不显示。
> 插件感来自「模块边界清晰」，而不是「运行时可装卸」。
>
> 复核已钉死：`PlaylistBrief.source`→`listKind`、播放/封面 headers 由 Source 下发、歌词复用 `LyricLine`、搜索 type=1/10/100/**1000**。
> 外审已钉死：酷狗**已有**歌单加/删曲；301 只属网易；封面 key 是 `sha1(url)` 不会撞；酷狗 id=**mixSongId** 不是 hash。
